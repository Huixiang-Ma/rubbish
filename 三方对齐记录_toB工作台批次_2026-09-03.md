# 三方需求互评与对齐记录 · toB 工作台批次

> 文档版本：v1.0
> 日期：2026-09-03
> 评审对象：《docs/superpowers/specs/2026-09-03-tob-workbench-design.md》（已批准规格）、《docs/superpowers/plans/2026-09-03-tob-workbench.md》（15 任务实现计划）、《工单要点提取与落点映射.md》
> 依据：工单方法论（任务一 §1.1-1.2）——编码前必须完成 Human + AI-A + AI-B 三方转述、互评与裁决；验收红线：至少 3 个业务/工程异常点 + 学生明确裁决。

---

## 1. 参与方职责

| 参与方 | 职责 | 关注点 |
|---|---|---|
| Human / 学生 | 裁决冲突、圈定范围、控制工期 | 业务合理性、工作量、交付质量 |
| AI-A（逻辑分析） | 需求转述、业务边界异常挖掘、闭环检查 | 场景、异常、演示叙事 |
| AI-B（工程实现） | 技术可行性、并发/状态/接口规格、实现风险 | 可落地性、一致性、回归风险 |

## 2. 需求转述（三方一致确认）

把 toB 工作台（`/b`）从"功能齐全但体验停留在基础版"改造为能与 toC 新水准对齐的企业管控台：去 AI 味专业视觉 + 侧边栏信息架构 + 方案详情抽屉（行程书/决策溯源/审计/记忆挂载点）+ 改单与父子版本 diff + 客户管理 + 客户之声 + 待审角标；后端仅 4 处最小改动；工单 7 主体（图记忆/强干预）独立成批，本批仅留 UI 挂载点。诚实口径全程不变。

## 3. AI-A 评审记录（逻辑 / 业务边界）

**核心判断**：规格覆盖了竞品叙事的三大主张，范围克制；以下 4 个业务闭环缺口需要在实现中明确口径：

- **A1 改单 409 无出路**：审批/改单并发时会遇到 `VERSION_CONFLICT`，计划只有 toast 报错，顾问拿到错误后没有下一步动作。
- **A2 "未归属"客户语义不可达**：客户管理页"未归属"行点击会清空客户筛选跳转列表——用户永远无法专门查看未归属任务。
- **A3 抽屉内容陈旧窗口**：抽屉打开期间任务从 RUNNING 流转到 COMPLETED，内容不刷新；演示时提交后立刻打开抽屉必然撞上。
- **A4 客户之声计数口径**：按 kind 筛选时另一侧计数恒为 0，计数卡若不标注会误导读数。

## 4. AI-B 评审记录（工程 / 实现风险）

**核心判断**：15 任务计划可执行，后端 TDD 用例可直接落地；以下 4 个工程风险需要裁决或顺手修复：

- **B1 前端窗口口径**：搜索与客户聚合都基于 limit=500 窗口，超窗口漏数据——规格只在客户管理页标注，方案列表页同样需要"窗口内"措辞，全站统一。
- **B2 CSV 导出链接在鉴权模式下 401**（计划遗漏）：`<a href="/api/audit/export.csv">` 直链无法携带 Bearer，`AUTH_ENABLED=true` 时与任务 8 修的四处一样会 401。修法：改 `apiFetch` + blob 下载（一处小改）。
- **B3 ECharts 重复 init**：切页签多次 `echarts.init` 同一 DOM 会累积实例并告警——现状已存在，顺手 `echarts.dispose` 后再 init。
- **B4 diff/读接口未挂管理守卫**：与现有 `/{job_id}/audit` 同层级（plans router 无 dependencies）。保持现状则 AUTH_ENABLED 只保护 admin_router 口径不变；统一挂守卫则 toC 复用读接口会被波及，改动面大。

**已知债务（记录不阻塞）**：`mdToHtml` 与 toC 页重复实现（单文件架构的 DRY 例外）；memory 挂载点空态属工单 7 边界。

## 5. 交叉互评

**AI-A 评 AI-B**：B2 是业务可见缺陷，必须本批修（升级为 D1 裁决项）；B1 同意，标注措辞全站统一为"（当前数据窗口内）"；B3 同意列入任务 8 顺手项，不单独开任务。

**AI-B 评 AI-A**：A1 最小闭环 = 409 toast 文案附"任务已被他人处理，请刷新后重试"，不做自动重试（YAGNI）；A2 建议"未归属"行禁用跳转 + title 解释，不做特殊筛选参数；A3 建议抽屉仅打开时拉取 + 关闭重开即刷新，不做自动轮询（演示口径，避免与后端轮询叠加）；A4 计数卡附"当前筛选口径"小字。

## 6. 关键异常点对齐结果（满足红线：≥3 个）

| 异常点 | 共识 | 裁决 |
|---|---|---|
| 409 版本冲突无出路 | toast 文案带刷新引导，不做自动重试 | 采纳 AI-B 最小闭环 |
| CSV 导出鉴权 401 | 直链改 blob 下载 | 待 Human 裁决（D1） |
| 前端窗口漏数据 | 全站"当前数据窗口内"统一标注 | 采纳 |
| 抽屉内容陈旧 | 打开时拉取，不做自动轮询 | 待 Human 裁决（D3） |
| ECharts 重复 init | dispose 后再 init，并入任务 8 | 采纳 |

## 7. 需要 Human 裁决的问题

见下方 D1-D4（已由 Human 裁决，结果记入 §8）。

## 8. Human 裁决结果

> 裁决于 2026-09-03 三方对齐会话中给出：

| 编号 | 问题 | Human 裁决 |
|---|---|---|
| D1 | CSV 导出在 AUTH_ENABLED=true 下 401：修复（blob 下载）还是维持标注？ | **修复**（并入任务 8） |
| D2 | 客户管理页“未归属”行点击行为：禁用跳转还是跳转全部？ | **禁用跳转** + title 解释 |
| D3 | 抽屉内任务状态刷新：打开时拉取 vs 5s 自动轮询？ | **5s 自动轮询**，抽屉打开期间刷新状态与可用内容 |
| D4 | diff 等读接口是否统一挂管理守卫？ | **维持现状**（与 toC 读接口一致，AUTH_ENABLED 只保护 admin_router） |

## 9. 最终对齐结论

> 规格与 15 任务计划维持已批准版本，叠加本记录的 5 处微调：①任务 8 增补 CSV blob 下载修复（D1）；②任务 8 增补 ECharts dispose；③任务 9 改单 409 文案带刷新引导；④任务 12 "未归属"行禁用跳转；⑤任务 9/12/13 统一"当前数据窗口内"标注、计数卡加筛选口径小字。裁决后即按计划进入实现（采用 executing-plans 内联执行：任务 7-15 涉及浏览器验证，Browser Use 仅主代理可用，不采用子代理驱动）。
