# 多 Agent 文旅系统 · 实现顺序梳理（从零到交付）

> 版本：v1.0 ｜ 日期：2026-08-28
> 上位文档：《网页端需求文档 v2.0》《开发任务分解与排期表》——本文回答"**先做什么、后做什么、为什么**"
> 任务编号沿用 T001–T037

---

## 0. 排序的五条原则（先讲清楚为什么这么排）

1. **先垂直切片，再横向加厚** —— 第一步就要让"输入一句话 → 页面出现行程"跑通，哪怕只用 1 个 Agent。**绝不先写完整编排框架再写 Agent**（那样到 D3 你还没有任何可演示的东西）。
2. **数据契约最先定** —— `travel_plan.md` 的 schema 必须在写第二个 Agent 之前定稿，否则每加一个 Agent 都要改结构。
3. **先上游 Agent，后下游 Agent** —— 调研/规划/行程是"产出型"，情感/安全/校验/汇报是"消费型"。上游没定稿就加下游，必然反复返工。
4. **先核心差异，后锦上添花** —— "算得准 / 说得清 / 可验证"优先于玩法和彩头。差异化能力先做完，才有砍的余地。
5. **可砍的最后做** —— 彩头、看板、多租户放最后，时间不够第一个砍的就是它们。

---

## 1. 主干实现顺序（Step 0 → Step 8）

---

### Step 0 · 垂直切片：**最先跑通"1 个 Agent 端到端"**

| 序 | 做什么 | 对应任务 | 为什么在这个位置 |
|---|---|---|---|
| 0.1 | 仓库与前后端脚手架 | T001 | 一切的地基 |
| 0.2 | LLM 客户端封装，跑通一次调用 | T002 | 先确认模型能调通，避免后面全盘卡死 |
| 0.3 | **定稿 `travel_plan.md` schema** | T003 | **数据契约最先定**，否则加 Agent 必返工 |
| 0.4 | docker compose 可一键起 | T004 | 早做，保证你一直在真环境里开发 |
| 0.5 | **1 个 Planner Agent 端到端**：输入 → LLM → 输出 md → 前端渲染 | T005 部分 + T016 部分 | 垂直切片，最早看到成果 |

- **做完标志**：输入"北京 3 天 5000 元"，页面出现一份可读行程。
- **卡住的退路**：LLM 调不通 → 先用固定样例数据打通前后端链路，LLM 后置接入（不要在这一步死磕）。
- ⚠️ **本步就要定死的架构决策**：**同步 vs 异步**。若 QPS≥200 要达标，必须异步（提交入队返回 job_id）。这个决策后期改要返工一半代码。

---

### Step 1 · 从 1 个 Agent 展开到 8 个

| 序 | 做什么 | 对应任务 |
|---|---|---|
| 1.1 | 从已有的 1 个 Agent 抽象出 Agent 基类 + 编排框架 | T005 |
| 1.2 | **上游产出型**：Researcher → Planner → Itinerary | T006 |
| 1.3 | **中游**：Budget | T006 |
| 1.4 | **下游消费型**：Sentiment → Safety → Validator → Reporter | T006 |

- **顺序理由**：下游 Agent 消费上游输出。上游 schema 没定稿就加下游，每改一次上游就要重调下游。
- **做完标志**：八个 Agent 的输出都在 `travel_plan.md` 里可见且各有实质贡献。
- **卡住的退路**（很关键）：时间紧就**只保留 4 个核心 Agent**（调研 / 规划 / 行程 / 校验），其余降级为同一 Agent 内的多段 prompt——对外仍称"八段职责"，工程上诚实说明是"四 Agent 实现八段职责"。

---

### Step 2 · 算得准：时空可行性校验

| 序 | 做什么 | 对应任务 |
|---|---|---|
| 2.1 | 本地静态景点库（经纬度 + 预计算通勤） | T008 |
| 2.2 | 校验器接入 Validator，拒绝不可行行程并给原因 | T009 |

- **做完标志**：输入"上午故宫下午长城当天往返"→ 拒绝并输出"两地车程 1.5h，当天闭园前无法覆盖"。
- **卡住的退路**：全国库来不及 → 只做演示城市的 20–30 个景点，够演示即可。
- ⚠️ 标注：静态库跑通，**"实时路网"不得承诺**。

---

### Step 3 · 可验证：断点续传 + 红蓝对抗

| 序 | 做什么 | 对应任务 |
|---|---|---|
| 3.1 | 状态持久化 + `resume_from` 锚点 | T011 |
| 3.2 | 断点续传 + kill-9 容灾演练 | T011 + T015 |
| 3.3 | Attack Agent（影子通道）+ Safety 拦截 | T012 + T013 |
| 3.4 | 红蓝对抗安全治理看板 | T014 |

- **顺序理由**：3.1 是 3.2 的前提（先有持久化才能断点）；3.3/3.4 相对独立，可与人并行。
- **做完标志**：强杀进程 5 秒内恢复、前 5 个已抓网页不重抓；注入攻击被拦截且看板实时计数。
- **卡住的退路**：锚点恢复做不顺 → 退而保证"重跑不重复抓网页"（缓存去重），5 秒锚点续传降级为"秒级重启 + 缓存命中"。
- 💡 **T014 是全场最佳演示钩子**，建议答辩开场演。

---

### Step 4 · 改了不崩：增量重规划

| 序 | 做什么 | 对应任务 |
|---|---|---|
| 4.1 | 依赖 diff 引擎 | T010 |
| 4.2 | 增量重规划接口 + 前端 diff 高亮 | T010 + T018 |

- **做完标志**：改一句需求，只重算受影响节点，返回"变了 N 处 / 保留 M 处"，**耗时较全量重算降低 ≥70%**（不承诺 <3s，真实 LLM 链路做不到）。
- **卡住的退路**：细粒度依赖 diff 太难 → 退到"按天/节点"粒度重算（粗粒度 diff），仍然算增量、仍然成立。

---

### Step 5 · toB 管控：挂起、审核、审计、交付

| 序 | 做什么 | 对应任务 |
|---|---|---|
| 5.1 | HITL 后端挂起 + 回滚 | T024 |
| 5.2 | 前端 HITL 审核台 | T026 |
| 5.3 | 前端顾问工作台（客户/方案管理、改单、版本对比） | T025 |
| 5.4 | 合规审计后台 + 方案白标交付 | T027 + T028 |

- **顺序理由**：5.1 是 5.2 的前提（先有后端挂起才有审核台）；5.3/5.4 相对独立。
- **做完标志**：超预算时流程挂起并可审批（reject 回滚）；审计日志可导出，含"谁/何时/改了什么/是否人工确认"。

---

### Step 6 · toC 玩法（互相独立，可任意顺序，可砍）

按**推荐优先顺序**排列（不是随意排的）：

| 优先级 | 玩法 | 任务 | 为什么这个顺序 |
|---|---|---|---|
| 🥇 1 | **辩论式可解释** | T017 + T018 | **"说得清"的核心，不可砍** |
| 🥈 2 | **翻车预演** | T022 | 演示效果最好、成本最低（Validator 多输出一段） |
| 🥉 3 | **虚拟游客 swarm** | T019 | 多 Agent 独有名片，单 LLM 抄不动 |
| 4 | 反事实后悔药 | T020 | 纯 LLM，2h 就能出 |
| 5 | 心情 → 人生剧本 | T021 | 换 prompt 模式，便宜 |
| 6 | 直播辩论大屏 | T023 | 效果好但前端成本高，可砍 |
| 7 | 角色名导团 | T030 | 锦上添花，最先砍 |

- **这七个互相独立**，谁有空谁做，时间不够从下往上砍。

---

### Step 7 · 部署与压测

| 序 | 做什么 | 对应任务 |
|---|---|---|
| 7.1 | 压测脚本 + QPS 验证 | T034 |

- ⚠️ **提醒**：docker（T004）应在 Step 0 就做；**异步任务架构是 Step 0 的决策，不是 Step 7 的事**。若 Step 0 定了同步，这里 QPS≥200 不可能达标。

---

### Step 8 · 收尾与交付

| 序 | 做什么 | 对应任务 |
|---|---|---|
| 8.1 | 彩头（盲盒/足迹/年报/徽章）+ 攻略长图 | T032 |
| 8.2 | 经营看板 + 多租户 | T029 + T031 |
| 8.3 | 两大场景端到端联调 | T035 |
| 8.4 | 全流程录屏 + 演示话术彩排 | T036 |
| 8.5 | 文档收尾（README/部署手册/诚实声明） | T037 |

- **8.1/8.2 是最先砍的**；8.3–8.5 是验收必需，**绝不能砍**。
- **录屏务必预演**：LLM 有不确定性，别裸跑现场演示，必须预录 + 缓存兜底。

---

## 2. 哪些必须严格串行，哪些可以并行

### 必须串行（改顺序必返工）
```
T001 脚手架 → T002 LLM封装 → T003 schema定稿 → T005 编排框架
   → T006 八Agent（上游→下游）→ T007 结构化输出
   → T010 增量重规划 → T018 diff高亮
   → T035 联调 → T036 录屏 → T037 文档
T011 状态持久化 → T015 断点续传演练
T024 HITL后端 → T026 审核台
T012 Attack → T013 Safety → T014 安全看板
```

### 可并行（谁有空谁做）
- **T008 静态景点库**（纯数据，任何时候都能做）
- **T016 前端页面**（与后端并行，先用 mock 数据）
- **Step 6 的七个玩法**（互相独立）
- **T029 经营看板 / T031 多租户 / T032 彩头**（toB/toC 收尾项）

> **并行能力取决于人数**：1 人 → 全串行只做 MVP；2 人 → A 轨后端 + B 轨前端；3 人 → 再加 C 轨（基建/压测/录屏/文档）。

---

## 3. 每个 Step 结束的自查（卡住就往下看）

| Step | 自问 | 不过怎么办 |
|---|---|---|
| 0 | 输入一句话，页面能出一份行程吗？ | LLM 不通 → 先用样例数据打通链路 |
| 1 | 八个 Agent 的输出都在 md 里且各有贡献吗？ | 时间紧 → 降到 4 个核心 Agent |
| 2 | 能拒绝"故宫+长城当天往返"吗？ | 库不够 → 只做演示城市 20–30 个景点 |
| 3 | 强杀后 5 秒续传？注入被拦且看板计数？ | 锚点难 → 退到缓存去重 + 秒级重启 |
| 4 | 改一句只动受影响节点、耗时降 ≥70%？ | 细 diff 难 → 退到按天粒度重算 |
| 5 | 超预算能挂起审批？审计能导出？ | 后端挂起是核心，不可降级 |
| 6 | 至少做完辩论 + 1 个玩法？ | 其余从下往上砍 |
| 7 | QPS 达标吗？ | 不达标 → 确认是否异步架构，同步则不可能 |
| 8 | 两大场景录屏完成？文档齐？ | 这是验收底线，必须完成 |

---

## 4. 一句话收口

> **实现顺序的本质是"先跑通最细的一根线，再一根根加粗"**：Step 0 用一个 Agent 打通端到端 → Step 1 展开到八个 Agent（先上游后下游）→ Step 2/3/4 加"算得准/可验证/改了不崩"三大核心差异 → Step 5 toB 管控 → Step 6 toC 玩法（可砍）→ Step 7/8 压测与交付。**schema 最先定、异步架构 Step 0 就决策、彩头最后做**，这三条守住，顺序就不会乱。
