# 文旅系统补全 · 实现 Agent 任务简报（全批次）

> 版本：v1.0 ｜ 日期：2026-09-04
> 用途：本文件按批次拆分，**每份简报可独立复制发给实现 Agent**。完整实现规格（数据契约、代码骨架、测试样例）见《补全方案_背景对齐_实现工单.md》，两者冲突时以工单为准、以代码现状为准。
> 项目根目录：`D:\Desktop\WL项目`（FastAPI + Python 3.12，Agent 管线在 `backend/app`）

---

# 批次 1：Sentiment 舆情 Agent + 抓取内容安全过滤（P0，不可砍）

【任务简报｜批次 1】

你在为「多 Agent 文旅系统」补齐与任务书对齐的缺口。完整规格见《补全方案_背景对齐_实现工单.md》**P0-1 / P0-2** 两节。

**任务 A：新增 Sentiment 舆情 Agent**
1. 新增 `backend/app/agents/sentiment.py`：继承 `app/agents/base.py` 的 `AgentBase`，`name="Sentiment"`；`run(context)` 读取 `Researcher` 的 spots 与 `Itinerary` 每日景点，逐景点输出舆情分类（`BENIGN / NEGATIVE_REVIEW / SERVICE_RISK / SAFETY_RISK / SCAM_RISK / UNKNOWN`），返回 `{"agent":"Sentiment","status":"ok","payload":{"reviews":[...],"risk_alerts":[...],"summary":"..."}}`。LLM real 模式可用 `llm_client.try_generate_json` 增强，失败/未配置回退本地库；无数据且 LLM 不可用时输出 `UNKNOWN` 保守文案（"建议出行前自行确认"）。
2. 新增 `backend/app/data/sentiment_reviews.json`：键名**必须覆盖** `scenic_spots_beijing.json` 全部景点；至少 2 条 `SAFETY_RISK`/`SCAM_RISK`、若干 `NEGATIVE_REVIEW`；字段：`risk_level / highlights / warnings / source`（source 标注"本地舆情库（演示口径）"）。
3. 管线接入：`backend/app/services/pipelines.py` 两管线在 `Validator` 后插入 `"Sentiment"`（toC：…Validator→Sentiment→Debate→Mood→Reporter；toB：…Validator→Sentiment→Consultant→Compliance→Reporter）；`backend/app/services/plan_processor.py` 的 `self.pipelines` 在 `ValidatorAgent()` 后同样插入。**不要**加入 `TAIL_PARALLEL_NAMES`。
4. `markdown_reporter.py`：插入"舆情与避坑提示"章节（toC 放"翻车预演"前、toB 放"合规审计摘要"旁），用 `outputs.get("Sentiment")` 兜底旧任务，注意 `summary_no / index_no` 编号同步 +1。

**任务 B：抓取内容安全过滤**
1. `backend/app/services/safety_service.py` 新增 `scan_payload(payload) -> (cleaned, events)`：递归遍历所有 str 字段——命中 `HIGH_RISK_PATTERNS` 替换为 `"[已移除风险片段]"` 并产出 `web_content_block` 事件；命中 `MEDIUM_RISK_PATTERNS` 走 `sanitize()` 并产出 `web_content_sanitize`；`SAFETY_COUNTER_KEYS` 增加这两项。
2. `plan_processor.py`：每个 Agent `run` 之后、`mark_node_done` 之前调用 `scan_payload`；事件写 `audit.log`（`web_content_block` 时 `SAFETY_BLOCKED.inc()` + `pg_mirror.record_safety`）；`_run_tail` 的并行结果同样处理。
3. 安全看板 `api/safety.py` 透传 counters 即可。

**硬约束**：不改现有模块对外接口签名；旧任务字段一律 `.get` 兜底；禁止 Agent 直接写 `travel_plan.md`；不新增第三方依赖。

**验收**：
```bash
cd D:\Desktop\WL项目\backend
python -m compileall app scripts
PYTHONIOENCODING=utf-8 python scripts/smoke_test.py
pytest tests -x
```
新增单测 `tests/test_sentiment_agent.py`、`tests/test_safety_content_filter.py`；手动验证：提交"北京 3 天 5000 元"→ 行程书出现"舆情与避坑提示"章节且含 ≥1 条 risk_alert；恶意输入仍触发安全挂起。

**交付**：改动文件清单、回归结果、与工单的差异说明；更新 `README.md` Agent 清单与诚实声明。

---

# 批次 2：来源 URL 溯源 + 白名单网页抓取（P1）

【任务简报｜批次 2】

你在为「多 Agent 文旅系统」补齐来源追溯与网页调研能力。完整规格见《补全方案_背景对齐_实现工单.md》**P1-1 / P1-2** 两节。

**任务 A：来源 URL 溯源**
1. `backend/app/services/scenic_spot_service.py`：spot 增加 `source_urls` 字段——高德 POI 命中景点填 `f"https://www.amap.com/search?query={urllib.parse.quote(name)}"`；静态库兜底景点留空数组（如实无来源）。
2. `backend/app/agents/itinerary.py`：`_enrich_with_fliggy` 命中时把 `booking_url` 追加进 `item["spot"]["source_urls"]`（先去重）。
3. `backend/app/services/markdown_reporter.py`：景点条目新增"信息来源"行——逐个 `[来源名](url)`；无 URL 时输出"静态演示数据，无外部来源链接"。
4. 新增 `backend/tests/test_source_traceability.py`：断言高德链接生成、飞猪链接追加、静态兜底为空且渲染不报错。

**任务 B：白名单网页抓取（演示口径，默认关闭）**
1. 新增 `backend/app/services/web_research.py`（标准库 `urllib.request` + `html.parser`，**不新增依赖**）：
   - `ALLOWLIST = {"www.dpm.org.cn", "www.badaling.gov.cn"}`，非白名单 URL 直接拒绝；
   - `fetch_page_text(url) -> str | None`：仅白名单、超时 5s、失败返回 None、间隔限速 ≥1s；
   - `extract_rules(html_text, spot_name) -> dict | None`：返回 `{"open_time","booking_rule","fetched_at","source_url"}`；
   - 不采集个人信息、不提交表单、不绕过访问控制。
2. `backend/app/agents/researcher.py`：仅当环境变量 `WEB_RESEARCH_ENABLED=true` 且景点在白名单时调用；成功则覆盖 `open_time`、新增 `booking_rule` 与 `fetched_at`/`source_url` 标记；失败静默回退原逻辑。**默认关闭，零配置行为与现状完全一致**。
3. `.env.example` 增加 `WEB_RESEARCH_ENABLED`；新增 `backend/tests/test_web_research.py`（mock `urlopen`，不发真实请求；断言非白名单拒绝、解析正确、失败降级）。

**硬约束**：抓取能力必须白名单硬编码 + 默认关闭；不新增依赖；不改主链路默认行为。

**验收**：
```bash
cd D:\Desktop\WL项目\backend
python -m compileall app scripts
PYTHONIOENCODING=utf-8 python scripts/smoke_test.py
pytest tests -x
```
手动验证：行程书每个景点有"信息来源"行；配置 `WEB_RESEARCH_ENABLED=true` 后故宫景点 open_time 带 `fetched_at` 与官网 URL。

**交付**：改动文件清单、回归结果、README 诚实声明补充（舆情库与网页抓取为演示口径）。

---

# 批次 3：RBAC 角色审批控制（P1）

【任务简报｜批次 3】

你在为「多 Agent 文旅系统」补齐角色审批控制。完整规格见《补全方案_背景对齐_实现工单.md》**P1-3** 节。

**任务**
1. `backend/app/services/auth_service.py`：
   - 账号表新增演示账号：`supervisor`（主管）、`consultant`（顾问），密码风格与现有 `admin/wl2026` 一致，并写入 `README.md` 演示账号区；
   - 确认 `issue_token` 已带角色（`"r": role`）、`verify_token(token, role=...)` 已支持角色校验，复用即可。
2. 新增 FastAPI 依赖 `require_role(*roles)`：解析 `Authorization` Bearer token，角色不在列表返回 `403 FORBIDDEN_ROLE`；无 token 返回 401。
3. `backend/app/api/plans.py` 应用：
   - `POST /api/plans/{job_id}/approval` → `require_role("supervisor", "admin")`
   - `POST /api/plans/{job_id}/replan` → `require_role("consultant", "supervisor", "admin")`
   - `POST /api/plans/{job_id}/feedback` → `require_role("consultant", "supervisor", "admin")`
4. 新增 `backend/tests/test_rbac.py`：traveler 角色调 approval → 403；supervisor → 放行；无 token → 401/403。

**硬约束**：不改变现有 admin/traveler 登录流程；角色校验只加在管理类端点，不拦截游客端（toC）只读接口。

**验收**：
```bash
cd D:\Desktop\WL项目\backend
python -m compileall app scripts
PYTHONIOENCODING=utf-8 python scripts/smoke_test.py
pytest tests -x
```
手动验证：toB 登录页可切换 admin/supervisor/consultant；游客 token 审批返回 403。

**交付**：改动文件清单、回归结果、README 演示账号表更新。

---

# 批次 4：独立 Intake Agent + 独立 Budget Agent（P2，重构）

【任务简报｜批次 4】

你在为「多 Agent 文旅系统」对齐任务书 8 Agent 清单，把 Intake（偏好解析）与 Budget（预算）从"由他处承担"拆为独立 Agent。完整规格见《补全方案_背景对齐_实现工单.md》**P2-1 / P2-2** 节。**本批次为重构，回归要求最高。**

**任务 A：独立 Intake Agent**
1. 新增 `backend/app/agents/intake.py`：`AgentBase` 子类，`name="Intake"`；`run(context)` 读取原始 `user_input`，输出：
```json
{"agent":"Intake","status":"ok","payload":{
  "normalized_input": {"destination":"北京","days":3,"budget":5000,"travelers":2,"preferences":["博物馆"],"constraints":["减少打车"],"parsed_flags":{"transit_first":true,"avoid_early_rise":false}},
  "missing": [], "warnings": []
}}
```
   - 归一化：目的地去"市"后缀；travelers 缺省 2；preferences/constraints 去重；"减少打车/优先地铁/经济实惠"→ `transit_first=true`（与 `itinerary.py` 现有口径一致）。
2. `pipelines.py` 两管线开头插入 `"Intake"`；`plan_processor.py` 实例化 IntakeAgent，并在其完成后 `context["user_input"] = intake_payload["normalized_input"]`（后续节点消费规范化输入；`state` 保留原始输入用于展示）。
3. 新增 `tests/test_intake_agent.py`：断言归一化、缺省填充、去重、transit_first 判定。

**任务 B：独立 Budget Agent（预算职责从 Validator 拆出）**
1. 新增 `backend/app/agents/budget.py`：迁移 `validator.py` 的 `_budget_breakdown` / `_economy_total` / `_travel_advice` / `_economy_tips` / `_what_if` 中预算相关部分；输出字段**与原 Validator 预算字段完全同名**（`estimated_budget / budget_breakdown / approval_required / economy_total / economy_tips`），最小化下游改动。
2. `validator.py`：删除预算逻辑，只保留强度、跨区跨度、通勤超时与翻车预演的非预算部分。
3. `pipelines.py`：两管线改为 `… Itinerary → Budget → Validator → …`（Sentiment 仍在 Validator 后）。
4. `plan_processor.py`：实例化 BudgetAgent；`WAITING_BUDGET_APPROVAL` 挂起判定迁移到 Budget 节点之后（逻辑不变，仅位置迁移；审批恢复后从 Validator 续跑，`_next_node` 自动生效）。
5. `markdown_reporter.py`：`validation["estimated_budget"]` 改读 `outputs["Budget"]["payload"]`，用 `.get` 双兜底旧任务。
6. 新增 `tests/test_budget_agent.py`；**重点回归**：`smoke_test.py` 第 3 条（预算挂起恢复）与第 6 条（增量重规划）必须通过。

**硬约束**：字段名保持不变；旧任务（无 Budget 输出）读取不报错；不改 `state.json` 结构。

**验收**：
```bash
cd D:\Desktop\WL项目\backend
python -m compileall app scripts
PYTHONIOENCODING=utf-8 python scripts/smoke_test.py
pytest tests -x
```
手动验证：预算 100 元 → 挂起 → 主管审批 → 恢复完成；行程书预算章节正常。

**交付**：改动文件清单、回归结果、与工单的差异说明；更新 `README.md` Agent 清单。

---

# 批次 5：两份产出物报告（P2）

【任务简报｜批次 5】

你在为「多 Agent 文旅系统」补两份产出物文档（放项目根目录），内容必须基于真实代码，不得虚构。

**任务 A：《Loop Engineering 提示词与自主验证报告.md》**
1. **各 Agent 提示词清单**：从 `backend/app/agents/*.py` 提取 LLM real 模式 prompt（Planner `_llm_plan`、Itinerary `_enrich_with_llm`、Debate、Mood、Consultant 等），逐个说明输入结构、输出 JSON schema、temperature、max_tokens。
2. **自主验证机制**：`llm_client.try_generate_json` 的 JSON 解析与失败重试（max_retries + 退避）、`_chat_via_httpx` → `_chat_via_socket` 降级通道、mock 兜底。
3. **验证闭环**：`scripts/smoke_test.py` 六条链路、`scripts/llm_offline_test.py`、`pytest backend/tests` 覆盖清单。
4. **Loop 改进记录**：引用真实修复案例（如 `compute_schedule` 溢出修复为"次日 HH:MM"、短途步行不调公交规划、高德 TLS 抖动退避重试），说明"发现→修复→验证"闭环。

**任务 B：《AI 代码审查报告.md》**
- 真实审查 `backend/app` 代码，找出 **3-5 个潜在 bug/隐患**，每条固定格式：
```
### Bug N：<一句话标题>
- 位置：<文件:行号>
- 现象/影响：
- 根因：
- 修复建议：
- 验证方式：
```
- 候选线索（**须自行核实后再写，不得照抄、不得为凑数虚构**）：
  1. `travel_context_service.py` `recommend_hotel` 的 `adjusted` 标记逻辑是否恒为 True；
  2. `plan_processor.py` 尾段并行 `pool.map` 与断点恢复顺序补跑的边界；
  3. `markdown_reporter.py` 编号变量 `summary_no / index_no` 分支自增是否正确；
  4. `fliggy_client.py` 签名重试与缓存 TTL 边界；
  5. `validator.py` `economy_total` 与 `approval_required` 在 travelers>2 时口径是否合理。
- 修复建议：低风险问题可直接修代码并跑回归；不影响行为的按"记录+建议"处理。

**验收**：两份文档齐全；报告中的每个引用（文件:行号、prompt、机制）都能在代码中核实；如修改了代码，`compileall` + `smoke_test.py` + `pytest tests -x` 必须通过。

**交付**：两份 md 文档路径 + 代码改动清单（如有）。

---

## 全局提示（所有批次通用）

- 每批次独立 git commit，message 标注批次号。
- 每个批次完成后必须跑通全局回归（compileall + smoke_test + pytest）。
- 发现本简报与代码现状不符时，以代码为准，并在交付说明中注明差异。
- 时间受限时砍减顺序：P2 → P1-2 → P1-1；**P0 两项不可砍**。
