# 多 Agent 文旅系统 · 任务二执行方案

> 文档版本：v1.0  
> 编制日期：2026-08-28  
> 输入依据：《任务一最终交付文档.md》及任务二执行原则  
> 阶段定位：任务二——MVP 工程实现与核心链路验证  
> 执行原则：先预留必须接入的接口，用 4 个核心 Agent 跑通异步可恢复闭环，再补安全、校验、挂起和演示能力，最后才考虑玩法和彩头。

---

## 目录

1. 任务二目标
2. 总体执行原则
3. 必须预留和接入的接口
4. MVP 技术架构
5. 4 个核心 Agent 实现方案
6. 异步可恢复闭环设计
7. 安全、校验、挂起能力补齐顺序
8. 前端最小实现范围
9. 推荐目录结构
10. 分阶段执行计划
11. 验收标准
12. 可砍项与后置项
13. 风险与兜底策略
14. 结论

---

## 1. 任务二目标

任务二的目标是将任务一中的架构方案落成一个可运行、可演示、可验收的 MVP 系统。

核心目标包括：

1. 跑通“用户输入 → 创建异步任务 → Worker 执行 → 4 个核心 Agent 协作 → 状态持久化 → Reporter 生成 `travel_plan.md` → 前端展示”的闭环；
2. 先预留必须接入的接口，避免后续接 LLM、Redis、状态存储、审批接口时大改结构；
3. 不一开始追求完整 8 个物理 Agent；
4. 不一开始追求炫酷前端；
5. 优先保证异步、可恢复、可审计、可演示；
6. 后续再补 Prompt 注入防御、时空校验、预算挂起、增量重规划、安全看板和玩法。

---

## 2. 总体执行原则

任务二执行时必须遵循以下原则：

### 2.1 先跑通主链路

第一目标不是功能全，而是先跑通最短闭环：

```text
输入一句旅行需求
  ↓
POST /api/plans 返回 job_id
  ↓
Worker 后台执行
  ↓
4 个核心 Agent 生成结构化结果
  ↓
Reporter 生成 travel_plan.md
  ↓
前端展示结果
```

### 2.2 先 4 Agent，后 8 职责展开

MVP 阶段不强制实现 8 个物理 Agent，先实现 4 个核心 Agent：

- Researcher；
- Planner；
- Itinerary；
- Validator。

文档和代码结构应预留 8 类职责扩展点，后续再拆出：

- Budget；
- Sentiment；
- Safety；
- Reporter。

其中 Reporter 在 MVP 可以先作为独立服务函数实现，也可以作为轻量 Agent 实现。

### 2.3 先接口预留，后真实接入

必须先把接口、抽象类、配置项留好。即使第一版使用 mock 数据，也要保证后续接入真实 API 时不改主流程。

### 2.4 先异步可恢复，后炫酷功能

MVP 优先级：

1. 异步任务；
2. 状态查询；
3. checkpoint；
4. 原子写；
5. 4 Agent 主链路；
6. Prompt 注入基础防御；
7. 时空校验；
8. 预算挂起；
9. 演示与录屏；
10. 玩法和彩头。

### 2.5 不夸大验收口径

- QPS≥200 只指任务提交接口；
- 5 秒恢复只指恢复调度；
- MVP 不承诺实时路网；
- MVP 不承诺全国景点库；
- MVP 不承诺完整 8 Agent 物理拆分。

---

## 3. 必须预留和接入的接口

任务二中需要优先预留接口边界。接口分为“必须实现的内部业务接口”“必须预留的外部 API 接口”“后置可选接口”。

### 3.1 必须实现的后端业务接口

#### 3.1.1 创建规划任务

```http
POST /api/plans
```

职责：

- 接收用户旅行需求；
- 生成 `request_hash`；
- 防重复提交；
- 创建 `job_id`；
- 写入任务队列；
- 立即返回 `QUEUED`。

请求：

```json
{
  "destination": "北京",
  "days": 3,
  "budget": 5000,
  "preferences": ["亲子", "博物馆", "美食"],
  "constraints": ["减少打车", "不要太赶"]
}
```

响应：

```json
{
  "job_id": "plan_20260828_001",
  "status": "QUEUED"
}
```

#### 3.1.2 查询任务状态

```http
GET /api/plans/{job_id}
```

职责：

- 查询任务状态；
- 返回当前执行节点；
- 返回进度；
- 返回恢复锚点；
- 返回错误信息。

响应：

```json
{
  "job_id": "plan_20260828_001",
  "status": "RUNNING",
  "current_agent": "Planner",
  "progress": 40,
  "resume_from": "planner.generate_outline",
  "version": 1,
  "error": null
}
```

#### 3.1.3 获取规划结果

```http
GET /api/plans/{job_id}/result
```

职责：

- 获取最终 `travel_plan.md`；
- 返回版本号；
- 返回 hash；
- 支持前端展示。

响应：

```json
{
  "job_id": "plan_20260828_001",
  "version": 1,
  "travel_plan_md": "# 北京 3 日游...",
  "sha256": "..."
}
```

#### 3.1.4 人工审批接口

```http
POST /api/plans/{job_id}/approval
```

任务二第一阶段可以先预留接口，第二阶段补实现。

职责：

- 处理预算超限审批；
- 处理高风险内容人工确认；
- 防重复审批；
- 写审计日志。

请求：

```json
{
  "decision": "approve",
  "operator": "student",
  "reason": "预算超出但可接受",
  "base_version": 1
}
```

响应：

```json
{
  "status": "APPROVED",
  "next_node": "Reporter"
}
```

#### 3.1.5 增量重规划接口

```http
POST /api/plans/{job_id}/replan
```

任务二第一阶段先预留，后续按天或节点粒度实现。

请求：

```json
{
  "change_request": "预算改为 3000 元，减少打车",
  "base_version": 1
}
```

响应：

```json
{
  "job_id": "plan_20260828_002",
  "parent_job_id": "plan_20260828_001",
  "status": "QUEUED",
  "replan_mode": "incremental"
}
```

---

### 3.2 必须预留的外部 API 接口

这些外部 API 不一定第一天全部真实接入，但代码必须预留抽象层。

#### 3.2.1 LLM API 接口

必须预留，优先真实接入。

用途：

- 行程规划；
- Agent 推理；
- 可解释文本生成；
- Prompt 注入分类；
- Output Parse fallback。

建议封装为：

```text
LLMClient.generate(prompt, schema=None, temperature=0.2)
LLMClient.generate_json(prompt, schema)
LLMClient.classify(prompt, labels)
```

必须支持两种模式：

| 模式 | 用途 |
|---|---|
| `real` | 调真实模型 API |
| `mock` | LLM 不可用时返回固定样例，保证链路可演示 |

配置项建议：

```text
LLM_PROVIDER
LLM_API_KEY
LLM_MODEL
LLM_TIMEOUT_SECONDS
LLM_MAX_RETRIES
LLM_MODE=real/mock
```

#### 3.2.2 队列接口

必须预留，建议直接接 Redis Streams。

封装为：

```text
QueueClient.enqueue(job)
QueueClient.consume(worker_id)
QueueClient.ack(job_id)
QueueClient.requeue(job_id, reason)
```

MVP 降级：

- Redis 可用：使用 Redis Streams；
- Redis 不可用：使用 SQLite / JSON 文件任务表模拟队列。

#### 3.2.3 状态存储接口

必须预留。

封装为：

```text
CheckpointStore.load(job_id)
CheckpointStore.save(job_id, state)
CheckpointStore.mark_node_done(job_id, node, output_hash)
CheckpointStore.get_resume_from(job_id)
```

MVP 可用：

- 本地 `state.json`；
- SQLite；
- Redis Checkpointer。

#### 3.2.4 文件写入接口

必须实现。

封装为：

```text
AtomicWriter.write_text(path, content)
AtomicWriter.write_json(path, data)
AtomicWriter.verify_sha256(path, expected_hash)
```

职责：

- `.tmp` 写入；
- flush；
- fsync；
- rename；
- sha256 校验。

#### 3.2.5 安全检测接口

必须预留，第一版可规则实现。

封装为：

```text
SafetyService.scan_user_input(text)
SafetyService.scan_untrusted_content(text)
SafetyService.classify_prompt_injection(text)
SafetyService.sanitize(text)
```

MVP 实现：

- 关键词规则；
- 正则匹配；
- 可选 LLM 分类。

---

### 3.3 后置可选外部 API

任务二初期不建议接入以下 API，只保留扩展点：

| API | 用途 | 任务二策略 |
|---|---|---|
| 地图 API | 实时路线、通勤时间、经纬度 | 不接，先用静态景点库和预计算通勤 |
| 搜索 API | 实时抓取攻略、景点评价 | 不接，先用本地样例数据 |
| OCR API | 图片攻略、票据识别 | 不接，除非后续明确需要 |
| 舆情 API | 外部舆情风控 | 不接，用关键词 + LLM 分类代替 |
| 短信/邮件 API | 审批通知 | 不接，前端状态展示即可 |
| 对象存储 API | 存储 PDF/长图/附件 | 不接，本地文件即可 |
| 支付/订单 API | 真实交易 | 禁止纳入 MVP |

---

## 4. MVP 技术架构

### 4.1 MVP 架构图

```text
前端最小页面
  ↓
FastAPI 后端
  ├─ POST /api/plans
  ├─ GET /api/plans/{job_id}
  ├─ GET /api/plans/{job_id}/result
  ├─ POST /api/plans/{job_id}/approval 预留/实现
  └─ POST /api/plans/{job_id}/replan 预留/实现
  ↓
任务队列 QueueClient
  ├─ Redis Streams
  └─ 本地任务表降级
  ↓
Worker
  ↓
4 核心 Agent
  ├─ Researcher
  ├─ Planner
  ├─ Itinerary
  └─ Validator
  ↓
Reporter / Markdown Renderer
  ↓
AtomicWriter
  ↓
data/jobs/{job_id}/travel_plan.md
```

### 4.2 MVP 状态流

```text
QUEUED
  ↓
RUNNING: Researcher
  ↓
RUNNING: Planner
  ↓
RUNNING: Itinerary
  ↓
RUNNING: Validator
  ↓
COMPLETED
```

第二阶段补齐：

```text
WAITING_RATE_LIMIT
WAITING_BUDGET_APPROVAL
WAITING_SAFETY_REVIEW
WAITING_PARSE_REVIEW
RECOVERY_REQUIRED
FAILED
CORRUPTED
```

---

## 5. 4 个核心 Agent 实现方案

### 5.1 Researcher Agent

职责：

- 读取用户输入；
- 查询本地静态景点库；
- 根据偏好筛选候选景点；
- 输出景点候选、开放时间、推荐理由和风险提示。

输入：

```json
{
  "destination": "北京",
  "days": 3,
  "budget": 5000,
  "preferences": ["博物馆", "亲子"]
}
```

输出：

```json
{
  "agent": "Researcher",
  "spots": [
    {
      "name": "故宫博物院",
      "tags": ["历史", "博物馆"],
      "open_time": "08:30-17:00",
      "visit_minutes": 180,
      "risk_notes": []
    }
  ]
}
```

### 5.2 Planner Agent

职责：

- 根据候选景点生成每日主题；
- 初步安排景点组合；
- 控制每天强度；
- 初步考虑预算。

输出：

```json
{
  "agent": "Planner",
  "days": [
    {
      "day": 1,
      "theme": "历史文化经典线",
      "spot_names": ["故宫博物院", "景山公园"]
    }
  ]
}
```

### 5.3 Itinerary Agent

职责：

- 细化每天行程；
- 增加时间段；
- 增加交通建议；
- 增加餐饮建议；
- 增加可解释理由。

输出：

```json
{
  "agent": "Itinerary",
  "itinerary": [
    {
      "day": 1,
      "items": [
        {
          "time": "09:00-12:00",
          "title": "参观故宫博物院",
          "transport": "地铁至天安门东站",
          "reason": "符合博物馆偏好，适合作为第一天核心景点"
        }
      ]
    }
  ]
}
```

### 5.4 Validator Agent

职责：

- 校验通勤时间；
- 校验闭园时间；
- 校验每日强度；
- 校验预算超限；
- 校验用户约束；
- 输出修正建议。

输出：

```json
{
  "agent": "Validator",
  "status": "needs_fix",
  "findings": [
    {
      "type": "spatial_conflict",
      "level": "high",
      "message": "故宫与长城安排在同一天时间过紧",
      "suggestion": "将长城调整到第 2 天"
    }
  ]
}
```

### 5.5 Reporter / Markdown Renderer

职责：

- 汇总 4 个核心 Agent 的结构化输出；
- 生成 `travel_plan.md`；
- 调用 `AtomicWriter` 原子写入；
- 计算 sha256；
- 更新任务状态。

Reporter 不是唯一状态源，只负责生成展示产物。

---

## 6. 异步可恢复闭环设计

### 6.1 任务创建

```text
POST /api/plans
  ↓
生成 job_id
  ↓
初始化 state.json
  ↓
入队
  ↓
返回 QUEUED
```

### 6.2 Worker 执行

```text
Worker consume job
  ↓
获取 job lock
  ↓
读取 checkpoint
  ↓
执行 resume_from 对应节点
  ↓
每个 Agent 完成后保存 output + checkpoint
  ↓
Reporter 写入 Markdown
  ↓
COMPLETED
```

### 6.3 checkpoint 最小字段

```json
{
  "job_id": "plan_20260828_001",
  "status": "RUNNING",
  "current_node": "Planner",
  "last_success_node": "Researcher",
  "resume_from": "Planner",
  "version": 1,
  "completed_nodes": ["Researcher"],
  "agent_outputs": {
    "Researcher": "agent_outputs/researcher.json"
  },
  "updated_at": "2026-08-28T10:00:00"
}
```

### 6.4 恢复规则

| 情况 | 恢复策略 |
|---|---|
| `state.json` 存在且 hash 正常 | 从 `resume_from` 继续 |
| Agent 输出存在且 hash 正常 | 不重复执行该 Agent |
| Worker 中断在 LLM 调用前 | 重新执行当前 Agent |
| Worker 中断在输出后、checkpoint 前 | 允许重跑，但优先读缓存 |
| `travel_plan.md` hash 不一致 | 重新由结构化输出生成 |
| `state.json` hash 不一致 | 进入 `RECOVERY_REQUIRED` |

---

## 7. 安全、校验、挂起能力补齐顺序

### 7.1 第一阶段：先跑通主链路

只做最小安全：

- 用户输入关键词检测；
- 明显 Prompt 注入拦截；
- Agent 输出 JSON parse 校验。

### 7.2 第二阶段：补时空校验

优先实现：

- 北京静态景点库；
- 经纬度；
- 开放时间；
- 预估游玩时间；
- 景点间预计算通勤时间；
- “故宫 + 长城当天往返”不可行演示。

### 7.3 第三阶段：补挂起审批

优先实现预算超限：

```text
Validator / Budget 判断超预算
  ↓
WAITING_BUDGET_APPROVAL
  ↓
POST /approval
  ↓ approve / reject
```

### 7.4 第四阶段：补 Prompt 注入防御

实现：

- `SafetyService.scan_user_input`；
- `SafetyService.scan_untrusted_content`；
- 高风险 block；
- 中风险 summary；
- 安全事件记录。

### 7.5 第五阶段：补演示能力

实现：

- 安全看板计数；
- 断点恢复按钮或演示脚本；
- 增量重规划；
- diff 摘要；
- 录屏脚本。

---

## 8. 前端最小实现范围

任务二前端不追求炫酷，只做最小可用界面。

### 8.1 必须页面

| 页面 | 功能 |
|---|---|
| 首页 / 创建任务页 | 输入目的地、天数、预算、偏好，提交任务 |
| 任务状态页 | 展示 `job_id`、状态、当前 Agent、进度 |
| 结果页 | 渲染 `travel_plan.md` |

### 8.2 第二阶段页面

| 页面 | 功能 |
|---|---|
| 审批页 | 审批预算超限或高风险内容 |
| 重规划页 | 输入变更需求并展示 diff 摘要 |
| 安全事件页 | 展示 Prompt 注入拦截次数和证据 |

### 8.3 暂不做

- 直播大屏；
- 复杂动画；
- 角色名导团；
- 经营看板；
- 多租户管理；
- 年报、徽章、盲盒等彩头。

---

## 9. 推荐目录结构

```text
project/
  backend/
    app/
      main.py
      api/
        plans.py
        approvals.py
        replans.py
      agents/
        base.py
        researcher.py
        planner.py
        itinerary.py
        validator.py
      services/
        llm_client.py
        queue_client.py
        checkpoint_store.py
        atomic_writer.py
        safety_service.py
        scenic_spot_service.py
        markdown_reporter.py
      models/
        schemas.py
        states.py
      workers/
        plan_worker.py
      data/
        scenic_spots_beijing.json
    tests/
  frontend/
    src/
      api/
        plans.ts
      pages/
        CreatePlanPage.tsx
        PlanStatusPage.tsx
        PlanResultPage.tsx
      components/
        PlanForm.tsx
        StatusBadge.tsx
        MarkdownViewer.tsx
  data/
    jobs/
  docker-compose.yml
  README.md
```

---

## 10. 分阶段执行计划

### 阶段 1：接口骨架与单 Agent 闭环

目标：先跑通输入到 Markdown 展示。

任务：

1. 初始化后端 FastAPI；
2. 初始化前端最小页面；
3. 定义请求、响应、状态枚举 schema；
4. 封装 `LLMClient`，支持 `real/mock`；
5. 实现 `POST /api/plans`；
6. 实现单 Planner 或简化 Planner；
7. 生成 `travel_plan.md`；
8. 前端展示结果。

验收：

> 输入“北京 3 天 5000 元”，页面能展示一份可读行程。

### 阶段 2：异步任务与 Worker

目标：从同步生成改为异步 `job_id`。

任务：

1. 实现 `QueueClient`；
2. 实现 Worker；
3. 实现 `GET /api/plans/{job_id}`；
4. 实现 `GET /api/plans/{job_id}/result`；
5. 前端轮询状态；
6. 任务完成后展示结果。

验收：

> 创建任务接口立即返回 `job_id`，前端可以看到 `QUEUED → RUNNING → COMPLETED`。

### 阶段 3：4 个核心 Agent

目标：形成 MVP 多 Agent 主链路。

任务：

1. 实现 `AgentBase`；
2. 实现 `ResearcherAgent`；
3. 实现 `PlannerAgent`；
4. 实现 `ItineraryAgent`；
5. 实现 `ValidatorAgent`；
6. 实现 `MarkdownReporter`；
7. 每个 Agent 输出 JSON；
8. Reporter 汇总生成 Markdown。

验收：

> `travel_plan.md` 中能看到调研、规划、详细行程、校验建议等内容。

### 阶段 4：checkpoint 与原子写

目标：实现可恢复和防文件损坏。

任务：

1. 实现 `CheckpointStore`；
2. 每个节点完成后写 `state.json`；
3. 实现 `AtomicWriter`；
4. 生成 `.sha256`；
5. Worker 启动时读取 `resume_from`；
6. 模拟中断恢复。

验收：

> Worker 中断后重新启动，可以从最近完整节点继续；`travel_plan.md` 不出现半截写入。

### 阶段 5：安全与校验

目标：补齐安全演示和不可行行程识别。

任务：

1. 实现 `SafetyService`；
2. 增加 Prompt 注入规则；
3. 实现北京静态景点库；
4. 实现通勤时间校验；
5. 实现闭园时间校验；
6. 增加安全事件记录。

验收：

> “忽略之前所有指令”能被识别；“上午故宫下午长城”能被判定不可行或建议拆分。

### 阶段 6：挂起、审批与重规划

目标：补齐 HITL 与基础变更能力。

任务：

1. 实现 `WAITING_BUDGET_APPROVAL`；
2. 实现审批接口；
3. 审批请求带 `base_version`；
4. 重复审批返回 `409`；
5. 实现基础重规划接口；
6. 返回 diff 摘要。

验收：

> 超预算任务会挂起，审批后继续或回滚；修改预算后能生成新版本。

### 阶段 7：演示、压测与交付

目标：整理可验收成果。

任务：

1. 准备 2–3 个固定演示 case；
2. 准备 mock 数据和缓存兜底；
3. 压测 `POST /api/plans` 提交接口；
4. 录屏；
5. 补 README；
6. 补部署手册；
7. 补诚实声明。

验收：

> 项目可以稳定演示，文档明确 QPS、恢复、实时路网、Agent 数量等边界。

---

## 11. 验收标准

任务二完成时，至少满足以下标准：

| 编号 | 验收项 | 标准 |
|---|---|---|
| A1 | 创建任务 | `POST /api/plans` 返回 `job_id` 和 `QUEUED` |
| A2 | 状态查询 | 前端能显示 `QUEUED / RUNNING / COMPLETED / FAILED` |
| A3 | 结果展示 | 前端能渲染 `travel_plan.md` |
| A4 | Agent 链路 | 至少 4 个核心 Agent 有结构化输出 |
| A5 | 异步执行 | API 不等待完整 LLM 生成 |
| A6 | 状态恢复 | Worker 重启后能从 `resume_from` 恢复调度 |
| A7 | 原子写 | Markdown 不出现半截文件，存在 sha256 校验 |
| A8 | Prompt 注入 | 高风险输入能被拦截 |
| A9 | 时空校验 | 明显不可行路线能被识别 |
| A10 | 预算挂起 | 超预算任务进入审批状态 |
| A11 | 审计 | 审批和安全事件有记录 |
| A12 | 交付 | README、部署说明、录屏和诚实声明齐全 |

---

## 12. 可砍项与后置项

### 12.1 任务二不优先做

- 完整 8 个物理 Agent；
- 直播辩论大屏；
- 角色名导团；
- 盲盒、徽章、年报；
- 经营看板；
- 多租户；
- 全国景点库；
- 实时路网；
- 真实支付或真实订单系统。

### 12.2 时间不足时优先砍

1. 彩头玩法；
2. 经营看板；
3. 多租户；
4. 完整审核台前端；
5. 细粒度增量 diff；
6. 8 个物理 Agent 拆分。

### 12.3 不能砍

- 异步 `job_id`；
- 任务状态查询；
- 4 个核心 Agent；
- `travel_plan.md` 输出；
- Reporter 单点写；
- checkpoint；
- 基础 Prompt 注入检测；
- 基础时空校验；
- 录屏和交付文档。

---

## 13. 风险与兜底策略

| 风险 | 影响 | 兜底策略 |
|---|---|---|
| LLM API 调不通 | 主链路无法生成 | `LLM_MODE=mock`，使用固定样例数据 |
| Redis 不可用 | 异步队列受阻 | 降级为 SQLite / JSON 文件任务表 |
| 8 Agent 来不及 | 工程量过大 | 4 Agent 承担 8 职责 |
| 景点数据不足 | 校验不稳定 | 只做北京演示城市数据 |
| 断点恢复不稳定 | 演示失败 | 预录 + 缓存已完成节点 |
| Prompt 注入误判 | 安全体验不稳定 | 高风险规则明确，未知默认保守 |
| 前端时间不足 | 展示弱 | 保留 3 个最小页面，不做炫酷效果 |
| 增量 diff 太复杂 | 开发超时 | 按天或节点粗粒度重算 |

---

## 14. 结论

任务二的合理执行方案是：

> 先把必须接入的接口边界留出来，再用 4 个核心 Agent 跑通异步可恢复闭环；随后补 Prompt 注入防御、时空校验、预算挂起、增量重规划和演示能力；最后再考虑玩法、彩头、多租户和经营看板。

任务二不追求“一开始功能全”，而追求“主链路稳定、架构正确、异常可解释、演示可复现”。只要守住异步任务、状态恢复、结构化输出、Reporter 单点写入和安全防御这五条主线，项目就具备继续扩展为完整 8 Agent 文旅系统的基础。
