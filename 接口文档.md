# 多 Agent 文旅系统 · API 接口文档

> 版本：2026-09-01（对应任务二 MVP 当前代码）
> 本文档面向直接调用本系统 API 的前端 / 第三方 / AI Agent。所有接口均为 JSON over HTTP（除 SSE 直播与 CSV 导出）。

## 0. 快速开始

| 项 | 值 |
|---|---|
| 本地服务 | `http://127.0.0.1:8000`（Docker 备用端口 `8010`） |
| 交互式文档 | `GET /docs`（Swagger UI）、`GET /redoc` |
| 认证 | 默认关闭（`AUTH_ENABLED=false`）；开启后 toB 管理类接口需 `Authorization: Bearer <token>` |
| 演示账号 | toC：`旅者 / 123456`；toB：`admin / wl2026` |
| 统一配置 | 外部真实 API 密钥统一配在项目根 `.env`（见 `.env.example` 与 `backend/app/api_registry.py`），未配置自动回退演示口径 |

创建一个行程并拿到结果的最短调用链：

```bash
# 1) 创建行程（异步，立即返回 job_id）
curl -X POST http://127.0.0.1:8000/api/plans \
  -H "Content-Type: application/json" \
  -d '{"destination":"北京市","days":3,"budget":8000,"origin":"上海","preferences":["亲子","博物馆","美食"]}'

# 2) 轮询状态（约 1-3 秒完成；也可看 /docs）
curl http://127.0.0.1:8000/api/plans/<job_id>

# 3) 取行程书（Markdown）
curl http://127.0.0.1:8000/api/plans/<job_id>/result
```

状态机取值：`QUEUED → RUNNING → COMPLETED`，中途可能进入 `WAITING_SAFETY_REVIEW`（注入拦截）或 `WAITING_BUDGET_APPROVAL`（超预算挂起），异常终态：`FAILED / CORRUPTED / RECOVERY_REQUIRED / REPLAN_REQUIRED`。

---

## 1. 行程规划 `/api/plans`

### 1.1 创建行程 `POST /api/plans`

请求体（`*` 为必填）：

| 字段 | 类型 | 说明 |
|---|---|---|
| destination* | string | 目的地，如 `北京市` |
| budget* | int | 预算金额（元），≥0 |
| days | int | 天数，1-14，默认 3 |
| travelers | int | 出行人数，1-20，默认 2（住宿每 2 人一间、打车每 3 人一车） |
| origin | string? | 出发地；不填不计大交通费用 |
| departure_date / return_date | string? | 出发/返回日期（YYYY-MM-DD，展示用） |
| mood | string? | 心情词（可多选逗号分隔），填写后生成"心情剧本"章节 |
| preferences / constraints | string[] | 偏好 / 约束标签 |
| customer / tenant | string? | toB 客户归属 / 租户标识 |

响应：`{"job_id": "plan_xxx", "status": "QUEUED"}`

幂等：同参数重复提交返回同一 `job_id`；生成器升级版本号后同参数会生成新任务。

### 1.2 行程列表 `GET /api/plans`

Query：`status`（按状态筛选）、`customer`、`tenant`、`limit`（默认 100）。
响应：`{"total": n, "items": [{job_id, status, progress, destination, origin, days, budget, customer, version, created_at, updated_at}]}`

### 1.3 任务状态 `GET /api/plans/{job_id}`

响应：`{job_id, status, current_agent, progress, resume_from, version, error, parent_job_id, completed_nodes}`
`version` 为乐观锁版本号，审批/重规划必须回传；`parent_job_id`/`completed_nodes` 供 toB 详情抽屉的版本对比入口与节点步骤条使用。

### 1.4 行程书结果 `GET /api/plans/{job_id}/result`

响应：`{job_id, version, travel_plan_md, sha256}`；`travel_plan_md` 为完整 Markdown（含每日行程、按时间排序的四餐推荐、辩论、翻车预演等章节）。

### 1.5 审计 `GET /api/plans/{job_id}/audit`

返回该任务全部审计事件（安全扫描、审批、完成等）。

### 1.6 审批 `POST /api/plans/{job_id}/approval`

请求体：`{"decision":"approve|reject","operator":"姓名","reason":"原因","base_version":<当前version>}`
- 挂起状态（`WAITING_*`）下才可审批；版本不符返回 `409 VERSION_CONFLICT`，状态非法返回 `409 INVALID_STATE`
- `approve` 后任务从 `resume_from` 节点续跑

### 1.7 增量重规划 `POST /api/plans/{job_id}/replan`

请求体：`{"change_request":"预算改为3000元，减少打车","base_version":<当前version>}`
响应：新 `job_id` + `diff_summary`（`incremental_by_request`，未变化节点直接复用，耗时更低）。子任务 state 落盘 `parent_job_id` 供版本对比。

### 1.8 反馈 `POST /api/plans/{job_id}/feedback`

请求体：`{"kind":"praise|complaint","operator":"姓名","content":"内容"}`

### 1.9 白标导出 `POST /api/plans/{job_id}/export`

请求体：`{"brand":"企业品牌名","consultant":"顾问署名"}` → 生成 `travel_plan_branded.md`（头部含中立声明）并返回 sha256。

### 1.10 版本对比 `GET /api/plans/{job_id}/diff`

Query：`base`（可选，指定父任务；默认取子任务 state 的 `parent_job_id`）。
响应：`{job_id, parent_job_id, added, removed, unchanged, truncated, lines:[{type:same|add|del, text}]}`（difflib 行级，上限 2000 行）。非重规划任务返回 `400 NOT_A_REPLAN`；任意文件未生成 `travel_plan.md` 返回 404。

---

## 2. 体验接口 `/api/plans/{job_id}/…`

| 接口 | 方法 | 说明 |
|---|---|---|
| `/map` | GET | 每日景点地图数据（含经纬度、舒适度） |
| `/nearby?kind=food\|parking\|toilet` | GET | 景点周边 POI（演示口径） |
| `/debates` | GET | 决策辩论双栏数据 |
| `/counterfactual` | GET | 反事实后悔药对照卡 |
| `/swarm` | GET | 虚拟游客 swarm 踩点反馈（3 条） |
| `/guides` | GET | 名导团评价（3 位） |
| `/dialogue` | POST | 与名导多轮对话，体：`{"guide_id":"dufu","message":"...","history":[]}` |
| `/debate/live` | GET | 直播辩论 **SSE** 流（`text/event-stream`，结束发 `done` 事件） |
| `/debate/vote` | POST | 辩论投票，体：`{"side":"规划方|游客方"}`，票数写回 travel_plan.md |

---

## 3. toB 治理接口（`AUTH_ENABLED=true` 时需 Bearer token）

| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/approvals/pending` | GET | 待审批队列（预算挂起 + 安全挂起） |
| `/api/feedbacks` | GET | 客户之声聚合：`total / praise_count / complaint_count / items[]`（job_id、created_at、kind、operator、content、customer、destination），演示级审计口径 |
| `/api/audit/export.csv` | GET | 全量审计导出（CSV 下载） |
| `/api/stats/overview` | GET | 任务统计（总数、状态分布） |
| `/api/safety/summary` | GET | 安全看板聚合（拦截率/攻击分布/按天曲线） |

---

## 3.1 预订与分享（P1 新增）

| 接口 | 说明 |
|---|---|
| `POST /api/services/booking/click` | 预订跳转埋点：`{"kind": "train\|flight\|attraction\|merchant\|entertainment", "job_id": null}`；Prometheus `booking_clicks_total` |
| `GET /s/{job_id}` | 行程书只读分享页（免登录、noindex、job_id 不可枚举）；数据走 `/api/plans/{id}/result` |

服务窗口契约扩展：`GET /api/services/{kind}` 响应新增顶层 `source`（`fliggy`=飞猪实时检索 / `local`=本地估算与官方渠道）；条目可带 `booking_url`（CPS 参数由后端 `FLIGGY_CPS_*` 环境变量统一追加）。降级顺序：飞猪 MCP →（熔断 60s/连续 3 败）→ 本地口径 → 诚实空态。

## 4. 旅行服务大厅 `/api/services`（演示口径）

| 接口 | 说明 |
|---|---|
| `GET /api/services/travel?destination=北京&origin=上海&days=3` | 出行模块聚合：火车票 + 机票 + 酒店多选项 |
| `GET /api/services/{kind}?destination=北京&origin=上海` | 单窗口；`kind = train\|flight\|attraction\|merchant\|entertainment` |

> 注意：以上为本地静态演示数据。**真实航班/酒店/景点/火车搜索请用下方 flyai-mcp 工具**（已启动）。

---

## 5. 语义检索 `/api/semantic`（pgvector）

| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/semantic/search?q=...` | GET | 语义检索（`DATABASE_URL` 未配置时为空结果 no-op） |
| `/api/semantic/add` | POST | 写入语义条目 |

---

## 6. 认证 `/api/auth`

| 接口 | 方法 | 请求体 / 说明 |
|---|---|---|
| `/api/auth/login` | POST | `{"realm":"toc\|tob","username":"...","password":"..."}` → `{token,name,role}` |
| `/api/auth/register` | POST | toC 注册 |
| `/api/auth/me` | GET | Header `Authorization: Bearer <token>` → 当前用户信息 |

---

## 7. 页面与系统

| 路径 | 说明 |
|---|---|
| `GET /` | 游客端 toC 页面 |
| `GET /b` | 企业端 toB 工作台 |
| `GET /metrics` | Prometheus 指标（`METRICS_ENABLED=true`） |
| `GET /api/integrations/status` | 外部集成状态（地图/检索等 provider 启用情况） |

---

## 8. flyai-mcp 工具（已启动，可直接调用）

当前会话中 **flyai-mcp 已启动**，提供真实旅行语义搜索能力，可直接调用以下工具；返回结果为真实供应商数据，与 `/api/services` 的本地演示口径互补（真实化改造时用这些工具的数据替换演示数据）。

| 工具 | 关键参数 | 用途 |
|---|---|---|
| `ai_search` | `query`（完整自然语言） | AI 语义搜索，酒店/机票/火车/景点统一入口，最推荐 |
| `keyword_search` | `query`（关键词） | 关键词泛搜，如 `北京 SIM 卡`、`杭州 3 天跟团` |
| `search_flight` | `origin`（必填）、`destination`、`depDate`（YYYY-MM-DD）、`journeyType`(1直达/2中转)、`seatClassName`(economy/business/first)、`maxPrice`、`sortType` | 搜索机票（国内/国际），返回航班号/舱位/时长/价格/预订链接 |
| `search_train` | 同 `search_flight`（`seatClassName`: second class/first class/business class/hard sleeper/soft sleeper） | 搜索火车票，返回车次/坐席/时长/价格 |
| `search_hotel` | `destName`（必填）、`keyWords`、`poiName`（景点旁）、`checkInDate/checkOutDate`、`hotelStars`(1-5)、`maxPrice`、`hotelBedTypes`(king/twin/multi)、`sort` | 搜索酒店/民宿，返回价格/评分/坐标/预订链接 |
| `search_marriott_hotel` | 同 `search_hotel` | 万豪集团旗下酒店 |
| `search_marriott_package` | `provinceOrCity`、`hotelName`、`keyword`、`sortType`(price_asc/price_desc) | 万豪套餐商品 |
| `search_poi` | `cityName`（必填）、`keyword`、`category`（nature/museum/temple/theme park/beach…）、`poiLevel`(1-5) | 搜索景点，返回门票信息/等级/地址 |

调用示例（语义搜索替代演示数据）：

```text
ai_search:  "北京 3 天 2 晚，2 大 1 小，预算 8000，亲子博物馆路线的酒店和门票"
search_train: origin=上海, destination=北京, depDate=2026-10-01
search_hotel: destName=北京, poiName=故宫, checkInDate=2026-10-01, checkOutDate=2026-10-03
```

> 说明：flyai-mcp 由 MCP 客户端注入，工具名为 `mcp__flyai-mcp__<工具名>`；本文档中的工具名即去掉前缀的短名。AI Agent 可把 `/api/plans`（创建/轮询/取结果）与 flyai-mcp（真实票务/酒店/景点搜索）组合使用：先用 flyai-mcp 拿真实报价，再提交行程生成。

---

## 9. 错误码约定

| HTTP | 场景 |
|---|---|
| 400 | 参数非法（如 services kind 不在枚举内） |
| 401 | 认证失败（用户名密码错误 / token 缺失） |
| 404 | 任务/结果不存在（result 可能在任务完成前瞬时 404，轮询即可） |
| 409 | `VERSION_CONFLICT`（乐观锁版本不符）／`INVALID_STATE`（当前状态不可审批） |
| 422 | 请求体字段校验失败（FastAPI 默认格式，`detail` 含字段级错误） |
