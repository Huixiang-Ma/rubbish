# 实现方案（完整落地 · 任务拆解与排期）

> 配套《完整落地方案.md》（路线图）与《技术方案_完整落地.md》（设计）。每个任务给出：改动文件（均为仓库真实路径）、接口契约、验收标准、预估人时（单人全栈口径）、依赖。
> 执行纪律：小步提交，每任务一 PR/一 commit 组；涉及后端的任务合并后立即 `docker compose -p wl_travel_mvp build backend && up -d backend` 验证；前端为挂载卷，刷新即生效。

---

## P0 清障加固（W1，共约 5 人日）

| # | 任务 | 改动文件 | 步骤与验收 | 人时 |
|---|---|---|---|---|
| 0.1 | QA 任务归档脚本 | 新增 `backend/scripts/archive_qa_jobs.py`；`backend/scripts/load_test.py` 压测 payload 统一加 `"constraints": ["qa:loadtest"]` 标记 | 脚本按 `constraints` 前缀 `qa:`/已知历史标记（`rbac-notoken401`）把任务目录 `mv` 至 `data/jobs_archive/`；跑一次后 `GET /api/plans` 仅剩真实任务；job_index 缓存 20s 内自动收敛 | 0.5d |
| 0.2 | 安全基线 | `.env`、`.env.example`、`backend/app/services/auth_service.py`（如需） | 轮换 toB 三角色口令与演示账号；`AUTH_ENABLED=true`；核对 CORS/debug 配置；`.env` 确认未被 git 跟踪（0.4 之后 `git status` 复查） | 0.5d |
| 0.3 | pg_mirror 状态口径 | `backend/app/main.py`（startup 延迟 1s 回查 `pg_mirror.enabled` 再记日志或直接删除误导字段） | 启动日志不再出现与事实矛盾的 `pg_mirror:false`；停 postgres 跑一单，主流程完成、日志出现 mirror 写失败 warning | 0.5d |
| 0.4 | git 入库 + CI | `git init`、远端、激活 `.github/workflows/ci.yml`；`deploy.sh`（见技术方案 §9） | push 后 CI 绿；镜像入 ACR；deploy.sh 在测试主机跑通含失败回滚 | 1d |
| 0.5 | 容量压测 | `backend/scripts/load_test.py` 三档执行；`docs/容量基线.md`（新增一页纸） | 记录 10/50/200 并发下的队列水位、P95 排队时长、LLM 限流表现；给出"单机可承载 X 并发提交"结论 | 1d |
| 0.6 | 备份 | 宿主 cron：`tar data/jobs` + `docker exec pg_dump`；恢复演练记录 | 从备份完整恢复一次并抽查 3 个任务可打开 | 0.5d |
| 0.7 | ICP 备案材料提交 | ——（行政项） | 当天提交，拿到备案号前用测试域名内测 | 0.5d |

**依赖**：0.4 完成前其余任务先本地提交；0.5 依赖 0.1（脏数据会污染统计）。

---

## P1 真实数据与体验闭环（W2-W4，共约 15-18 人日）

| # | 任务 | 改动文件 | 接口/契约 | 验收 | 人时 | 依赖 |
|---|---|---|---|---|---|---|
| 1.0 | 飞猪接口探测 spike | 新增 `backend/scripts/probe_fliggy.py` | 真实 key 调五类查询，输出字段样例到 `docs/飞猪MCP探测.md` | 明确每类窗口可用字段；决定 train 是否走 CPS | 0.5d | FLIGGY_AI_API_KEY |
| 1.1 | 票务三级降级接入 | `backend/app/api/services.py`、`backend/app/services/fliggy_client.py`（熔断器）、`backend/app/services/integrations.py` | `GET /api/services/{kind}` +`source` +item.`booking_url`；熔断指标 `fliggy_requests_total` | 北京/杭州/成都三地五窗口均有真实数据或诚实降级；断网/无 key 自动回落本地口径；5 分钟缓存命中 | 3d | 1.0 |
| 1.2 | 前端预订跳转 + CPS | `frontend/index.html`（`renderServiceWindow`、`data-svc-book` 处理器） | 有 `booking_url` 渲染"前往预订"（新窗 + `rel=noopener`）；埋点 `booking_clicks_total` | 点击跳转供应商落地页且带推广参数；无链接条目维持官方渠道口径 | 1d | 1.1 |
| 1.3 | 高德/和风全量真实化 | `.env`（AMAP_KEY/QWEATHER_API_KEY）、必要时 `backend/app/services/amap_client.py`/`qweather_client.py` 的配额日志 | 行程书数据来源标注实时 | 生成一单北京行程：天气为和风实时、通勤为高德路径规划；key 缺失时页面显示"本地估算口径"降级标注 | 0.5d | key 申请 |
| 1.4 | 生成中直播辩论 | `frontend/index.html` 第三段（pollStatus/EventSource/投票条） | 消费 `GET /{id}/debate/live`（SSE）+ `POST /{id}/debate/vote` | 生成到 Debate 节点时弹层实时滚动正反方发言；投票后倾向刷新且裁决记录含投票影响；SSE 断开不影响主日志 | 2.5d | — |
| 1.5 | 体验中心四模块接线 | `frontend/index.html`（EX 对象改 async 装载 + `lastJobId` 上下文） | `GET .../debates`、`/guides`+`/dialogue`、`/swarm`、`/counterfactual`、`/map`+`/nearby` | 六模块全部展示当前 job 真实数据；无 job 时引导生成/选历史，不出现任何静态示例；Leaflet 地图渲染动线与周边 POI 高亮 | 3.5d | lastJobId 机制（0.5d，含在本项） |
| 1.6 | 分享页 | 新增 `frontend/share.html`；`backend/app/main.py` 路由 `/s/{job_id}`；`frontend/index.html` 书封分享按钮 | `GET /s/{job_id}` 免登录只读；IP 频控 60/min | 微信内置浏览器打开可读完整行程书；`noindex`；生成后一键复制链接 | 1.5d | — |
| 1.7 | 移动端适配 | `frontend/index.html`（CSS 断点 ≤820px / ≤480px） | —— | 375/390 宽真机全流程（输入→生成→回看→分享）可用；触控目标 ≥44px | 2d | — |
| 1.8 | 辩论/账本章节结构化渲染 | `frontend/index.html`（chDebate 双栏卡、chLedger 分项表） | `GET /{id}/debates` 结构化数据 | 行程书④章为正反双栏卡片、③章为分项表格（对接 §1 技术方案） | 1.5d | — |
| 1.9 | P1 集成验收 | —— | —— | 按落地方案 §4.3 前三条做一次全流程走查并记录问题清单 | 0.5d | 1.1-1.8 |

**里程碑**：W4 末内测版上线（测试域名）。

---

## P2 商业化前置（W5-W8，共约 15-20 人日）

| # | 任务 | 改动文件 | 接口/契约 | 验收 | 人时 |
|---|---|---|---|---|---|
| 2.1 | 手机号+微信登录 | `backend/app/api/auth.py`、`backend/app/services/auth_service.py`、`frontend/index.html` 登录门禁；新增 `SMS_*`/`WECHAT_*` 配置 | token 机制不变（HMAC），`realm=toc` 凭证扩展 | 手机验证码登录、微信扫码登录各跑通一次；旧口令登录保留 | 3d |
| 2.2 | 任务归属 + 历史云端化 | `backend/app/models/schemas.py`（PlanRequest 内部注入 user_id）、`backend/app/api/plans.py`（`GET /mine`、`POST /claim`）、`backend/app/services/checkpoint_store.py`（state 写 user_id）、PG `ALTER TABLE plan_jobs ADD COLUMN user_id`、`frontend/index.html` 历史列表切 `GET /api/plans/mine` | `GET /api/plans/mine`（Bearer）→ 本人任务倒序；`POST /api/plans/claim {job_ids[]}` 认领近 72h 游客单 | 换设备登录后历史完整可见；认领后 toB 列表该单 customer 显示来源用户昵称 | 3d |
| 2.3 | toB 多租户做实 | `frontend/admin.html`（租户筛选/席位页）、`backend/app/api/admin.py` | `tenant` 字段贯穿：创建/列表/导出 | 两租户数据互不可见；白标导出带租户品牌 | 3d |
| 2.4 | 成本治理 | `backend/app/services/llm_client.py`（usage 解析+Counter `llm_tokens_total{node}`）、`backend/app/api/plans.py`（`POST /api/plans` 配额中间件）、Grafana panel JSON（`observability/`） | `GET /api/stats/overview` +近 7 日成本；超免费额度返回 429 且前端引导文案正确 | 同 IP 第 4 单被限；看板可见每单成本分布 | 2.5d |
| 2.5 | 内容合规固化 | `backend/app/services/safety_service.py` 复查、分享页/行程书声明（1.6 已带） | UGC（dialogue/feedback）过敏感词；声明页脚存在 | 抽样注入用例全部拦截/清洗 | 1d |
| 2.6 | P2 集成验收 + 公测 | —— | —— | 落地方案 §4.3 第 1、4、5 条达成 | 1d |

---

## P3 上线与运营（W3 起并行，共约 6-8 人日 + 长期）

| # | 任务 | 改动文件 | 验收 | 人时 |
|---|---|---|---|---|
| 3.1 | 云主机部署 | `deploy.sh`、Caddyfile（新增）、compose 生产 override（`docker-compose.prod.yml`：端口收敛、restart 策略、资源限制） | 公网 HTTPS 可访问；`/b` 有白名单；证书自动续期 | 1.5d |
| 3.2 | 告警 | 新增 `observability/alert-rules.yml` + Alertmanager 飞书接收器 | 人为打挂 backend，5 分钟内飞书收到告警 | 1d |
| 3.3 | 发布流程固化 | CI deploy job + 备份前置 | 一次真实发布演练：发布→探活失败→自动回滚 | 1d |
| 3.4 | 种子运营准备 | 分享页统计、toB 试用账号 3 套、演示数据脚本（真实生成 3 个目的地精品单） | 演示单可 5 分钟内重建 | 1d |
| 3.5 | 观测复盘机制 | 每周看板截图 + 问题清单入仓库 `docs/运营周记/` | 连续 4 周执行 | 持续 |

---

## 执行顺序与并行关系

```
W1  P0.1→P0.5 串行（P0.7 行政并行）
W2  1.0 spike ─► 1.1+1.2（票务线，1 人）
    1.4 直播辩论（若补前端则并行，否则 W3 顺延）
    3.1 云部署 + 域名解析（与业务线无冲突，可由后端侧完成）
W3  1.5 体验中心 ─► 1.8 章节结构化
    1.6 分享页（独立可插空）
W4  1.7 移动端 ─► 1.9 集成验收 ─► 内测上线
W5-6 2.1 ─► 2.2（账号线串行）；2.4 成本治理（并行）
W7   2.3 toB 多租户；3.2/3.3 告警与发布演练
W8   2.5 ─► 2.6 公测验收
```

## 变更管理约定

1. 所有新接口先改 `backend/app/models/schemas.py` + 在《接口文档.md》登记，再写实现；
2. 前端仍坚持"真实数据才展示"：任何新模块若无数据可显，一律显示诚实空态，禁止静态示例；
3. 每个任务验收项写进 PR 描述，合并即视为自验通过；跨任务集成验收用 1.9/2.6 的清单；
4. 数据结构变更（如 plan_jobs 加列）必须同时给出回滚语句。
