# 文旅多 Agent 系统 · 背景对齐补全方案（实现工单）

> 版本：v1.0 ｜ 编制日期：2026-09-04
> 编制依据：源码级核查结论（`README.md`、`backend/app/`、`data/jobs/` 466 个真实任务、`压测指标报告.md`、`工单要点提取与落点映射.md`）
> 面向读者：**实现本方案的另一 Agent**。本工单中的"现状"均为已核实事实；改动必须落在 `D:\Desktop\WL项目` 现有代码结构内，不得另起炉灶。

---

## 0. 背景与目标

背景任务书要求系统具备：Manus 式文件驱动规划（`travel_plan.md` + 断点续传）、8 个 Agent 协同（Intake/Planner/Web Research/Sentiment/Itinerary/Budget/Human Review/Report）、网页调研与来源追溯、舆情过滤、预算与人机协同审核、对抓取内容的安全过滤。

源码核查确认：**骨架已完整落地**（文件驱动、断点续传、多 Agent 管线、预算挂起审批、HITL、Prompt 注入防御、真实外部 API 接入），剩余 **6 类缺口** 需按本工单补齐：

| 编号 | 缺口 | 性质 | 优先级 |
|---|---|---|---|
| P0-1 | Sentiment 舆情 Agent 完全缺失（0 代码） | 功能硬缺口 | **P0** |
| P0-2 | 安全过滤只覆盖用户输入，未覆盖外部抓取内容 | 背景明确要求 | **P0** |
| P1-1 | 方案缺少来源 URL 溯源（只有数据源级标记） | 背景痛点"缺乏来源追溯" | P1 |
| P1-2 | 无网页抓取（仅 API 化采集，票价/预约非实时） | 口径性缺口 | P1 |
| P1-3 | RBAC 角色体系缺失（仅 admin/traveler） | 工单要求 | P1 |
| P2-1 | 无独立 Intake Agent（偏好解析由 API+前端承担） | Agent 构成对齐 | P2 |
| P2-2 | 无独立 Budget Agent（预算职责在 Validator 内） | Agent 构成对齐 | P2 |
| P2-3 | 《Loop Engineering 报告》《AI 代码审查报告》未产出 | 产出物缺口 | P2 |

**全局硬约束（实现时必须遵守）**：
1. 所有改动保持现有架构模式：Agent 继承 `app/agents/base.py` 的 `AgentBase`，输出 `{"agent": name, "status": "ok|...", "payload": {...}}`；状态写入走 `CheckpointStore`；最终产物由 `MarkdownReporter` 单点渲染，**禁止 Agent 直接写 travel_plan.md**。
2. 兼容旧数据：`data/jobs/` 下 466 个旧任务无新增节点输出，读取侧一律用 `outputs.get("Xxx")` 兜底，不得因缺字段抛错。
3. 每次改动后必须通过回归：`python -m compileall app scripts` + `PYTHONIOENCODING=utf-8 python scripts/smoke_test.py` + `pytest backend/tests -x`。
4. 诚实声明：新增的演示口径（舆情库、抓取白名单）必须在 README 诚实声明区补充说明。

---

## 1. P0-1：Sentiment 舆情 Agent（最高优先）

### 1.1 目标
新增独立舆情 Agent，对行程候选资源做负面舆情/风险过滤，输出避坑提示与风险告警，写入 `travel_plan.md`。对应背景"舆情过滤（Sentiment）"与需求文档 §11.2 的舆情分类设计（`BENIGN / NEGATIVE_REVIEW / SERVICE_RISK / SAFETY_RISK / SCAM_RISK / UNKNOWN`）。

### 1.2 现状
- 无任何 sentiment/舆情相关代码。
- 管线（`app/services/pipelines.py`）：
  - toC：`Researcher → Planner → Itinerary → Validator → Debate → Mood → Reporter`
  - toB：`Researcher → Planner → Itinerary → Validator → Consultant → Compliance → Reporter`
- `app/agents/` 现有：base/planner/researcher/itinerary/validator/debate/mood/consultant/compliance。

### 1.3 改动文件清单
| 文件 | 动作 |
|---|---|
| `backend/app/agents/sentiment.py` | **新增**：SentimentAgent |
| `backend/app/data/sentiment_reviews.json` | **新增**：北京景点舆情数据（演示口径） |
| `backend/app/services/pipelines.py` | 修改：两管线在 Validator 后插入 "Sentiment" |
| `backend/app/services/plan_processor.py` | 修改：实例化 SentimentAgent 并加入两管线 |
| `backend/app/services/markdown_reporter.py` | 修改：新增"舆情与避坑提示"章节 |
| `backend/app/agents/__init__.py` | 修改：导出（如现有模式需要） |
| `backend/tests/test_sentiment_agent.py` | **新增**：单测 |
| `README.md` | 修改：Agent 清单与诚实声明 |

### 1.4 实现步骤

**Step 1：数据文件 `backend/app/data/sentiment_reviews.json`**
- Schema：
```json
{
  "故宫博物院": {
    "risk_level": "BENIGN",
    "highlights": ["...", "..."],
    "warnings": [],
    "notes": "需提前 7 天预约，周一闭馆"
  },
  "八达岭长城": {
    "risk_level": "NEGATIVE_REVIEW",
    "highlights": ["..."],
    "warnings": ["节假日人流量极大，缆车排队 1 小时以上", "部分黑车宰客，认准官方摆渡车"],
    "source": "本地舆情库（演示口径）"
  }
}
```
- 覆盖要求：键名必须覆盖 `backend/app/data/scenic_spots_beijing.json` 中全部景点 name（实现时先读静态库提取景点名再编写，不得遗漏）；`risk_level` 至少出现 2 条 `SAFETY_RISK`/`SCAM_RISK` 与若干 `NEGATIVE_REVIEW`，用于演示过滤效果；每条 `source` 标注"本地舆情库（演示口径）"。

**Step 2：`backend/app/agents/sentiment.py`**
```python
class SentimentAgent(AgentBase):
    name = "Sentiment"

    def run(self, context):
        # 1) 收集待分析资源：Researcher 的 spots + Itinerary 每日 items 中的 spot 名
        # 2) 加载 sentiment_reviews.json（复用 ScenicSpotService.load_spots 的读取模式）
        # 3) LLM real 模式增强（可选）：llm_client.try_generate_json 对未收录景点做分类；
        #    失败/未配置时全部回退本地库（命中 UNKNOWN 的按保守降权处理）
        # 4) 输出 payload：
        #    {
        #      "reviews": [{"name","risk_level","highlights","warnings","source"}],
        #      "risk_alerts": [{"name","risk_level","message","suggestion"}],
        #      "summary": "共分析 N 个资源，其中 X 条存在风险提示"
        #    }
```
- `risk_alerts` 只收录 `SAFETY_RISK` / `SCAM_RISK` 与带 warnings 的 `NEGATIVE_REVIEW`；`BENIGN` 不产生告警。
- 建议降级规则（诚实口径）：无本地数据且 LLM 不可用 → `UNKNOWN`，输出"暂未获取到该景点舆情信息，建议出行前自行确认"。

**Step 3：管线接入**
- `pipelines.py`：
  - `TOC_PIPELINE = ["Researcher", "Planner", "Itinerary", "Validator", "Sentiment", "Debate", "Mood", "Reporter"]`
  - `TOB_PIPELINE = ["Researcher", "Planner", "Itinerary", "Validator", "Sentiment", "Consultant", "Compliance", "Reporter"]`
- `plan_processor.py`：`self.pipelines["toc"]` 与 `["tob"]` 在 `ValidatorAgent()` 后插入 `SentimentAgent()`。
- **不要**把 Sentiment 加入 `TAIL_PARALLEL_NAMES`（它消费 Validator 之前的输出，保持串行，逻辑简单且便于演示进度）。
- 挂起兼容：Validator 超预算挂起时 `resume_from` 为 Validator 下一节点（即 Sentiment），审批后自动从 Sentiment 续跑，无需额外处理（`CheckpointStore._next_node` 基于 `pipeline_nodes` 自动生效）。

**Step 4：`markdown_reporter.py` 新增章节**
- 位置：toC 在"翻车预演"章节前插入 `## N. 舆情与避坑提示`；toB 同样插入（放在"合规审计摘要"之前或之后均可，保持编号连续——注意现有 `summary_no/index_no` 的编号变量需要同步 +1）。
- 渲染内容：
  - 每条 `risk_alerts`：`- ⚠️ 景点名 [risk_level]：message → 建议：suggestion`
  - 每个 `reviews`（可选）：`- 景点名：highlights 摘要；warnings 摘要（来源：xxx）`
- 用 `outputs.get("Sentiment")` 兜底，旧任务无此输出时渲染"暂无舆情数据"。

**Step 5：测试 `backend/tests/test_sentiment_agent.py`**
- `test_benign_no_alert`：BENIGN 景点不产生 risk_alerts。
- `test_scam_alert`：SCAM_RISK 景点进入 risk_alerts 且带建议。
- `test_llm_failure_fallback`：LLM 不可用（mock 模式）时回退本地库、不抛错。
- `test_pipeline_contains`：两管线字符串均含 "Sentiment"。
- 跑 `smoke_test.py` 回归（六链路不得被破坏）。

### 1.5 验收标准
1. 提交"北京 3 天 5000 元"，`travel_plan.md` 出现"舆情与避坑提示"章节且包含至少 1 条 risk_alert（用含 warnings 的景点验证）。
2. toB 提交（带 `customer` 字段）同样出现该章节。
3. 旧任务（无 Sentiment 输出）读取正常、不报错。

---

## 2. P0-2：外部抓取内容安全过滤

### 2.1 目标
背景明确要求"对抓取的网页内容执行安全过滤，防御 Prompt 注入"。当前 `SafetyService.scan_untrusted_content` 只被用于扫描 `user_input`，**外部 API 返回内容（Researcher 的 spots、Itinerary 的 POI/餐饮/天气文本）未过安全过滤**。本项补齐：对所有 Agent 输出做统一递归扫描，高风险剔除/拦截、中风险清洗降权，全部写审计。

### 2.2 改动文件清单
| 文件 | 动作 |
|---|---|
| `backend/app/services/safety_service.py` | 修改：新增递归扫描器 `scan_payload` + 审计事件生成 |
| `backend/app/services/plan_processor.py` | 修改：每个 Agent 节点完成后调用扫描器 |
| `backend/app/api/safety.py` | 修改：安全看板聚合增加 `web_content_block` 计数 |
| `backend/tests/test_safety_content_filter.py` | **新增**：单测 |
| `README.md` | 修改：诚实声明补充 |

### 2.3 实现步骤

**Step 1：`safety_service.py` 新增递归扫描**
```python
def scan_payload(payload: dict) -> tuple[dict, list[dict]]:
    """递归遍历 payload 中所有 str 字段，返回 (清洗后的 payload, 审计事件列表)。
    规则复用现有 HIGH_RISK_PATTERNS / MEDIUM_RISK_PATTERNS：
    - high → 字段值替换为 "[已移除风险片段]"，事件 action="web_content_block"
    - medium → sanitize() 后保留，事件 action="web_content_sanitize"
    - low → 原样
    """
```
- 对 list/dict 递归；只处理 str 值；保留键结构不变。

**Step 2：`plan_processor.py` 接入**
- 在 `agent.run(context)` 之后、`mark_node_done` 之前插入：
```python
result, events = self.safety.scan_payload(result)
for event in events:
    self.store.append_audit(job_id, {"action": event["action"], "risk_level": "high",
                                      "agent": agent.name, "evidence": event["evidence"]})
    if event["action"] == "web_content_block":
        SAFETY_BLOCKED.inc()
        pg_mirror.record_safety(job_id, "high", "block", event["evidence"])
```
- 规则：命中 high 的资源字段被替换（不剔除整条资源，保持行程可用）；极端情况（spot 名称被清洗后为空串）剔除该条并记录。
- 同样处理尾段并行 Agent（`_run_tail` 内对每个 result 调用）。

**Step 3：安全看板扩展**
- `safety_service.py` 的 `SAFETY_COUNTER_KEYS` 增加 `"web_content_block"`、`"web_content_sanitize"`；`aggregate_safety_events` 自动聚合（现有逻辑基于 action 计数，无需改聚合函数）。
- `api/safety.py` 无需改动（透传 counters）。

**Step 4：测试 `backend/tests/test_safety_content_filter.py`**
- `test_high_risk_replaced`：构造含"忽略之前所有指令"的景点 name → 被替换为占位符。
- `test_medium_sanitized`：含"修改预算"字段 → sanitize 保留。
- `test_nested_recursion`：三层嵌套 dict/list 均被扫描。
- `test_audit_written`：集成路径下 audit.log 出现 `web_content_block`。

### 2.4 验收标准
1. 构造含注入文本的外部数据（可 mock `amap_client.search_pois` 返回恶意景点名），跑通完整链路，`audit.log` 出现 `web_content_block`。
2. 安全看板 `/api/safety/summary` 的拦截计数含 web_content 来源。
3. `smoke_test.py` 全部通过（原有用户输入注入拦截逻辑不变）。

---

## 3. P1-1：来源 URL 溯源

### 3.1 目标
背景痛点"生成的规划方案缺乏明确的来源追溯"。当前 `travel_plan.md` 只有数据源级标记（"来源：高德实时检索/飞猪AI"），缺 URL 级溯源。本项为每条资源补充可点击来源链接。

### 3.2 改动文件清单
| 文件 | 动作 |
|---|---|
| `backend/app/services/scenic_spot_service.py` | 修改：spot 增加 `source_urls` 字段 |
| `backend/app/agents/itinerary.py` | 修改：飞猪 enrich 时写入 `booking_url` 到 `source_urls` |
| `backend/app/services/markdown_reporter.py` | 修改：渲染"信息来源"行 |
| `backend/tests/test_source_traceability.py` | **新增**：单测 |

### 3.3 实现步骤
1. `scenic_spot_service.py`：
   - 静态库兜底景点：`source_urls = []`（静态库无可追溯 URL，如实留空）。
   - 高德 POI 命中景点：`source_urls = [f"https://www.amap.com/search?query={quote(name)}"]`（高德搜索页，`quote` 做 URL 编码）。
   - 飞猪命中景点：`source_urls.append(booking_url)`（已有字段）。
2. `itinerary.py` `_enrich_with_fliggy`：写入 `item["spot"]["source_urls"]` 合并飞猪 `booking_url`。
3. `markdown_reporter.py` 景点条目新增行：
   - `- 信息来源：` + 逐个 `[来源名](url)`；无 URL 时输出"静态演示数据，无外部来源链接"。
4. 测试：断言高德路径生成 amap.com 链接、飞猪路径追加 booking_url、静态兜底为空列表且渲染不报错。

### 3.4 验收标准
行程书每个景点条目出现"信息来源"行；有飞猪预订链接的景点出现可点击链接。

---

## 4. P1-2：白名单网页抓取（演示口径，可选实现）

### 4.1 目标
背景要求"网页抓取，实时捕获票价变动、预约限制"。受合规与稳定性约束，本项实现**白名单 + 演示口径**的轻量网页调研，不实现通用爬虫：仅抓取少量公开页，提取开放时间/预约规则文本，带 `fetched_at` 与 `source_url` 标记。

### 4.2 约束（必须遵守）
- 白名单域名：仅允许 `www.dpm.org.cn`（故宫官网）、`www.badaling.gov.cn` 等 2-3 个公开官网；在代码中硬编码白名单，任何非白名单 URL 直接拒绝。
- 限速：单任务最多抓取 N 页（默认 3），每页间隔 ≥1s；超时 5s；失败静默降级（不影响主链路）。
- 只提取文本（开放时间/预约规则），**不采集任何个人信息、不提交表单、不绕过任何访问控制**；robots 声明禁止的页面跳过。
- 该能力默认关闭（环境变量 `WEB_RESEARCH_ENABLED=true` 才启用），默认零配置下主链路行为与现在完全一致。

### 4.3 改动文件清单
| 文件 | 动作 |
|---|---|
| `backend/app/services/web_research.py` | **新增**：白名单抓取 + 文本提取（标准库 `urllib.request` + `html.parser`，不新增第三方依赖，保持 `requirements.txt` 不动） |
| `backend/app/agents/researcher.py` | 修改：抓取命中时对 spot 覆盖 `open_time`/`booking_rule` 并标记 `fetched_at`/`source_url` |
| `backend/tests/test_web_research.py` | **新增**：单测（mock HTTP，不发真实请求） |
| `.env.example` | 修改：加 `WEB_RESEARCH_ENABLED` |
| `README.md` | 修改：诚实声明 |

### 4.4 实现步骤
1. `web_research.py` 提供：
```python
ALLOWLIST = {"www.dpm.org.cn", "www.badaling.gov.cn"}
def fetch_page_text(url: str) -> str | None   # 仅白名单；超时/失败返回 None
def extract_rules(html_text: str, spot_name: str) -> dict | None
# 返回 {"open_time": "...", "booking_rule": "...", "fetched_at": "ISO", "source_url": url}
```
2. `researcher.py`：`WEB_RESEARCH_ENABLED=true` 且该景点在白名单时调用；成功则覆盖 open_time、新增 booking_rule，标记实时来源；失败静默回退原逻辑。
3. 测试全部 monkeypatch `urllib.request.urlopen`，断言：非白名单 URL 拒绝、解析提取正确、失败返回 None。

### 4.5 验收标准
配置启用后，故宫景点 open_time 带 `fetched_at` 与官网 `source_url`；未配置时行为与现在完全一致（回归通过）。

---

## 5. P1-3：RBAC 角色审批控制

### 5.1 目标
工单要求"JWT 鉴权 + 角色（顾问/主管）审批控制"。当前 `auth_service.py` 仅 admin/traveler 两角色。本项新增"顾问 consultant / 主管 supervisor"角色，并让审批类接口校验角色。

### 5.2 改动文件清单
| 文件 | 动作 |
|---|---|
| `backend/app/services/auth_service.py` | 修改：账号表扩展顾问/主管演示账号；token 携带角色 |
| `backend/app/api/plans.py` | 修改：approval 端点加 `require_role("supervisor")` |
| `backend/app/api/auth.py`（如存在） | 修改：登录返回 role |
| `backend/tests/test_rbac.py` | **新增**：单测 |
| `README.md` | 修改：演示账号表 |

### 5.3 实现步骤
1. `auth_service.py`：
   - 账号表增加：`supervisor / 主管演示密码`、`consultant / 顾问演示密码`（密码写入 README 演示账号区，与现有 admin/wl2026 风格一致）。
   - `issue_token` 已带 `"r": role`；`verify_token(token, role=...)` 已支持角色校验（现有代码已具备，确认复用）。
2. 新增 FastAPI 依赖：
```python
def require_role(*roles):
    def dep(authorization: str = Header(...)) -> None:
        payload = verify_token(authorization.removeprefix("Bearer ").strip())
        if not payload or payload.get("r") not in roles:
            raise HTTPException(403, "FORBIDDEN_ROLE")
    return dep
```
3. `plans.py` 应用：
   - `POST /api/plans/{job_id}/approval` → `require_role("supervisor", "admin")`
   - `POST /api/plans/{job_id}/replan` → `require_role("consultant", "supervisor", "admin")`
   - `POST /api/plans/{job_id}/feedback` → `require_role("consultant", "supervisor", "admin")`
4. 测试：traveler token 调 approval → 403；supervisor token → 放行；无 token → 401/403。

### 5.4 验收标准
用游客角色 token 审批返回 403；主管 token 审批成功；toB 登录页可切换三种角色。

---

## 6. P2-1：独立 Intake Agent（可选重构）

### 6.1 目标
对齐背景 8 Agent 清单的 Intake（偏好解析）。新增轻量 IntakeAgent 做输入规范化，让"偏好解析"有独立 Agent 落点。

### 6.2 改动
| 文件 | 动作 |
|---|---|
| `backend/app/agents/intake.py` | **新增**：IntakeAgent |
| `backend/app/services/pipelines.py` | 修改：两管线开头插入 "Intake" |
| `backend/app/services/plan_processor.py` | 修改：管线实例化 IntakeAgent；Intake 输出规范化后的 user_input 写入 context |
| `backend/tests/test_intake_agent.py` | **新增** |

### 6.3 实现要点
- `IntakeAgent.run` 输出：
```json
{
  "agent": "Intake",
  "payload": {
    "normalized_input": {
      "destination": "北京", "days": 3, "budget": 5000, "travelers": 2,
      "preferences": ["博物馆"], "constraints": ["减少打车"],
      "parsed_flags": {"transit_first": true, "avoid_early_rise": false}
    },
    "missing": [], "warnings": []
  }
}
```
- 归一化规则：目的地去"市"后缀；travelers 缺省 2；preferences/constraints 去重；"减少打车/优先地铁/经济实惠"→ `transit_first=true`（与 `itinerary.py` 现有判断口径保持一致）。
- `plan_processor.py`：Intake 完成后 `context["user_input"] = intake_payload["normalized_input"]`（后续节点消费规范化输入；`state` 里保留原始输入用于展示）。
- 风险提示：Itinerary/Validator 读取的 `user_input["budget"]` 等键名保持不变，因此下游无需改动；仅新增节点在最前。

---

## 7. P2-2：独立 Budget Agent（重构，风险中等）

### 7.1 目标
把预算职责从 ValidatorAgent 拆出为独立 BudgetAgent，对应背景 8 Agent 清单的 Budget。

### 7.2 改动
| 文件 | 动作 |
|---|---|
| `backend/app/agents/budget.py` | **新增**：BudgetAgent（迁移 `validator.py` 的 `_budget_breakdown/_economy_total/_travel_advice/_economy_tips/_what_if` 中预算相关部分） |
| `backend/app/agents/validator.py` | 修改：删除预算逻辑，保留时空/强度校验 |
| `backend/app/services/pipelines.py` | 修改：两管线改为 `... Itinerary → Budget → Validator → ...` |
| `backend/app/services/plan_processor.py` | 修改：实例化 BudgetAgent；`WAITING_BUDGET_APPROVAL` 判定迁移到 Budget 节点之后 |
| `backend/app/services/markdown_reporter.py` | 修改：`validation["estimated_budget"]` 改读 `outputs["Budget"]["payload"]`（用 `.get` 双兜底旧任务） |
| `backend/scripts/smoke_test.py` | 修改：预算挂起链路断言适配新节点 |
| `backend/tests/test_budget_agent.py` | **新增** |

### 7.3 实现要点
- BudgetAgent 输出保持与原 Validator 预算字段**完全同名**（`estimated_budget/budget_breakdown/approval_required/economy_total/economy_tips`），最小化下游改动。
- 挂起判定：Budget 节点完成后若 `approval_required` 则挂起（逻辑与现 Validator 处一致，仅位置迁移）；审批恢复后从 `_next_node`（Validator）续跑。
- Validator 保留：强度、跨区跨度、通勤超时、翻车预演中的非预算部分；`_what_if` 中预算条目迁移到 Budget。
- 回归重点：`smoke_test.py` 第 3 条（预算挂起恢复）与第 6 条（增量重规划）必须通过。

---

## 8. P2-3：两份产出物报告

### 8.1 《Loop Engineering 提示词与自主验证报告.md》（新增，放项目根目录）
内容大纲（基于真实代码撰写，不得虚构）：
1. **各 Agent 提示词清单**：从 `agents/*.py` 提取 LLM real 模式 prompt（Planner `_llm_plan`、Itinerary `_enrich_with_llm`、Debate、Mood、Consultant 等），逐个说明输入结构、输出 JSON schema、temperature、max_tokens。
2. **自主验证机制**：`llm_client.try_generate_json` 的 JSON 解析与失败重试（max_retries + 退避）、`_chat_via_httpx` → `_chat_via_socket` 降级通道、mock 兜底。
3. **验证闭环**：`scripts/smoke_test.py` 六条链路、`scripts/llm_offline_test.py`、`pytest backend/tests` 覆盖清单。
4. **Loop 改进记录**：引用真实修复案例（如 `compute_schedule` 溢出修复为"次日 HH:MM"、短途步行不调公交规划、高德 TLS 抖动退避重试），说明"发现→修复→验证"闭环。

### 8.2 《AI 代码审查报告.md》（新增，放项目根目录）
- 要求：真实审查 `backend/app` 代码，找出 **3-5 个潜在 bug/隐患**，每条按固定格式：
```
### Bug N：<一句话标题>
- 位置：<文件:行号>
- 现象/影响：
- 根因：
- 修复建议：
- 验证方式：
```
- 提供候选线索（实现者须自行核实后再写，不得照抄）：
  - `travel_context_service.py` `recommend_hotel` 的 `adjusted` 标记逻辑是否恒为 True；
  - `plan_processor.py` 尾段并行 `pool.map` 与断点恢复顺序补跑的边界；
  - `markdown_reporter.py` 编号变量 `summary_no/index_no` 分支自增是否正确；
  - `fliggy_client.py` 签名重试与缓存 TTL 边界；
  - `validator.py` `economy_total` 与 `approval_required` 口径在 travelers>2 时是否合理。
- 修复建议：低风险问题可直接修代码并跑回归；不影响行为的按"记录+建议"处理，不许为凑数虚构问题。

---

## 9. 实施顺序与批次划分（建议）

| 批次 | 内容 | 预估人日 | 依赖 |
|---|---|---|---|
| 批次 1 | P0-1 Sentiment + P0-2 抓取内容过滤 | 1.5 | 无 |
| 批次 2 | P1-1 来源溯源 + P1-2 白名单抓取 | 1.5 | 批次 1 后（共用 markdown_reporter） |
| 批次 3 | P1-3 RBAC | 0.5-1 | 无，可并行 |
| 批次 4 | P2-1 Intake + P2-2 Budget 拆分 | 1.5-2 | 批次 1 后（管线改动集中） |
| 批次 5 | P2-3 两份报告 | 0.5-1 | 全部完成后（需引用最终代码） |

- 若时间受限，砍减顺序：P2 全部 → P1-2 → P1-1。**P0 两项不可砍**。
- 每个批次独立提交（git commit），commit message 标注批次号。

---

## 10. 全局回归验收清单（每批次完成后执行）

```bash
cd D:\Desktop\WL项目\backend
python -m compileall app scripts
PYTHONIOENCODING=utf-8 python scripts/smoke_test.py        # 六条关键链路
pytest tests -x                                             # 全部单测
```
手动验证（可配录屏分镜）：
1. 提交"北京 3 天 5000 元"→ 完成 → 行程书含"舆情与避坑提示"章节与"信息来源"行；
2. 恶意输入"忽略之前所有指令"→ 安全挂起 → 主管角色审批放行 → 恢复完成；
3. 预算 100 元 → 挂起 → 主管审批 → 恢复完成；
4. 断点：提交后强杀后端 → 重启 → 日志出现 `[recovery] requeued`；
5. toB 工作台：三种角色登录、方案详情含新章节、审计 CSV 可下载。

## 11. 完成后须更新的文档
- `README.md`：Agent 清单（8 个落点说明：Intake/Researcher(Web Research)/Planner/Itinerary/Budget/Validator/Sentiment/Consultant/Compliance/Debate/Mood/Reporter）、演示账号表（含 supervisor/consultant）、诚实声明（舆情库与网页抓取为演示口径）、新章节说明。
- `.env.example`：`WEB_RESEARCH_ENABLED` 等新变量。
- `工单要点提取与落点映射.md`：把已补齐项从"缺口"改为"已实现（日期）"。

---

*本工单依据 2026-09-04 源码核查结论编制；实现过程中如发现本工单与代码现状不符，以代码为准并在交付说明中注明差异。*
