# 背景对齐补全 · 交付说明

> 日期：2026-09-04 ｜ 依据：《补全方案_背景对齐_实现工单.md》v1.0（全批次 1-5）

## 一、交付物清单

### 批次 1（P0，不可砍）
| 交付物 | 路径 |
|---|---|
| Sentiment 舆情 Agent | `backend/app/agents/sentiment.py`（新增） |
| 演示舆情库（覆盖北京静态库全部 12 景点，含 1×SAFETY_RISK、1×SCAM_RISK、1×SERVICE_RISK、3×NEGATIVE_REVIEW） | `backend/app/data/sentiment_reviews.json`（新增） |
| 双管线接入（Validator 后插入） | `backend/app/services/pipelines.py`、`backend/app/services/plan_processor.py` |
| 行程书"舆情与避坑提示"章节（toC 第 4 章 / toB 第 5 章，编号已顺延） | `backend/app/services/markdown_reporter.py` |
| 抓取内容递归安全过滤（scan_payload，接入核心 Agent + 尾段并行 + 尾段补跑三条执行路径） | `backend/app/services/safety_service.py`、`backend/app/services/plan_processor.py` |
| 安全看板计数扩展（web_content_block / web_content_sanitize） | `backend/app/services/safety_service.py` |
| 单测 | `backend/tests/test_sentiment_agent.py`（5）、`backend/tests/test_safety_content_filter.py`（6） |

### 批次 2（P1）
| 交付物 | 路径 |
|---|---|
| 来源 URL 溯源（高德检索页链接 / 飞猪预订链接追加 / 静态库如实留空） | `backend/app/services/scenic_spot_service.py`、`backend/app/agents/itinerary.py`、`backend/app/services/markdown_reporter.py` |
| 行程书景点"信息来源"行 | `backend/app/services/markdown_reporter.py` |
| 白名单网页调研（白名单硬编码 2 域名、默认关闭、标准库实现零新依赖、1s 限速 5s 超时失败静默） | `backend/app/services/web_research.py`（新增）、`backend/app/agents/researcher.py` |
| 环境变量 | `.env.example`：`WEB_RESEARCH_ENABLED=false` |
| 单测 | `backend/tests/test_source_traceability.py`（3）、`backend/tests/test_web_research.py`（7） |

### 批次 3（P1）
| 交付物 | 路径 |
|---|---|
| toB 三角色演示账号（supervisor/sv2026、consultant/ct2026） | `backend/app/services/auth_service.py` |
| RBAC 守卫 `require_role(*roles)`（401 无 token / 403 FORBIDDEN_ROLE） | `backend/app/api/auth.py` |
| 三端点挂载（approval→supervisor+admin；replan、feedback→consultant+supervisor+admin） | `backend/app/api/plans.py` |
| 单测 | `backend/tests/test_rbac.py`（5：403/401/放行/顾问权限矩阵） |

### 批次 4（P2，重构）
| 交付物 | 路径 |
|---|---|
| Intake Agent（输入规范化：去"市"后缀、天数收敛、去重、transit_first 等 parsed_flags） | `backend/app/agents/intake.py`（新增）、`backend/app/services/pipelines.py`、`backend/app/services/plan_processor.py`（管线最前 + 规范化输入替换） |
| Budget Agent（预算分项/经济口径/挂起判定从 Validator 拆出，**字段名完全不变**） | `backend/app/agents/budget.py`（新增）、`backend/app/agents/validator.py`（瘦身）、管线改为 `… Itinerary → Budget → Validator …`、挂起判定迁移 |
| 下游双兜底读取（Budget 优先 → Validator 旧字段） | `backend/app/agents/base.py`（get_budget_payload）、debate/consultant/compliance/markdown_reporter/plans.py economy_tips |
| 单测 | `backend/tests/test_intake_agent.py`（4）、`backend/tests/test_budget_agent.py`（6） |

### 批次 5（P2）
| 交付物 | 路径 |
|---|---|
| Loop Engineering 提示词与自主验证报告 | `Loop_Engineering_提示词与自主验证报告.md` |
| AI 代码审查报告（5 个已修复 bug，全部经复现验证；候选线索逐一核实结论） | `AI_代码审查报告.md` |

### 文档
- `README.md`：toB 三角色演示账号、Agent 构成（10 节点/8 类职责对齐）、诚实声明 4 条新增
- `.env.example`：`WEB_RESEARCH_ENABLED=false`
- `工单要点提取与落点映射.md`：8 项缺口全部标记已实现（含日期）

## 二、回归结果（每批次均执行）

| 批次 | compileall | smoke_test 六链路 | pytest |
|---|---|---|---|
| 批次 1 | ✅ | ✅ 全绿 | 80→**86** 全过 |
| 批次 2 | ✅ | ✅ 全绿（exit=0） | 86→**90** 全过 |
| 批次 3 | ✅ | ✅ 全绿 | 90→**95** 全过（含旧 tob_workbench 用例补 token） |
| 批次 4 | ✅ | ✅ 全绿（exit=0） | 95→**105** 全过 |
| 交付后复跑 | ✅ | — | — |

## 三、与工单的差异说明

1. **"交通与住宿参考"章节**：批次 1 回归时发现 smoke 断言的该章节在历史输出中从未渲染过（先行失败断言）。按工单"以代码为准"原则，补渲染（数据源 Validator/Budget 的 travel_advice），并把 smoke 等待窗 90s→240s（外部 API 限流时段实测单稿 >90s）。
2. **toB 旧用例补 token**：RBAC 生效后 `test_tob_workbench` 的 replan 用例按新权限正确返回 401，用例补 admin token（与前端 admin.html 的 `auth()` 行为一致）；toB 前端 UI 本身无需改动（admin 登录即有全部权限，supervisor/consultant 可直接用新账号登录）。
3. **Sentiment 挂起兼容**：已确认 `CheckpointStore._next_node` 基于 pipeline_nodes 自动生效，Validator 挂起恢复后从 Sentiment 续跑，未额外处理。
4. **Tail 并行未变**：Sentiment 未加入 `TAIL_PARALLEL_NAMES`，保持串行（工单要求）。
5. **AI 代码审查报告的 Bug 1-5 均为"已修复"**：5 个中 3 个是本轮重构/新增暴露的真实缺陷（Budget 拆分下游崩溃、前端按钮 ID 重复、web_research 解析），2 个是先行失败问题（smoke 90s 窗口、断言章节缺失）；候选线索中 2 个核实为"不构成 bug"（adjusted 标记、pool.map 边界），1 个核实为口径自洽（travelers>2），已在报告"核实无误"区注明。

## 四、遗留与建议

1. `city_photo` 限速器为进程内实现，多实例部署需改分布式限速（压测报告已提示）
2. 首页 P95=98ms 贴线达标，如需余量可降 GZip 级别或改 zstd 预压缩
3. 生成链路吞吐受 `WORKER_COUNT=1` 限制，扩容路径已具备（唯一消费者名 + 分布式锁）
