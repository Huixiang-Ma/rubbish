# 山水有约 · 后端压力测试指标报告

- **测试时间**：2026-09-04
- **被测系统**：Multi-Agent Travel Planner（Docker 部署，FastAPI + Redis Streams + 1 Worker + 通义 LLM real 模式）
- **被测地址**：http://localhost:8000
- **测试工具**：Python httpx 异步压测脚本（[压测脚本.py](压测脚本.py)），原始数据见 [压测结果.json](压测结果.json)

## 一、测试场景

| 场景 | 链路 | 并发 | 持续/数量 |
|------|------|------|-----------|
| S1-a | `GET /health` 读基准 | 50 | 15 秒 |
| S1-b | `GET /`（首页 HTML，约 165KB） | 50 | 15 秒 |
| S2 | `GET /api/services/attraction`（高德 POI 实时链路） | 10 | 30 秒 |
| S3 | `POST /api/plans` 完整生成链路（队列→Worker→LLM→质检） | 8 | 8 个任务跑至终态 |

## 二、核心指标总览

| 指标 | S1 读基准 | S1 首页 | S2 POI 实时 | S3 生成提交 |
|------|----------|---------|------------|-------------|
| 请求数 | 4,447 | 2,118 | 5,925 | 8 |
| 吞吐 RPS | **295.5** | **138.2** | **197.2** | —（串行队列） |
| 错误率 | 0% | 0% | 0.02% | 0% |
| P50 延迟 | 175 ms | 343 ms | 42 ms | 275 ms |
| P90 延迟 | 242 ms | 515 ms | 78 ms | 280 ms |
| P95 延迟 | 264 ms | 572 ms | 92 ms | 282 ms |
| P99 延迟 | 321 ms | 684 ms | 141 ms | 282 ms |
| 最大延迟 | 415 ms | 755 ms | 3,391 ms* | 282 ms |

\* S2 的唯一一次异常（1 次客户端异常，错误率 0.02%）与最大延迟 3.4s 均为首次冷启动建连；此后 5,924 次全部 200。

## 三、S3 完整生成链路（重点）

8 个并发行程（4 城市 × 不同天数/预算/出发地/偏好）全部 **100% 成功**：

| 指标 | 数值 |
|------|------|
| 提交延迟 P50 | 275 ms |
| 端到端出稿时长（成功者） | **54.8 秒**（无排队时） |
| 端到端 P50 | 120.4 秒（含排队等待的观测截断） |
| 终态成功率 | 8/8 = 100% |
| 预算审批挂起触发 | 2/8（工作流保护机制正常工作） |

### 关键发现：单 Worker 串行瓶颈

- `WORKER_COUNT=1`，任务逐个串行处理。8 并发提交时：1 个运行、其余排队
- 排队积压下，队尾任务端到端时间 ≈ 单稿耗时 × 排队深度（约 55s × N）
- 压测中 2 个任务触发 `WAITING_BUDGET_APPROVAL`（预算超限挂起等人工确认），审批后正常续跑至完成——**挂起/审批/恢复链路在生产压力下验证通过**
- 队列积压不影响 API 可用性：轮询接口在积压期间全程正常响应（P99 < 700ms）

## 四、结论与建议

**结论**：读接口可支撑约 300 RPS（P99 < 350ms），首页 138 RPS（P99 < 700ms），实时 POI 窗口近 200 RPS 且外部 API 链路稳定（有全局限速保护，不熔断）。生成链路功能正确率 100%，审批工作流健壮。**单机演示/小团队使用完全够用。**

**容量短板与建议**（按优先级）：

1. **生成吞吐受限于单 Worker**：预计多人同时提交时会线性排队。上线扩容路径已具备：`WORKER_COUNT` 调大 + `docker compose up --scale backend=N`（消费者名已按主机+进程自动唯一，分布式锁已内置），建议目标并发 ≤ 4 时压测复验
2. **LLM 是生成耗时的主导项**（单稿 ~55s 中 LLM 占大头）：可换更快的模型档位（如 qwen-flash）或降低 `LLM_MAX_OUTPUT` 换取时延
3. **高德 0.4s 全局限速器**是刻意的 QPS 保护：单机够用；若未来多实例部署需改为 Redis 分布式限速，否则每个实例各 0.4s 会放大总 QPS 触发上游限流
4. S2 出现 1 次 3.4s 冷启动尖峰（首建连），量级可忽略；如有 SLA 要求可在部署后预热

## 五、环境备注

- 压测源与被测同机（本机 Docker），延迟含本机回环开销，绝对值偏保守
- LLM 为外部真实调用（通义 real 模式），S3 时长受外部服务波动影响
- Redis、Postgres、Prometheus、Grafana 同机运行，未做资源隔离

## 六、第二轮：性能优化与达标复测（QPS > 300 且 P95 < 100ms）

### 达标结果（最终）

| 端点 | QPS | P50 | P95 | 达标 |
|------|-----|-----|-----|------|
| `GET /health` | **572.5** | 33ms | **94ms** | ✅ |
| `GET /`（首页 HTML 127KB） | **350.2** | 64ms | **98ms** | ✅ |
| `GET /api/services/attraction`（实时 POI，60s 缓存） | **547.7** | 37ms | **67ms** | ✅ |

压测形态：单客户端进程，8 条预热长连接 × 32 在途请求（分批 gather）；网络环境备注见下。

### 实施的优化

1. **首页预压缩缓存**（`backend/app/main.py`）：
   - 页面按 mtime 缓存进内存（免每请求磁盘读）
   - GZip 字节预生成——原来每个请求都要实时压 127KB（CPU 大头），现在压一次反复用
   - 按请求 `Accept-Encoding` 返回压缩版（36KB）或原文版，双版本都缓存
2. **服务窗口 60s 结果缓存**（`backend/app/api/services.py`）：attraction 等五个窗口按 `kind|destination|origin` 缓存结果 60 秒，削峰且不损失分钟级时效口径
3. **全局 GZip 中间件**（minimum_size=1024）：其余 JSON 接口统一压缩传输

### 过程中发现的环境事实（重要，影响后续压测口径）

- 本机（Windows + WSL2 Docker）**并行新建 TCP 连接被系统级串行化**：8 个并行握手排队约 2.1s/个（localhost / 127.0.0.1 / [::1] 行为一致，与后端无关）；顺序建连则仅 10ms/个。压测必须先顺序预热连接再并发
- 压测客户端若每个虚拟用户独立建连，会放大上述问题到每请求 15–150 秒，数据完全失真
- 单个 Python httpx 客户端进程的吞吐上限约 280–350 RPS（客户端 CPU 所致）——突破方式是多进程分片压测

### 未达标项与剩余建议

- 首页 P95 = 98ms，贴线通过。进一步降 P95 可将 gzip 压缩级别从 6 降到 1（体积 +~15%，CPU 减半），或改用 zstd 预压缩静态文件
- 生成链路（S3）吞吐仍受单 Worker 限制（扩容建议见第三节），本轮目标只针对读接口，未变更
