# 工单要点提取与落点映射（文旅工单 × 图记忆工单）

> 版本：v1.0 ｜ 日期：2026-09-03
> 输入：①《多Agent协同-2-文旅资源调研与个性化行程规划系统任务工单》（`D:\Desktop\实训一\多Agent 协同项目 -2-...md`）；②《多Agent协同-7-跨系统协同共享图记忆与运行态强干预系统工单V1.0-20260810.pdf》（6 页，已全文提取）
> 目的：从两份工单中提取**本项目可使用**的要点，映射到现状与落点，供范围决策。

---

## 1. 工单 2（文旅）× 现状盘点：已实现为主，缺口如下

| 工单 2 要求 | 现状 | 判定 |
|---|---|---|
| 文件驱动规划 `workspace/travel_plan.md` + 断点续传 | checkpoint state.json + sha256 + 原子写入 + kill-9 恢复演示 | ✅ 已实现 |
| 多 Agent（Intake/Planner/Web Research/**Sentiment**/Itinerary/Budget/Human Review/Report 8 Agent） | 6 Agent + Reporter，诚实声明"不追求 8 个物理 Agent" | ⚠️ **缺独立舆情（Sentiment）Agent**；预算并入 Itinerary/Validator |
| 网页抓取 / 实时票价 | 静态景点库（诚实边界，无商业 API） | ⚠️ 口径性缺口，不补 |
| Prompt 注入防御 | SafetyService + 拦截率看板 + 红蓝对抗量化 | ✅ 已实现 |
| JWT 鉴权 + **角色**（顾问/主管）审批控制 | 演示级 HMAC token（admin/旅者双门），无角色体系 | ⚠️ 缺角色 RBAC（可后续批次） |
| 状态机（Planning/Running/Suspended/Recovering/Completed） | 11 态状态机 + 恢复 | ✅ 已实现 |
| 原子写入 + 哈希一致性 | AtomicWriter + sha256 | ✅ 已实现 |
| Docker Compose / 守护自启 / Prometheus+Grafana | compose + observability profile + /metrics | ✅ 已实现 |
| Locust 压测 QPS≥200 | load_test.py，实测 246 RPS 口径 | ✅ 已实现（**《压测报告》独立文档未见**） |
| HITL 超预算挂起 | WAITING_BUDGET_APPROVAL + 审批台 | ✅ 已实现 |
| 《Loop Engineering 提示词与自主验证报告》 | 未见独立文档 | ⚠️ 产出物缺口 |
| 《AI 代码审查报告》（3-5 个 AI 潜在 bug 与修复） | 未见独立文档 | ⚠️ 产出物缺口 |

**结论**：工单 2 的工程主体已被本项目覆盖；可提取的是 **3 个产出物缺口文档** + 2 个功能缺口（舆情 Agent、角色 RBAC），均建议另立批次，不动当前 toB 批次。

---

## 2. 工单 7（图记忆 + 运行态强干预）× 现状：五个可用点

工单 7 的两大痛点（多轮对话实体约束遗忘、运行中 State 无法强干预）恰好落在本项目**刚建好的对话式规划**与**既有 checkpoint 架构**的接缝上，可提取价值高。

### A. 实体三元组抽取与图记忆（可用 ✅，核心）

- 工单要求：参考 TencentDB-Agent-Memory / Mem0 / Cognee，把多轮对话实体关系沉淀为三元组（`(User)-[HAS_ALLERGY]->(Seafood)`），定义 Chat Memory / LLM-Wiki / Code-Graph 三类 Schema。
- 现状：无任何实体/关系表；仅 `document_chunks`（pgvector，mock 8 维向量）+ 内存兜底。
- **落点**：PostgreSQL 路线（工单允许"PG pgvector/Graph 扩展 **或** 轻量级 Neo4j"，选 PG 与现有 alembic/pg_mirror 一致，零新中间件）。alembic 新增迁移：
  - `memory_entities`（实体：name、type、thread_id/job_id、置信度）
  - `memory_triples`（head_id、relation、tail_id、source_round、status[active/superseded]、created_at）——LPG 建模用关系表 + 递归 CTE 做拓扑检索
  - Chat Memory ✅ 落 triples；LLM-Wiki ✅ 落 Researcher 景点调研条目；**Code-Graph 对文旅系统无业务意义 → 仅 Schema 预留不实现（诚实口径）**
- 抽取引擎：`memory/engine.py`——LLM real 模式结构化抽取（`try_generate_json` 边界已有）+ mock 关键词兜底；合并规则：同 (head,relation,tail) 去重，冲突标记 `superseded` 而非覆盖（保留可追溯）。

### B. 多层级检索（可用 ✅）

- 工单要求：向量检索 + 关联图拓扑检索。
- 落点：检索管线 = 第一层 pgvector 召回 + 第二层 triples 1-hop 拓扑扩展，合并去重。直接补强 toC 对话式规划的 context 合并（"第 15 轮仍记得第 3 轮的海鲜过敏"即演示分镜）。

### C. 运行态强干预 Memory Mutator API（可用 ✅，差异化亮点）

- 工单要求：强干预 API 允许外部/人工审批节点更新**运行中**的 Graph State，Agent 下次决策优先读取干预后状态；原子性修改防脏读；**动态修改需安全验签**。
- 现状：审批 API 仅在挂起态改状态；运行中任务无干预入口；worker 节点循环若内存持有 state，存在工单点名的"脏读/Lost Update"问题（实现时核对 `plan_processor.py` 每节点是否 re-load，若不需要加 version 校验）。
- 落点：`POST /api/plans/{job_id}/intervene`——Bearer/HMAC 验签（复用 auth 体系）+ SETNX 锁 + 原子写 state + `version++` + audit 落库 + 节点边界 version 校验。**与 toB 竞品主张"事前可追责"完美契合，是 HITL 审核台的天然增强。**

### D. 并发竞态审计 + Redis SETNX 锁（可用 ✅）

- 现状：`distributed.py` 已有分布式锁（worker 用），memory 写入/干预接口未用。
- 落点：intervene 与 triples 写入统一套锁；产出《AI 状态覆写竞态问题审计报告》。

### E. 测试与指标（可用 ✅）

- `tests/test_memory_intervention.py`：`test_extraction_triples`（三元组提取断言）+ `test_runtime_state_override`（强干预后下一节点不再引用被禁实体）。
- `scripts/simulate_conversation.py`：15 轮模拟（第 8 轮注入过敏实体、第 12 轮强干预、第 15 轮校验无违规推荐 + 全链路时延）。
- 指标：记忆提取 <150ms、干预成功率 100%——mock 口径可达成，UI/报告标注演示口径。

---

## 3. 落点建议：与当前 toB 批次的关系

**工单 7 主体（A-E，约 3 人日）建议独立成批**，按其自有产出物走（设计文档、决策记录、源码、测试集、两份报告、面试 QA 库）——塞进当前 toB UI 批次会把 1.5-2 人日的改造撑爆。

**当前 toB 批次可无损纳入的 UI 落点（增量约 0.5 人日）**：
1. 详情抽屉加第 4 页签**"记忆与干预"**：该任务/客户的记忆三元组卡片（过敏禁忌/偏好/约束）+ 干预历史时间线——竞品报告里"可审计到 Agent 级"主张的又一块 UI 拼图；
2. 审核台/抽屉加**"强干预"**操作入口（挂起态任务修正约束/预算 → intervene API）；
3. 抽屉页签预留挂载点：记忆批次未完成前显示"记忆中心建设中"空态，不阻塞当前批次交付。

**另立待办（工单 2 缺口）**：舆情 Agent、角色 RBAC、《压测报告》《Loop 报告》《AI 代码审查报告》三份产出物文档。


---

## 补齐状态（2026-09-04 实现回执）

| 编号 | 缺口 | 状态 |
|---|---|---|
| P0-1 | Sentiment 舆情 Agent | ✅ 已实现（agents/sentiment.py + 舆情库 + 双管线 + 行程书章节，单测 5 个） |
| P0-2 | 抓取内容安全过滤 | ✅ 已实现（scan_payload 递归扫描接入三条执行路径，看板计数扩展，单测 6 个） |
| P1-1 | 来源 URL 溯源 | ✅ 已实现（高德/飞猪 source_urls + 行程书"信息来源"行，单测 3 个） |
| P1-2 | 白名单网页抓取 | ✅ 已实现（web_research.py 白名单硬编码 + 默认关闭，单测 7 个） |
| P1-3 | RBAC 角色审批 | ✅ 已实现（supervisor/consultant 演示账号 + require_role 三端点，单测 5 个） |
| P2-1 | 独立 Intake Agent | ✅ 已实现（输入规范化 + parsed_flags，管线最前，单测 4 个） |
| P2-2 | 独立 Budget Agent | ✅ 已实现（从 Validator 拆出，字段名不变，双兜底读取，单测 6 个） |
| P2-3 | 两份产出物报告 | ✅ 已产出（Loop_Engineering_提示词与自主验证报告.md、AI_代码审查报告.md） |

回归：compileall 通过；smoke_test 六链路全绿；pytest 105 用例全部通过。
