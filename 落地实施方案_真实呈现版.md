# 可落地实施方案 · 真实效果呈现版

> 版本：v1.0 ｜ 日期：2026-08-29
> 上位文档：《融合版落地方案 v1.1》（现状映射）、《融合版需求文档》（S/C/B 需求）
> 目标：在"无商业旅游数据 API 授权"约束下，把剩余批次做成**观众看不出是演示**的效果呈现版，且每个验收点有可复现实测证据。

---

## 0. "真实效果"怎么实现：三条线

| 线 | 手段 | 真实度 | 依赖 |
|---|---|---|---|
| **真实生成** | 配置 `LLM_MODE=real` 后，C 系列全部内容（辩论/点评/对话/剧本/反馈）由真模型生成；代码边界已就绪（`try_generate_json` + mock 兜底），**配 key 即切真，零代码改动** | 真 | ⚠️ 唯一外部依赖：一个 OpenAI 兼容 API key（通义/DeepSeek/OpenAI 任一，最低额度即可） |
| **真实渲染** | ① 流式：FastAPI SSE 真·流式输出；② 地图：Leaflet + OpenStreetMap 开放瓦片（开放数据非商业授权，需现场联网；断网自动降级 SVG 坐标示意）；③ 图表：ECharts 本地 vendor，断网可跑；④ 语音：浏览器 Web Speech API（本地免费真 TTS）给名导团/直播辩论配音；⑤ 导出：html2canvas 真 PNG 长图、打印 CSS 真 PDF、CSV 真导出 | 真 | 地图需现场联网，其余断网可跑 |
| **真实验证** | 每个验收点配可复现命令与实测数字：压测曲线（已实测 245.8 RPS）、S4 增量耗时对比、拦截率统计、断点恢复日志 | 真（可复现） | 无 |

> 诚实口径不变：出行数据（通勤/票价/天气/住宿价）仍为"本地估算/演示样例"；我们呈现的真实是**生成真实、交互真实、验证真实**，不谎称接了携程/高德。

---

## 1. 唯一卡点（需要你做）

1. **申请 LLM API key**（推荐通义 DashScope 或 DeepSeek，充值 10 元够全部演示）→ 按 README 配置 env → 重跑 smoke 即全链路真模型。没有它：批次 4 内容类玩法退回模板演示（架构话术兜底："边界已留，配置即切真"）。
2. **确认演示现场可联网**（地图瓦片用；不可联网则自动降级，不影响其他项）。

---

## 2. 批次 3 · toB 管控（真实后台，约 1.5 天）

### B1 顾问工作台 `frontend/admin.html`

| 项 | 实现 |
|---|---|
| 方案列表 | `GET /api/plans?status=&customer=`：扫描 DATA_ROOT 的 state.json 建内存索引（job_id/状态/进度/客户/时间），前端表格 + 状态筛选 |
| 客户归属 | `POST /api/plans` 请求加可选 `customer` 字段落 state；列表按客户分组 |
| 版本对比 | replan 父子 job 两份 travel_plan.md 用 difflib 真算 diff，前端红绿高亮（"改了 N 处/保留 M 处"） |
| 改单 | 页面内 replan 表单（复用现有 API + diff_summary） |
| 内容位 | 公告/文化活动静态 JSON（吸收原型"公共服务"），工作台可编辑展示 |

### B2 独立审核台（admin.html 内页签）

- `GET /api/approvals/pending`：聚合 WAITING_BUDGET_APPROVAL / WAITING_SAFETY_REVIEW 任务队列
- 页面：挂起队列 → 逐条 confirm/reject（复用 approval API，带 base_version 409 保护）→ 操作人落审计
- 真实点：队列是真实挂起任务，审批 409/版本冲突真实触发

### B4 合规审计与反馈

- `GET /api/plans/{id}/audit`（单任务）、`GET /api/audit/export.csv`（全量：谁/何时/改了什么/是否人工确认）
- 反馈通道：`POST /api/plans/{id}/feedback`（好评/投诉 + 内容）落审计（吸收原型"填写好评/投诉"）

### B3 安全治理看板（增强现有 /api/safety/summary）

- SafetyService 每次扫描落审计（allow/sanitize/block 全记）→ 拦截率 = block/总扫描有真实分母
- 新增：攻击模式分布（高危 pattern 命中统计）、按天时间桶曲线、CSV 导出
- 前端：ECharts 分布饼图 + 拦截率折线（本地 vendor）

### B5 白标交付

- `POST /api/plans/{id}/export?brand=&consultant=`：md 头部注入企业名/Logo 占位/顾问署名 + 交付记录落审计
- 前端打印 CSS → 浏览器"另存为 PDF"（真 PDF，无第三方库）

### S4 节点级增量重规划（达成"耗时降≥70%"）

- replan 时若 destination/preferences 未变：预填 parent 的 Researcher 输出与 `completed_nodes`，`resume_from=Planner`，只重算受影响链
- `scripts/bench_replan.py`：全量 vs 增量各跑 N 次，输出耗时对比表（mock 下看趋势，**LLM real 下实测达标**）
- 验收口径：LLM real 模式耗时降 ≥70%，演示时现场跑 bench 出数字

---

## 3. 批次 4 · toC 玩法（真实生成，约 2 天）

> 每项都是"Agent/接口 + SSE/前端"模式；LLM real 生成真内容，失败自动落模板。前端页签全部进 admin/toC 页。

| 项 | 实现 | 真实点 |
|---|---|---|
| **C6 直播辩论·你当裁判** | `GET /api/plans/{id}/debate/live`（SSE）：规划方 vs 游客方基于真实行程数据多轮互怼（LLM real；按句推流做流式感）；前端全屏双栏气泡 + Web Speech 朗读 + `POST vote` 投票锁定（落审计并写进 md） | 真流式、真语音、投票真实改变产物 |
| **C5 角色名导团（名人对话）** | GuidePanelAgent：3 persona（杜甫/本地吃货/亲子妈妈）差异化点评；`POST /api/plans/{id}/dialogue` 多轮追问对话 | 真 LLM 多轮对话 + 语音朗读 |
| **C3 虚拟游客 swarm** | SwarmAgent：3 个游客 persona 用真实数据"试玩"（visit_minutes/通勤估算/舒适度演示表）→ 差异化反馈（如"周二下午爆满改早"） | 反馈引用真实估算数字 |
| **C4 反事实后悔药** | CounterfactualAgent：已选 vs 放弃景点（Researcher 候选差集）+ 分项预算数字 → "放弃 X / 换来 Y"对照卡 | 对照卡引用真实预算分项 |
| **C7 心情→人生剧本** | `PlanRequest` 加可选 `mood`；MoodAgent 情绪弧线叙事 + 文化主题路线（吸收原型）；前端输入框加心情词 | 真生成叙事行程 |
| C2 前端双栏 | toC 结果页把"决策辩论"渲染为真双栏卡片 | 呈现升级 |

---

## 4. 批次 5 · 收尾（约 1.5 天）

| 项 | 实现 | 真实度 |
|---|---|---|
| 地图模式 | 结果页 Leaflet + OSM 瓦片标注每日景点与动线；检测断网自动切 SVG 坐标示意 | 在线=真地图；离线=降级 |
| 周边服务 | 静态库：找美食/车位/厕所（各 10-20 条演示 POI）+ 位置计算复用 haversine"距您 X 米" | 数字真算，POI 演示 |
| 景区舒适度 | 按景点热度演示表 + 时段舒适度等级 | 演示（标注） |
| 我的行程/收藏 | localStorage 收藏 + 我的行程列表（历史 job） | 真数据 |
| C9 攻略长图 | html2canvas 一键导出 PNG（含品牌标识） | 真 |
| C10 以图规划 | 上传图 → mock 视觉描述 → 行程（标"演示"） | ⚠️ |
| C11 彩头 | 盲盒/徽章/年报空壳（标"行业标配"） | 空壳 |
| B6 经营看板 | ECharts：任务量趋势/完成率/客户分布——**聚合真实任务数据** | 真 |
| B7 多租户 | job 加 tenant 标签 + 列表过滤隔离（演示级） | 演示 |

---

## 5. 排期与执行顺序

| 顺序 | 内容 | 工时 | 产出验收 |
|---|---|---|---|
| 1 | 批次 3 toB（B1→B2→B4→B3→B5→S4） | 1.5 天 | admin.html 五个页签可用；bench_replan 出对比数字 |
| 2 | 批次 4 toC 玩法（C6→C5→C3→C4→C7） | 2 天 | 直播辩论/名人对话/swarm/后悔药/剧本全链路 |
| 3 | 批次 5 收尾 | 1.5 天 | 地图/长图/经营看板/周边服务 |
| 4 | 联调 + 录屏（分镜见 §7） | 1 天 | 录屏 + 文档收尾 |

> 与需求文档"全量 135 人时（3 人 5.6 天）"口径对齐；单人按上表约 6 天。

---

## 6. 每批的统一验收纪律

1. compileall + 内存/Redis 双模式 smoke 全绿后才算完成；
2. 每个新功能至少一条 smoke 链路或可复现 curl；
3. ⚠️ 项在 UI 上可见位置标"演示"；
4. 每批完成更新《融合版落地方案》状态列与 README 演示脚本。

---

## 7. 真实效果演示分镜（录屏/答辩用，随批次更新）

1. **真模型生成**（有 key）：提交"北京 3 天 5000 元，心情：想被治愈" → 状态流转 → travel_plan.md 的辩论/剧本为真 LLM 内容（可切换 LLM_MODE=mock 对比同一问题的模板输出，反差即卖点）。
2. **toB 管控**：admin 工作台方案列表 → 超预算任务进审核台 → confirm → 审计 CSV 导出打开（谁/何时/改了什么）。
3. **算得准**：地图模式看每日动线（OSM 真瓦片）→ 展示"故宫→长城同日"任务被拒（时空引擎算出 66 公里/打车 3 小时）。
4. **直播辩论**：全屏双栏 SSE 流式互怼 + 语音朗读 → 投票锁定 → 结果写入 travel_plan.md。
5. **名人对话**：与"杜甫"追问行程取舍（真 LLM 多轮）。
6. **断点恢复**：任务运行中 kill -9 后端 → 重启 → `[recovery] requeued=[...]` → 续跑完成（5 秒口径）。
7. **压测**：现场跑 `load_test.py` 出 RPS 数字 + bench_replan 出增量对比数字。

---

## 8. 风险与兜底

| 风险 | 兜底 |
|---|---|
| 无 LLM key | mock 模板全链路可演示；话术："架构边界已留，配置即切真" |
| 现场断网 | 地图自动降级 SVG；其余全离线 |
| LLM 延迟高 | SSE 分句推流缓解等待感；演示用预跑任务（缓存兜底，口径同方案风险表） |
| LLM 输出偏差 schema | `_valid` 校验 + 模板兜底（已实现），演示不会开天窗 |
| 单文件前端膨胀 | admin.html 与 toC 页分离；组件复用保持 vanilla，迁移 React 另立决策 |
