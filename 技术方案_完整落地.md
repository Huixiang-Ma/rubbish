# 技术方案（完整落地 · P0-P3）

> 配套《完整落地方案.md》（路线图）与《实现方案_完整落地.md》（任务拆解）。
> 原则延续既定架构：文件事实源 + PG 镜像、单文件原生 HTML 前端、演示口径必须诚实降级。本文只写"新增/改造"的设计，不重复已有架构。

---

## 1. 真实票务链路（飞猪 MCP 三层降级）

### 1.1 数据流

```
toC 服务大厅点击 ──► GET /api/services/{kind}?destination=&origin=
                          │
                 ┌────────▼─────────┐
                 │ services.py      │  现有 TTL 缓存（_window_cache）保留
                 └────────┬─────────┘
                          ▼
              _dispatch_service_window() 改造为三级：
              ① fliggy_client（FLIGGY_AI 开关开启时）── 真实班次/报价，item 带 booking_url
              ② travel.* 本地估算 + 官方渠道指引（现有逻辑，作为 12306/航司官网口径）
              ③ 全失败：note="实时数据源暂不可用" + 官方渠道
```

### 1.2 关键设计

- **响应契约扩展**（向后兼容）：每个窗口结果新增 `"source": "fliggy" | "local"` 顶层字段；fliggy 条目新增 `booking_url`（带 CPS 追踪参数的供应商跳转链接）。前端 `renderServiceWindow`（index.html 第三段脚本）按 `booking_url` 渲染"前往预订"跳转链接替代现有 `data-svc-book` 死按钮；无 `booking_url` 的条目维持官方渠道口径。
- **熔断与降级**：`fliggy_client` 增加进程内熔断器（连续 3 次失败或 P95>5s → 熔断 60s，期间直接走 ②）。复用 `_window_cache`（TTL 5 分钟）控制配额，fliggy 命中率与耗时进 Prometheus（`fliggy_requests_total{result=ok|fail|breaker}`）。
- **超时预算**：MCP 调用 3s 超时（httpx），超时即降级——服务大厅是"即点即看"场景，慢数据等于没数据。
- **字段映射**（fliggy MCP 返回 → 现有窗口 schema，在各窗口函数内做适配器）：
  - train: `train_no/departure/duration/seat/price/booking_url`
  - flight: `flight_no/departure/duration/seat/price/booking_url`
  - hotel(merchant)/attraction/entertainment: 名称/评分/位置/价目提示/预约链接
  - 映射失败的脏数据**丢弃不渲染**，宁缺毋假（全站诚实口径延续）。
- **Spike 前置**：实现前先做半天接口探测（真实 key 调一次五类查询），确认 MCP 工具清单与字段；探测脚本落 `backend/scripts/probe_fliggy.py`。**若飞猪不提供国内火车票，则 train 窗口固定走 12306 官方渠道口径，仅 flight/hotel 走 CPS。**

## 2. 生成中直播辩论（SSE + 投票）

- 后端 `GET /api/plans/{id}/debate/live`（SSE）与 `POST .../debate/vote` 已存在，直接消费。前端改造点（index.html 第三段）：
  - `pollStatus` 检测到 `current_agent === "Debate"` 时建立 `EventSource(apiBase + "/api/plans/" + jobId + "/debate/live")`；消息追加进 `genLog`（区分角色配色，复用现有 logRow 样式加 `.debate-pro/.debate-con` 类）。
  - 辩论进行中在弹层插入投票条（复用 `vote-track` 样式），点击 → `POST /debate/vote {job_id, topic_id, side}` → 后端计入裁决权重，返回最新倾向后本地重绘。
  - SSE `onerror` 静默关闭（生成日志主链路不受影响）；`COMPLETED` 或弹层关闭时 `close()`。
  - 历史回看：辩论章（chDebate）后续接 `GET /{id}/debates` 结构化双栏（正方/反方/裁决卡），替代纯 Markdown 提取。

## 3. 体验中心四模块接线（per-job 上下文）

统一上下文机制：第三段脚本新增模块变量 `lastJobId`（生成完成时与历史回看时赋值）。体验中心点击时：

| 模块 | 接口 | 前端行为 |
|---|---|---|
| 决策辩论 | `GET /{id}/debates` | 双栏卡片（正/反 + 裁决），数据源为该 job 真实辩论 |
| 名导团 | `GET /{id}/guides` + `POST /{id}/dialogue` | 三个向导 chip + 对话输入框，回复追加渲染 |
| 虚拟游客 | `GET /{id}/swarm` | 人格卡列表（舒适度/步数/预警为真实计算值） |
| 反事实 | `GET /{id}/counterfactual` | "换一种改法"按钮循环渲染推演结果 |
| 地图周边 | `GET /{id}/map` + `GET /{id}/nearby?kind=` | Leaflet（vendor 已有）画动线 polyline + 点位 marker；"最近的洗手间/车位/好面"按钮查真实 POI 并在图上高亮 |

无 `lastJobId` 时弹层提示"先生成或从历史行程选择一本行程书"，并附历史列表快捷入口——不展示任何静态示例数据（延续诚实口径）。每个弹层对同一 job 做内存缓存，行程重新生成后失效。

## 4. 分享页

- 新增 `frontend/share.html`（独立轻量页，复用主站样式变量），后端新增路由 `GET /s/{job_id}` 返回该页。
- 页面加载即 `GET /api/plans/{id}/result` 渲染只读行程书（逐日 + 七章 Markdown 视图，不做表单/交互）；页脚固定"AI 生成内容 · 价格以供应商公示为准"。
- 无需登录；`X-Robots-Tag: noindex`；结果接口对 `job_id` 枚举的防护：UUID 段 12 位 hash 已具备不可猜性，另加分享频控（IP 60 次/分）。
- toC 生成完成后在书封下加"分享"按钮 → `navigator.clipboard` 复制链接。

## 5. 移动端适配

单文件 CSS 追加两档断点，不改 DOM 结构：
- `≤820px`：`.pipe`/`.feat-grid`/`.hall` 降为 2 列；`hero-stats` 换行；规划表单 `sb-places` 竖排。
- `≤480px`：全部单列；`book-tabs`/`day-tabs` 横向滚动；`gen-overlay`/`d-overlay` 全屏化（inset:0，圆角取消）；触控目标 ≥44px。
- 验收基线：375×667（iPhone SE）与 390×844 全流程真机可用。

## 6. 账号体系与历史云端化（P2）

- **登录方式**：保留现有 HMAC token 机制（`auth_service.py`），realm=toc 增加两种凭证：手机号+短信验证码（阿里云短信，`SMS_*` 配置）、微信扫码（站点应用，OAuth2 回调换 token）。口令登录保留为兜底。
- **用户-任务归属**：
  - PG `plan_jobs` 加列 `user_id TEXT`（幂等 ALTER）；文件 state 同步写 `user_id`（`PlanRequest` 新增服务端注入字段，前端不传，由 token 解出）。
  - 新增 `GET /api/plans/mine`（Bearer 鉴权，按 user_id 过滤，复用 job_index 缓存）——toC 历史列表数据源从 localStorage 切到此接口；localStorage 降级为 `lastJobId` 等轻量缓存。
  - 未登录仍可生成（游客单，user_id=null），登录后可"认领"近 72h 内同设备游客单（localStorage job_id 列表一次性绑定）。
- **迁移**：存量 planHistory 中的 job_id 首次登录时批量调用 `POST /api/plans/claim` 绑定。

## 7. CPS 预订转化

- `booking_url` 由后端统一构造：`fliggy_client` 返回的供应商链接追加推广位参数（`UNIONID`/`SID` 等入 `.env`，`FLIGGY_CPS_*`），前端不感知参数细节。
- 埋点：`booking_clicks_total{kind}`（Prometheus Counter）+ 跳转前写 audit（job_id 可空）。
- 归属：点击时若带 `lastJobId`，记录 job↔点击关联，供后续佣金对账粗口径。

## 8. 成本治理

- **token 埋点**：`llm_client._chat_completion` 响应解析 `usage`（prompt/completion tokens），累计 `llm_tokens_total{node}` Counter + 单任务 state 记录 `llm_cost`（按 `LLM_PRICE_IN/OUT` 环境变量折算）。
- **看板**：Grafana 增 panel（每单成本分布、每日 token 消耗）；`GET /api/stats/overview` 增"近 7 日 LLM 成本"。
- **配额**：Redis `INCR quota:{user|ip}:{yyyyMMdd}` EX 86400；免费阈值 `FREE_PLAN_PER_DAY`（默认 3）超量返回 402/429 + 前端引导登录/明日再试。复用现有 `WAITING_RATE_LIMIT` 状态位展示。
- **防刷**：`POST /api/plans` 增加 IP 级 10 次/小时（Redis 滑窗）。

## 9. 部署拓扑与 CI/CD

```
GitHub/Gitee push ─► CI：pytest + ruff ─► build 镜像 ─► 推 ACR
                                                    ─► deploy job：ssh 主机 `./deploy.sh`
  deploy.sh = tar 备份 data ─► docker compose pull ─► up -d ─► /health 探活 ─► 失败自动回滚上一镜像 tag
```

- 主机拓扑沿用 Compose：backend(可 `WORKER_COUNT>1`) + redis + postgres + prometheus + grafana；Caddy 反代 80/443 自动签证书，`/b` 路径加 basic-auth + IP 白名单。
- 备份：宿主 cron 每日 `tar data/jobs` + `pg_dump`，保留 14 天。
- 告警：Prometheus rules（queue_depth>50 持续 5m、job 失败率>20% 5m、backend up==0、PG/Redis down）→ Alertmanager → 飞书机器人 webhook（notify 通道复用）。

## 10. 安全基线

- 演示口令全部轮换；`AUTH_ENABLED=true`；`.env` 不入 git（`.env.example` 同步更新新增变量：`SMS_*`/`WECHAT_*`/`FLIGGY_CPS_*`/`LLM_PRICE_*`/`FREE_PLAN_PER_DAY`）。
- 依赖：`pip-audit` + `npm audit`（vendor 手工核查）入 CI。
- 服务端新增接口全部过现有 RBAC 装饰器口径；`/s/{share}` 与 `/api/services/*` 为唯一免登录面，各挂频控。

---

## 附：新增/变更接口一览

| 接口 | 变更类型 | 说明 |
|---|---|---|
| `GET /api/services/{kind}` | 响应扩展 | +`source`、item +`booking_url` |
| `GET /s/{job_id}` | 新增 | 分享页路由 |
| `GET /api/plans/mine` | 新增 | 登录用户历史（Bearer） |
| `POST /api/plans/claim` | 新增 | 游客单认领 |
| `GET /api/stats/overview` | 响应扩展 | +近 7 日 LLM 成本 |
| `POST /api/plans` | 行为变更 | 服务端注入 user_id；IP/用户配额 |
