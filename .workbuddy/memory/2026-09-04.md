# 2026-09-04 工作日志

## toB 工作台前端（/b）整页重生成 — 完成
- 文件：`frontend/admin.html`（单文件重构，1662 行，HTML/CSS/JS 全内联；ECharts 走 `/vendor/echarts.min.js` + document.write 兜底）。
- 视觉重构：公司管控端风格（墨绿 #25604a + 金 #a97f3c + 深绿侧栏 #13251c），白卡片圆角、登录门禁 split-screen、8 页导航分 4 组。
- 页面与后端 API 契约全匹配（来源 backend/app/api/{plans,admin,safety,auth}.py、app/config.py）：
  - plans→/api/plans(+detail/result/audit/approval/replan/diff/export/feedback)
  - approvals→/api/approvals/pending（批准 approve / 驳回 reject）
  - safety→/api/safety/summary；audit→/api/plans/{id}/audit + /api/audit/export.csv
  - voice→/api/feedbacks；export→POST /api/plans/{id}/export（brand/consultant）
  - stats→/api/stats/overview（daily_created/status_distribution/customers）；customers=前端按 customer 聚合
  - 登录→POST /api/auth/login {realm:'tob'}；演示账号 admin / wl2026（auth_service.verify realm tob 校验，AUTH_ENABLED=false 默认宽松）
- 详情抽屉：行程书(md 轻量渲染)/决策溯源(debates+counterfactual+swarm)/审计时间线/记忆占位，5s 轮询；行级 diff 弹窗（父版本对比）。
- 认证：localStorage adminToken+adminName；/api/auth/me 校验 role=admin。
- 冒烟：node --check + HTML 配平 + jsdom 38 断言全过（mock fetch 覆盖 8 页导航、登录门禁、改单弹窗、diff、溯源 tab、Escape/遮罩关闭）。
- 待办提醒：记忆中心(共享图/强干预)属工单7独立批次，本页为占位说明。

## 功能差距审计（规格 vs 实现 vs 后端能力）
- 规格内（2026-09-03-tob-workbench-design 8 页）全部覆盖；8 个异常闭环点核对：A1(409文案✓) A3(抽屉打开拉取✓) A4(口径小字✓) B1(数据窗口标注✓) B2(CSV blob✓) B3(echarts dispose✓) B4(后端口径) 均落实。
- **唯一规格内缺口 A2**：客户管理"未归属"行仍可点"查看方案"，而后端 plans 过滤是 `state.customer != customer` 严格比对，customer=未归属 匹配不到空值→跳转空列表。裁决应为"未归属行禁用跳转+title 解释"，尚未修复。
- 规格外（后端已就绪、前端未呈现、属工单7/后续批次）：`/guides`角色导游点评、`/dialogue`多轮对话、`/debate/live`(SSE)+`/debate/vote`(投票落审计)、`/map`+`/nearby`(点位表,全内联下不宜真地图)、`/api/integrations/status`(外部依赖状态,契合管控台)、`/semantic/*`(弱)。正确不接：POST /api/plans、/auth/register(游客端/企业发号职责)。

## 三处缺口补齐 — 完成（第 2 轮）
- 按产品全景 B-1~B-7 验收，补 3 个前端缺口（后端零改动），文件现 1698 行：
  1. **安全页"最近拦截明细"表**：page-safety 新增 `#safetyBlockTable`（时间/任务编号/风险等级/命中证据），接 `/api/safety/summary.recent_safety_blocks`；HIGH 含 high/高风险 正则红色标注（inline var(--danger-soft/deep)），空态"暂无拦截记录"。
  2. **经营看板租户过滤**：page-stats 新增 `#statsTenant`+`#statsApply`+`#statsReset`，loadStats 拼 `?tenant=`（encodeURIComponent），meta 行显示"租户视图：xxx/全部"。注：plans 列表响应**不含 tenant 字段**，故不做 datalist、沿用方案页自由输入式（与后端等值过滤一致）。注意后端 overview 返回 `tenant` 回显字段。
  3. **A2 未归属裁决落地**：loadCustomers 中 `customer==='未归属'` 行去掉 data-customer/clickable、操作列改"不可筛选"span、行 title 解释；真实客户行行为不变。事件委托靠 closest('tr[data-customer]') 天然隔离。
- 冒烟：jsdom 24 断言全过（明细表含红色标注+空态、租户请求带参+口径切换+重置、未归属点击不离页不污染筛选、真实客户跳转预填、其余 loader 无异常）。

## toB 功能清单 × 项目内可实现性评估（产品咨询轮）
- 基准：通用 toB 功能清单（6 族+P0 8+横切 6）套本项目代码资产逐项判定。
- 盘点关键资产：backend 有 agents(planner/researcher/validator/compliance/consultant/debate/itinerary/mood)+plan_worker+queue(memory|redis)+recovery+checkpoint+atomic_writer；**真实外部客户端封装存在**(amap/fliggy/qweather_client)；notify.py=飞书群机器人(生产可用,未配 webhook 静默)+邮件扩展点占位；pg_mirror 默认 disabled(启用才得语义检索/镜像表)；Prometheus metrics+observability/；integrations.py=**扩展点式**(地图/搜索/OCR 本地 Mock available()=True、Remote available()=False、支付"禁止纳入 MVP"代码注释原文)；auth 仅 admin/traveler 两角色+require_admin 单一守卫(HMAC Bearer)；config 全走 env(auth_enabled/admin/feishu/queue/llm)。
- 判定骨架：✅已落地=品牌白标(export 文稿+预览)/经营看板(overview 三图+租户过滤)/实时风险拦截(safety 三图一表 high→block)/客户聚合；🔧底座齐待开发=审核流程状态图(state.json+JobStatus 全量可数,缺 SVG 泳道+节点停留统计)、客户分级/指标体系(纯前端可演示级)、开放 API 密钥(FastAPI 自带 OpenAPI)、站内通知中心(可基于 jsonlog+飞书扩展点)、全局搜索(内存兜底 semantic 可演示)；⚠️半程=多租户(应用层 state.tenant 过滤可用,无租户生命周期/配额/泄漏扫描,同库同文件无 DB 级隔离)；❌外部依赖=SSO(自签 token 无 OIDC/SAML,需第三方 IDP)、组织角色树(需新增 RBAC 模型+改造 require_admin 守卫+管理页)、策略中心(代码内规则需抽 DSL+热更新)、真实第三方健康探测(集成状态卡只能展示"扩展点状态",/api/integrations/status 后端就绪前端 30 行可接入)。
- 三大约束墙：①文件 JSON 主存+应用层租户字段(非 DB 级隔离) ②外部服务策略=Mock/明确不接/无 key(地图票务支付邮件短信 SSO) ③身份与配置=单 admin+env 驱动,运行时配置化需重构数据模型。
