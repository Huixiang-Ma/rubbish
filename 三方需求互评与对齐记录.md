# 三方需求互评与对齐记录

> 文档版本：v1.1（正式交付版）  
> 日期：2026-08-28  
> 依据：《实现顺序梳理.md》、AI-A 需求澄清结果、AI-B 工程方案结果及交叉评审结果  
> 目的：在进入编码前，完成 Human + AI-A + AI-B 的需求转述、冲突点梳理与对齐裁决。

---

## 目录

1. 参与方职责
2. 原始需求转述
3. AI-A 评审记录
4. AI-B 评审记录
5. 关键异常点对齐结果
6. 需要学生裁决的问题
7. 已达成共识的裁决
8. 待确认项清单
9. 最终对齐结论

---

## 1. 参与方职责

| 参与方 | 职责 | 关注点 |
|---|---|---|
| Human / 学生 | 下达需求、裁决冲突、圈定 MVP、控制范围 | 业务合理性、工作量、交付质量 |
| AI-A | 逻辑分析、需求转述、边界异常挖掘、业务闭环建议 | 场景、异常、防御、退路 |
| AI-B | 工程实现、技术选型、状态机、接口、锁、存储设计 | 可落地性、并发、状态恢复、工程规格 |

---

## 2. 原始需求转述

项目目标是实现一个“多 Agent 文旅行程规划系统”。系统不应被理解为一次性生成旅游攻略的普通聊天机器人，而应体现以下主线：

1. 从最小端到端链路开始，先用 1 个 Planner Agent 跑通“输入一句旅行需求 → 输出 `travel_plan.md` → 前端展示”闭环；
2. 再逐步扩展到 8 个职责 Agent；
3. 在接入第二个 Agent 之前定稿 `travel_plan.md` schema；
4. 若要支撑 QPS≥200，Step 0 就必须裁决异步架构；
5. 支持状态持久化、断点恢复、高并发防重、Prompt 注入防御、预算审批挂起、增量重规划；
6. 彩头、多租户、经营看板等功能可后置或砍掉。

---

## 3. AI-A 评审记录

### 3.1 AI-A 核心判断

AI-A 认为原始需求主线清晰，产品定位应为面向游客与文旅顾问的多 Agent 行程规划与审核系统。其核心结论包括：

- 不承诺实时路网、全国全量景点库、生产级 SaaS、多租户和完全无人审批；
- MVP 必须覆盖端到端链路、断点恢复、Prompt 注入防御、增量重规划、审计和录屏；
- 外部网页内容默认不可信；
- 文件并发写入必须禁止多个 Agent 直接写最终 Markdown。

### 3.2 AI-A 重点异常点

- 文件并发写入损坏；
- API 速率限制；
- 恶意 Prompt 注入；
- 断点状态检测；
- 高并发重复审批与预算资损。

### 3.3 AI-A 结论

AI-A 主张：

- Step 0 就采用异步 `job_id` 模式；
- `travel_plan.md` 只是展示产物，不是唯一状态源；
- 八大职责可以文档完整保留，工程上允许 4 Agent 承担 8 段职责；
- QPS≥200 只承诺提交接口，不承诺完整生成吞吐；
- 高风险 Prompt 注入应直接拦截，中风险内容摘要后降权使用。

---

## 4. AI-B 评审记录

### 4.1 AI-B 核心判断

AI-B 认为原始需求已经具备工程落点，并补全了可执行方案：

- 前端 → FastAPI → Redis Streams → Worker → Agent Orchestrator → LLM / 工具 → 状态存储；
- 采用异步任务架构，不走同步整链路；
- 通过 Redis Checkpointer、文件快照、数据库和原子写支撑恢复；
- 通过 Redis 分布式锁防重复执行、防重复审批、防并发写文件；
- Reporter 单点生成 `travel_plan.md`。

### 4.2 AI-B 重点异常点

- 文件并发写入损坏；
- API 速率限制；
- 恶意 Prompt 注入；
- 断点状态检测；
- 高并发防重；
- 状态挂起；
- 超时解耦。

### 4.3 AI-B 结论

AI-B 主张：

- `travel_plan.md` 只能作为结果产物；
- LLM 限流时进入 `WAITING_RATE_LIMIT`；
- Budget 超限时进入 `WAITING_BUDGET_APPROVAL`；
- Safety 失败默认保守拦截；
- Output Parse 失败必须 fallback 重试，不能直接信任原文；
- 断点恢复验收口径应解释为“5 秒内恢复调度”，不是“5 秒内生成完毕”。

---

## 5. 关键异常点对齐结果

### 5.1 文件并发写入损坏

**共识：**

- 不允许多个 Agent 并发写 `travel_plan.md`；
- Reporter 单点原子生成 Markdown；
- 状态文件采用 `.tmp → fsync → rename → sha256`；
- 任务独立目录存放状态；
- hash 不一致进入人工恢复，不自动覆盖。

**对齐裁决：**

> `travel_plan.md` 只作为展示与导出产物，真实状态以 checkpoint / DB / JSON 状态为准。任何 Agent 不得直接并发写最终 Markdown。

### 5.2 API 速率限制与超时解耦

**共识：**

- Step 0 就采用异步 `job_id` 架构；
- FastAPI 只负责接收请求与返回 `job_id`；
- Redis Streams 或同类队列负责削峰；
- Worker 层执行限流与指数退避；
- 任务提交接口吞吐与完整生成吞吐必须分开定义。

**对齐裁决：**

> QPS≥200 仅承诺“提交任务接口 QPS”，不承诺“完整行程生成 QPS”。

### 5.3 恶意 Prompt 注入

**共识：**

- 外部网页、评论、工具输出默认是不可信数据；
- Safety 先行；
- 高风险直接拦截；
- 中风险摘要后降权使用；
- Safety 失败默认保守拦截；
- Output Parse 失败必须 fallback。

**对齐裁决：**

> 外部内容不得直接进入 Planner 原始上下文；高风险内容直接丢弃，中风险内容摘要后使用，低风险内容保留审计记录后进入上下文。

### 5.4 断点状态检测

**共识：**

- checkpoint 是恢复锚点；
- Worker 重启后需扫描 pending、running 超时任务、残留 tmp 文件和 hash 一致性；
- 恢复优先使用最近完整 checkpoint；
- 状态不一致进入 `CORRUPTED` 或 `RECOVERY_REQUIRED`。

**对齐裁决：**

> 断点续传验收口径为：Worker 重启后 5 秒内发现可恢复任务并恢复调度；不承诺 LLM 调用 5 秒内完成。

### 5.5 高并发防重与状态挂起

**共识：**

- 使用 job 锁、文件锁、审批锁、重规划锁、request_hash 幂等键；
- 超预算或高风险内容必须进入挂起状态；
- 审批、重规划、审计日志写入必须版本化；
- 审批失败不得生效。

**对齐裁决：**

> 重复审批、重复重规划、重复任务提交均必须幂等处理；挂起不是失败，而是可恢复中间状态。

---

## 6. 需要学生裁决的问题

| 编号 | 问题 | AI-A 建议 | AI-B 建议 | 状态 |
|---|---|---|---|---|
| D1 | 是否从 Step 0 就采用异步任务架构？ | 是 | 是 | 已统一 |
| D2 | `QPS≥200` 的口径是什么？ | 提交接口 QPS | 提交接口 QPS | 已统一 |
| D3 | 是否必须物理实现 8 个独立 Agent？ | 不强制，文档保留八职责 | 不强制，MVP 可 4 Agent | 已统一 |
| D4 | `travel_plan.md` 是否作为唯一状态源？ | 否 | 否 | 已统一 |
| D5 | 断点续传 5 秒如何验收？ | 5 秒内恢复调度 | 5 秒内恢复调度 | 已统一 |
| D6 | 是否承诺实时路网与全国景点库？ | 不承诺 | 不承诺 | 已统一 |
| D7 | Prompt 注入高风险内容如何处理？ | 直接丢弃 | 直接拦截 | 已统一 |
| D8 | toB 审核台是否纳入 MVP？ | 后端挂起审批必须保留 | 后端审批必须保留，完整前端可简化 | 已统一 |
| D9 | 增量重规划是否承诺 `<3s`？ | 不承诺 | 不承诺 | 已统一 |
| D10 | Redis 是否第一版必做？ | 可文档先行，MVP 可降级 | 优先做，赶工时可降级 | 待学生确认 |

---

## 7. 已达成共识的裁决

1. **Step 0 采用异步 `job_id` 架构。**
2. **`travel_plan.md` 只作为展示产物，不是唯一状态源。**
3. **MVP 至少保留 4 个核心 Agent，文档上保留 8 类职责。**
4. **QPS≥200 只指提交接口吞吐。**
5. **断点续传以 checkpoint 为准，验收口径为 5 秒内恢复调度。**
6. **外部内容默认不可信，高风险 Prompt 注入直接拦截。**
7. **状态挂起、审批和重规划必须版本化、幂等化。**
8. **实时路网与全国景点库不纳入 MVP 硬承诺。**

---

## 8. 待确认项清单

| 编号 | 待确认项 | 说明 |
|---|---|---|
| U1 | Redis 是否在第一版就落地 | 若时间充足，建议直接做；若赶工，可先文件/DB 降级 |
| U2 | toB 审核台前端是否纳入 MVP | 后端挂起审批必须保留，完整前端可后置 |
| U3 | 8 个 Agent 是否要物理拆类 | 工程上建议可降级为 4 Agent 承担八职责 |
| U4 | 是否保留辩论式可解释和翻车预演 | 建议保留，属于高价值演示能力 |

---

## 9. 最终对齐结论

三方对齐后的统一结论是：

> 先用 1 个 Agent 跑通端到端，再扩展为多 Agent；先定 `travel_plan.md` schema，再做编排；先保证异步、状态、恢复、安全这些硬核工程点，再做玩法和彩头。最终 MVP 应以“异步任务、状态可恢复、文件不并发写、外部内容不可信、挂起可审计”为核心裁决。
