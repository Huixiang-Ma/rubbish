# 完整落地方案（现状 → 可运营产品）

> 定位：本文是"从当前代码现状出发，补齐到可对外运营"的执行方案。
> 与《融合版落地方案》《落地实施方案_真实呈现版》的区别：那两份写于批次开发前；本文以**已验证的代码现状**为基线，只列还没做到的，按"先能上线、再能赚钱、再能规模化"排序。
> 基线日期：2026-09-05。所有"已完成"项均经过运行验证，非文档宣称。

---

## 1. 现状基线（已落地 ✅ / 有库存但未接线 🔌 / 缺口 ❌）

### 1.1 已落地（✅ 可依赖，无需重做）

| 能力 | 现状 |
|---|---|
| toC 生成管线 | 9 节点（Intake→Researcher→Planner→Itinerary→Budget→Validator→Sentiment→Debate/Mood→Reporter），异步队列 + checkpoint 断点恢复 + 崩溃自动重入队 |
| 行程书产物 | Markdown 七章齐备（含费用账本/出行清单），结构化 itinerary 双渲染，sha256 原子写 |
| HITL | 预算超支/安全审核挂起-批准-续跑，409 幂等，版本号递增 |
| toB 工作台 | 八页签（列表/HITL/安全/审计/客户之声/白标/经营看板/客户管理），改单+父子 diff，toC 来源标记 |
| 数据层 | 文件事实源 + PostgreSQL 镜像已启用（plan_jobs/audit_logs/safety_events/document_chunks），启动自动建表 |
| 可观测 | Prometheus /metrics + Grafana 看板 + 结构化 JSON 日志 + 飞书审批提醒 |
| 账号 | toC 注册/登录、toB 三角色 RBAC（演示级） |
| toC 前端 | 全真实数据口径（演示数据已清零）、历史行程回看、出行人数联动预算 |
| 真实服务 | 服务大厅实时窗口（官方渠道指引口径）、高德/和风客户端已具备 |

### 1.2 有库存但 toC 前端未接线（🔌 本仓库里就有，工作量是"接"不是"建"）

| 后端已有 | 接口 | toC 前端现状 |
|---|---|---|
| 决策辩论双栏 | `GET /api/plans/{id}/debates` | 行程书第④章纯 Markdown，未用结构化双栏 |
| 直播辩论 SSE + 投票 | `GET .../debate/live`（SSE）、`POST .../debate/vote` | 体验中心"直播辩论"只有说明文字，生成中不可看、不可投票 |
| 名导团 + 对话 | `GET .../guides`、`POST .../dialogue` | 占位说明"尚未接入"（**此前判断有误**：后端能力已存在） |
| 虚拟游客踩点 | `GET .../swarm` | 同上 |
| 反事实推演 | `GET .../counterfactual` | 同上 |
| 地图与周边 | `GET .../map`、`GET .../nearby?kind=food` | 静态说明，未画图 |
| 飞猪 AI 开放平台 | `fliggy_client.py`（MCP Streamable HTTP），`FLIGGY_AI_API_KEY` 预留 | 服务大厅无真实班次/房价，只有官方渠道指引 |

> 修正在先：此前会话中我说"名导团/虚拟游客/反事实后端没有对应能力"，经复查**不准确**——`backend/app/api/experience.py` 已实现全部六个体验接口。方案据此调整：P1 的主要工作是前端接线与体验打磨，不是后端新建。

### 1.3 缺口（❌ 真正要新建的）

1. **预订/支付闭环**：无任何交易能力（也无支付牌照，需用分销/跳转模式规避，见 §4.2）。
2. **账号体系**：演示级（旅者/123456、明文演示口令），无手机号/微信登录，历史记录存 localStorage 换设备即失。
3. **生产部署**：本地单机 Docker Compose，无域名、无 HTTPS、无 ICP 备案、无 CI/CD 实效（workflow 存在但仓库未入 git）。
4. **真实数据源覆盖率**：高德/和风需配置密钥才走实时；未配置时为本地静态估算（页面有诚实标注，但商用必须全量真实）。
5. **数据卫生**：data/jobs 已积累 496 个 QA 压测任务，混在真实任务里。
6. **移动端**：toC 页面桌面优先，移动端未适配（旅游流量 80%+ 在手机）。
7. **成本与配额治理**：LLM 每单真实生成约 1-11 分钟（实测），token 成本、限流、防刷均未治理。
8. **压测与容量**：有 `scripts/load_test.py`，但没有容量结论（单机多少并发、Redis 队列积压水位）。

---

## 2. 落地路线：四个阶段

```
P0 清障加固（1 周）→ P1 体验与数据闭环（2-3 周）→ P2 商业化前置（3-4 周）→ P3 上线运营（并行推进）
                                    ↘ P3 的域名/备案/合规在 P1 期间就要启动（备案周期 1-4 周）
```

### P0 清障与加固（1 周，不上线也能做）

| # | 任务 | 做法 | 验收 |
|---|---|---|---|
| 0.1 | 数据清理 | QA 压测单归档：`data/jobs` 中 `constraints` 含 `rbac-notoken401` 等标记的移入 `data/jobs_archive/`（不删，保留审计） | `GET /api/plans` 只剩真实任务 |
| 0.2 | 安全基线 | 改掉演示口令；`AUTH_ENABLED=true`；`.env` 移出版库（入 secret 管理）；Flask/FastAPI debug 关闭；CORS 收紧到自有域名 | 渗透自查清单通过 |
| 0.3 | 数据库兜底验证 | PG 镜像写失败演练（停 postgres 跑一单，确认主流程不受阻）；把"启动日志 pg_mirror:false 造成误读"改为就绪后回写真实状态 | 日志/文档口径一致 |
| 0.4 | git 入库 + CI | 仓库 `git init` + 推远端，激活 `.github/workflows/ci.yml`；镜像构建推 registry | push 即出镜像 |
| 0.5 | 容量压测 | `scripts/load_test.py` 三档（10/50/200 并发提交），记录：队列积压水位、Worker 吞吐、LLM 并发限制下的排队时长 | 产出《容量基线》一页纸 |
| 0.6 | 备份 | data/jobs + PG 每日定时备份（compose 加 backup 服务或宿主 cron） | 恢复演练一次 |

### P1 真实数据与体验闭环（2-3 周，产品成型关键）

**目标：用户从"生成一本行程书"到"行前行中都用得上"，全部真实数据。**

| # | 任务 | 做法 | 验收 |
|---|---|---|---|
| 1.1 | 飞猪真实票务接入 | 配置 `FLIGGY_AI_API_KEY`，服务大厅五窗口改走 `fliggy_client`（已有 MCP 客户端）：真实车次/航班/酒店/门票，带"实时价以供应商为准"；无 key 降级现有官方渠道口径 | 任选目的地可见真实可查的班次与报价 |
| 1.2 | 高德/和风全量真实化 | 配 `AMAP_KEY`/`QWEATHER_API_KEY`，行程内通勤/天气/周边全部实时；未配密钥的环境显式降级提示 | 行程书数据来源标注全部为实时 |
| 1.3 | 生成中直播辩论 | toC 生成弹层接入 `GET /debate/live`（SSE）：实时滚动正反方对质；接 `POST /debate/vote`：用户中途投票真实影响裁决走向 | 生成时可看可投，裁决含用户投票影响记录 |
| 1.4 | 体验中心四模块接线 | 名导团（`/guides`+`/dialogue` 对话）、虚拟游客（`/swarm`）、反事实（`/counterfactual`）、地图周边（`/map`+`/nearby`，Leaflet 已在 vendor）逐个接真实 per-job 接口，替换占位说明 | 六模块无一"说明性占位" |
| 1.5 | 行程书分享 | 生成后生成只读分享页（`/s/{job_id}`，免登录、白标水印），toC 传播基本盘 | 微信内打开可读 |
| 1.6 | 移动端适配 | toC 单页做响应式（表单/时间轴/弹层三处断点），真机过一遍 | 375px 宽度全流程可用 |
| 1.7 | 章节结构化 | 第④章辩论、③账本改用结构化接口渲染（双栏正反方、分项表格），替代 Markdown 提取 | 行程书更接近原型稿观感 |

### P2 商业化前置（3-4 周）

| # | 任务 | 做法 | 说明 |
|---|---|---|---|
| 2.1 | 真实账号体系 | 手机号验证码 + 微信扫码登录（替换演示门禁）；历史行程云端化：`plan_jobs` 按用户 ID 归属，历史列表改服务端查询（`GET /api/plans?user=`），localStorage 只做缓存 | 解锁跨设备；也是归因/复购的前提 |
| 2.2 | 预订转化（轻闭环） | **不做自营代订**（需牌照/资金池）。两条路：a) 飞猪分销 CPS——票/房带追踪参数跳转，成交返佣；b) 酒店直连谈协议价后置 | 商业模式：先 CPS 佣金 + toB 白标订阅 |
| 2.3 | toB SaaS 化 | 多租户已有字段（`tenant`）做实：租户隔离、品牌白标导出计费、顾问席位管理；从"演示工作台"升级为"可卖给旅行社的顾问工具" | 对标竞品携程商旅"程星途"，差异化在辩论可解释 + HITL |
| 2.4 | 成本治理 | LLM 用量埋点（每单 token/时长已有基础）：按单成本看板；免费额度（如 3 单/日）+ 超量登录；限流（IP/用户级 429 已有 WAITING_RATE_LIMIT 状态位） | 毛利可算，防刷 |
| 2.5 | 内容合规 | 生成内容审核走已有 safety_service 全量；用户 UGC（反馈/对话）敏感词过滤；行程书页脚"AI 生成，价格以供应商公示为准"声明（已有，固化为法务口径） | 上线合规底线 |

### P3 上线与运营（P1 起并行启动）

| # | 任务 | 要点 |
|---|---|---|
| 3.1 | 云部署 | 单云主机（4C8G 起步）跑现有 Compose 拓扑即可支撑容量基线（多 Worker + Redis 已就绪）；量大再迁 K8s，架构无需重写 |
| 3.2 | 域名/备案/HTTPS | **P1 期间立即提交 ICP 备案**（1-4 周）；Caddy/Nginx + Let's Encrypt；`/b` 加 IP 白名单或 VPN（企业端不公开） |
| 3.3 | CI/CD | git push → CI 出镜像 → 主机 `docker compose pull && up -d`；数据卷先备份后发布 |
| 3.4 | 告警 | Prometheus 告警规则：队列积压、任务失败率、LLM 错误率、PG/Redis 存活 → 飞书机器人（notify 通道已有） |
| 3.5 | 种子运营 | toB：找 2-3 家中小旅行社/定制师免费试用换反馈（白标导出是钩子）；toC：小红书/抖音"看 AI 吵架帮你做行程"内容切口——直播辩论是天然素材 |

---

## 3. 里程碑与节奏（总计约 8-10 周到可运营）

| 时点 | 交付 |
|---|---|
| W1 末 | P0 完成：数据干净、CI 出镜像、容量基线一页纸 |
| W2 起 | ICP 备案提交；云主机就绪 |
| W4 末 | P1 核心完成：真实票务 + 直播辩论 + 四模块接线 + 分享页 → **内测版上线**（备案下来前用测试域名/内网穿透小范围） |
| W6 末 | 移动端 + 账号体系 → **公测** |
| W8-10 | CPS 转化 + toB 试用 → **可运营版本**，开始谈第一个白标合同 |

人力口径：当前为单人全栈节奏，P1 建议至少补一名前端（1.3-1.7 全是前端工作量）；LLM 成本按每单 0.5-2 元预估，免费额度策略下千级 DAU 月成本可控在千元级。

---

## 4. 关键决策与风险

### 4.1 已定决策（本方案依赖）
- **不填演示数据**：全站真实数据口径已确立（2026-09-05 会话完成），后续任何新模块一律"有真实数据才展示"。
- **PG 是镜像不是主库**：文件事实源 + PG 审计镜像的架构保持，商业化阶段（2.1）把用户归属写入 state/PG 后，历史查询仍走文件索引（job_index 已能扛，启动预热已修）。
- **票务不自营**：CPS 分销 + 跳转，规避牌照与资金风险。

### 4.2 风险清单

| 风险 | 影响 | 对策 |
|---|---|---|
| 飞猪 MCP 接口能力/配额不足（未实测） | P1.1 排期失准 | 第一周先做接口探测 spike（半天），不行就退回"官方渠道指引"口径并找智行/同程等替代分销源 |
| LLM 生成时长 1-11 分钟 | toC 转化漏斗 | 直播辩论把等待变内容（P1.3）；进度浮标已有；长单（>5 天）考虑拆节点流式出稿 |
| 单人维护 + 496 压测数据此类卫生问题复发 | 线上数据污染 | P0.1 归档脚本固化成 `scripts/archive_qa_jobs.py`，压测一律打 `qa:` 前缀 constraint |
| 备案周期不可控 | 公测延期 | P1 用境外节点/内网穿透做内测，与备案并行 |
| 演示口令泄露史（旅者/123456 已公开于文档） | 上线后被扫 | P0.2 强制改密 + 上线前轮换全部密钥 |

### 4.3 验收定义（"完整落地"的完成线）
1. 公网域名 HTTPS 可访问，移动端全流程可完成"输入→生成→直播辩论→回看→分享"；
2. 服务大厅展示真实可预订供应商数据，CPS 链路出第一笔佣金；
3. toB 至少 1 家外部旅行社在用工作台（白标导出产生真实交付物）；
4. 全部任务/审计可在 PG 查到，告警能自动叫人；
5. 每单 LLM 成本可见、有免费额度与限流兜底。

---

## 5. 立即行动（本周）

1. 跑飞猪 MCP 接口探测 spike（决定 P1.1 走向）——半天；
2. 提交 ICP 备案材料——当天；
3. QA 任务归档脚本 + 安全基线四项（0.1/0.2）——一天；
4. git init + 远端 + CI 激活（0.4）——半天；
5. 招募/确认前端协作人（P1 前端工作量大头）。
