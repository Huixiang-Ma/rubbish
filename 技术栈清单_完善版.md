# 多 Agent 文旅系统 · 技术栈定版（完善版）

> 版本：v2.2（定稿）｜日期：2026-08-30 ｜ v2.2 变更：生产演进六项全部落地实测（PG 镜像/pgvector/PEL 重投递/多 Worker 分布式锁/Prometheus+结构化日志/审批通知/CI），本阶段引入两项落地（httpx/pydantic-settings），扩展点接口化
> 输入依据：《任务二执行方案.md》《核心技术架构深度剖析手册.md》《落地实施方案_真实呈现版.md》《融合版落地方案.md》+ 实际代码核对（requirements.txt / docker-compose.yml / Dockerfile / app 源码 / frontend）
> 三档口径：**已落地**（当前代码在用）、**本阶段引入**（随本次完善引入，配套文件已建）、**生产演进**（按预留边界，触发条件到了再做）。
> 配套文件：`backend/requirements-dev.txt`（工程依赖分层）、`.env.example`（全部环境变量说明）。

---

## 0. 选型总原则（沿用架构手册）

1. **mock-first**：LLM、地图、舆情等外部依赖全部可降级，断网主链路可演示；
2. **私有化数据不出域**：本地文件 + docker compose 即可完整运行；
3. **先闭环后加厚**：先保证状态、安全、恢复三条主线，再补工具链和玩法；
4. **不为凑数引入依赖**：生产依赖保持最小集，开发工具全部隔离在 dev 层。

## 1. 后端选型（定版）

| 类别 | 定版选型 | 版本 | 状态 | 理由与备选说明 |
|---|---|---|---|---|
| 语言 | Python | 3.12（`python:3.12-slim`） | 已落地 | 与 Dockerfile 一致；备选 3.11 无必要收益 |
| Web 框架 | FastAPI + Uvicorn | 0.112.2 / 0.30.6 | 已落地 | 异步 + 自动 OpenAPI 文档；备选 Flask/Django 缺原生 async 与 schema 校验 |
| 数据校验 | Pydantic | 2.8.2 | 已落地 | 请求/响应与 Agent JSON schema 双用；v1 不兼容不回退 |
| 任务队列 | InMemory（`deque+Lock`）+ Redis Streams 双实现 | redis-py 5.0.8 / Redis 7-alpine | 已落地 | 本地零依赖演示，Docker 用 Streams；PEL 自动重投递留到生产演进 |
| 状态存储 | 本地 `state.json` checkpoint | — | 已落地 | MVP 口径；替换边界已预留（见 §5） |
| LLM 接入 | OpenAI 兼容 `/chat/completions`，`real/mock` 双模式，失败降级 mock | — | 已落地 | 供应商无关（通义/DeepSeek/OpenAI 均可）；不绑定官方 SDK，保留 mock-first |
| LLM HTTP 客户端 | httpx ≥ 0.27（已完成 urllib 迁移） | httpx ≥ 0.27 | 已落地 | 连接池 + 统一超时；不绑定官方 SDK，保留"任意 OpenAI 兼容供应商"口径 |
| Worker 模型 | `threading` 后台线程 + 启动恢复扫描 | — | 已落地 | MVP 单 Worker；多 Worker 与分布式锁见 §5 |
| 安全防御 | 关键词规则 + 正则（+ 可选 LLM 分类）；Prompt 隔离模板 | — | 已落地 | 不训练小模型；`SafetyService` 四接口边界已预留 |
| 时空校验 | 静态景点库经纬度 + haversine + 路网系数 1.4 | — | 已落地 | 只算不接，不承诺实时路网 |
| 流式输出 | FastAPI `StreamingResponse`（`text/event-stream`） | — | 已落地 | C6 直播辩论；备选 WebSocket 无必要 |
| 工程防御 | AtomicWriter（.tmp→fsync→rename→sha256）、幂等键、`base_version` 乐观锁（重复审批 409）、append-only 审计 | — | 已落地 | 分布式锁体系随 Redis 模式预留 |
| 部署 | Docker + docker compose | — | 已落地 | Redis AOF + `data/jobs` 挂载，端口 env 可覆盖 |
| 出行服务演示层 | 静态模板 + 确定性生成（名称 hash 作种子）+ FastAPI 参数化路由 `/api/services/{kind}` | — | 已落地 | 车票/机票/商家/景点/娱乐五窗口；竞品形态吸收，"只算不接"，无真实交易 |
| 启动恢复 | `recover_pending_jobs` 扫描 + 重新入队 + `RECOVERY_REQUIRED` | — | 已落地 | QUEUED/RUNNING 重挂载；state.json 损坏进人工恢复 |
| 页面认证 | HMAC 签名 token + 本地用户文件 + 内置演示账号 + `AUTH_ENABLED` 可选 API 门禁 | — | 已落地 | 演示级门禁：toC 山水登录页 / toB 工作台登录页，注册落 `auth_users.json` |

## 2. 前端选型（定版）

### 2.1 当前阶段（已落地，功能闭环前不迁）

| 类别 | 定版选型 | 状态 |
|---|---|---|
| 页面 | 原生单文件 HTML/JS：`index.html`（toC）+ `admin.html`（toB），FastAPI 同源托管（`StaticFiles` + `HTMLResponse`） | 已落地 |
| 数据请求 | 原生 `fetch` 轮询 + `EventSource`（SSE 直播辩论）+ `localStorage`（apiBase） | 已落地 |
| 图表 | ECharts（本地 vendor，断网可跑） | 已落地 |
| 地图 | Leaflet + OpenStreetMap 瓦片，断网降级 SVG 示意 | 已落地 |
| 导出 | html2canvas PNG 长图 / 打印 CSS PDF / CSV（后端接口） | 已落地 |
| 语音 | 浏览器 Web Speech API（本地 TTS） | 已落地 |
| UI 框架模式 | 弹窗式交互（modal-overlay + 页签懒加载）：行程书/体验中心/确认/改行程/服务大厅五弹窗，主页面仅表单+我的行程 | 已落地 |
| 中文呈现 | 状态机值 → 中文映射（`STATUS_CN`），Agent 节点 → 中文（`NODE_CN`），toB 行程标识 = `出发地 -- 目的地` | 已落地 |
| 响应式 | `@media` 断点（760px/980px）双列折叠；移动端可用但非本阶段重点 | 已落地 |

**决定**：维持单文件方案直到 S/C/B 全部验收（融合版落地方案 §8.1 偏差已声明）；单文件膨胀的唯一缓解手段是 admin 与 toC 分离，已执行。

### 2.2 迁移阶段（触发条件：批次 5 完成或 toC 页面代码量失控）

| 类别 | 定版选型 | 说明 |
|---|---|---|
| 构建 | Vite 5 | dev server 代理 `/api` 至 FastAPI |
| 框架 | React 18 + TypeScript 5 | 与任务二执行方案 §9 目录规划一致 |
| 路由 | React Router | `/`（toC）与 `/b`（toB）双端 |
| 服务端状态 | TanStack Query | 替代手写 fetch 轮询；SSE 用原生 EventSource + invalidation |
| 客户端状态 | Zustand | 轻量；mood/apiBase 全局配置 |
| Markdown 渲染 | `react-markdown` + `remark-gfm` | travel_plan.md 表格/标题正常渲染（现为文本直出） |
| 图表/地图/导出 | **复用现有 vendor**（ECharts / Leaflet / html2canvas）封装 hooks | 维持"断网可跑"口径，不换库 |
| 质量/测试 | ESLint + Prettier；Vitest + Testing Library；E2E Playwright | 覆盖五条演示链路 |

## 3. 多 Agent 架构（定版口径）

- **职责**：八类职责（Researcher / Planner / Itinerary / Budget / Sentiment / Safety / Validator / Reporter），实现允许合并；当前 5 Agent（+Debate）+ Mood 条件节点，答辩口径为"4 核心 Agent 承担 8 职责，扩展按同一模式"。
- **推理范式**：CoT 内部拆解不外露 + ReAct（规划—行动—观察—校验）；输出一律结构化 JSON。
- **解析降级链**：schema parse → JSON 修复器 → fallback prompt 重生成 → `WAITING_PARSE_REVIEW`。
- **状态机**：QUEUED → RUNNING →（4 种 WAITING 挂起）→ COMPLETED / FAILED / CORRUPTED。
- **恢复**：checkpoint + `resume_from` + 启动恢复扫描；验收口径"重启后 5 秒内恢复调度"。
- **HITL**：审批带 `base_version`，重复审批 409；增量重规划节点级复用。

## 4. 工程配套（本阶段引入，配套文件已建）

| 类别 | 定版工具 | 落地文件 | 用法 |
|---|---|---|---|
| 测试 | pytest ≥ 8 + pytest-asyncio + httpx（AsyncClient） | `backend/requirements-dev.txt` | 补自研 smoke/load 脚本缺的单测层；smoke_test 保留为验收口径 |
| HTTP 客户端（测试/后续 LLM） | httpx ≥ 0.27 | 同上 | 先进测试层，LLM 客户端迁移时复用 |
| Lint/Format | ruff ≥ 0.6 | 同上 | `ruff check app scripts` / `ruff format` |
| 类型检查 | mypy ≥ 1.11 | 同上 | 首轮只覆盖 `app/services` 与 `app/models` |
| 配置管理 | pydantic-settings（`app/config.py` Settings 单例 + LLM 运行时直读） | pydantic-settings ≥ 2.4 | 已落地 | 全部环境变量集中声明；`.env.example` 同步 |
| 依赖安全 | pip-audit ≥ 2.7 | 同上 | 私有化交付前必跑 |
| Git 钩子 | pre-commit ≥ 3.8 | 同上 | ruff + 基础检查 |
| 环境变量文档 | `.env.example`（根目录） | 已建 | 16 个 env 变量（含 DATA_ROOT 注释行）与 compose / 代码默认值一一对应 |
| 验收脚本矩阵 | `smoke_test.py`（7 链路）、`load_test.py`（提交压测）、`bench_replan.py`（增量对比）、`llm_offline_test.py`（假 OpenAI 服务器） | 已落地 | 自研验收口径；与 §4 单测层互补 |

**依赖分层原则**：生产镜像只装 `backend/requirements.txt`（4 个锁定包）；开发工具一律进 `requirements-dev.txt`，不进 Dockerfile。

## 5. 生产化演进（按预留边界，触发条件明确才做）

| 方向 | 定版选型 | 触发条件 |
|---|---|---|
| 数据库 | PostgreSQL + SQLAlchemy 2 + Alembic + pgvector（pgvector/pgvector:pg16） | ✅ 已落地（本次）：state.json 仍为事实源，PG 为审计/任务/安全事件镜像层 + document_chunks 语义检索；`DATABASE_URL` 未配置时整层 no-op，延迟初始化自愈 |
| 向量检索 | pgvector + `/api/semantic` 接口（mock embedding，接口已定型） | ✅ 已落地（演示级）：真 embedding 模型随 LLM key 接入 |
| 队列可靠性 | XAUTOCLAIM 重投递 + ack-after-process + 消费幂等（COMPLETED 短路 + 分布式任务锁） | ✅ 已落地（本次）：at-least-once 语义，PEL 重投递实测通过 |
| 多 Worker | `WORKER_COUNT` 多线程 + Redis 分布式锁（SET NX PX + Lua 释放） | ✅ 已落地（本次）：WORKER_COUNT=2 实测 |
| 可观测 | 结构化 JSON 日志（job_id 贯穿全事件）+ Prometheus `/metrics` + Grafana（compose `--profile observability`） | ✅ 已落地（本次）：计数器/直方图实测，采集配置齐 |
| 审批通知 | 飞书 webhook（`FEISHU_WEBHOOK_URL`）已实现，邮件接口占位 | ✅ 代码落地（通知未配置时静默跳过；真实值班场景接入 webhook 即生效） |
| CI/CD | GitHub Actions（`.github/workflows/ci.yml`：compileall + 基础 ruff + smoke + llm_offline + docker build） | ✅ 工作流已建；`git init` 入库推送后自动生效 |

## 6. 明确不接入（MVP 边界，文档规定）

地图 API、搜索 API、OCR API、舆情 API、短信/邮件 API、对象存储 API、支付/订单 API（支付禁止纳入 MVP）——只保留扩展点。

## 7. 版本基线

- **生产锁定**：`fastapi==0.112.2`、`uvicorn[standard]==0.30.6`、`pydantic==2.8.2`、`redis==5.0.8`；镜像 `python:3.12-slim`、`redis:7-alpine`。
- **开发基线**：`requirements-dev.txt` 采用下限版本（`>=`），首次全量安装后可收紧为 `==` 锁定。
- **基础设施**：compose 内 Redis 开 AOF；backend 无状态，状态全在 `data/jobs` 挂载卷。

## 8. 文档规划 vs 当前实现差异速查

| 项 | 文档规划 | 当前实际 | 处置 |
|---|---|---|---|
| 前端框架 | React + TS + Vite | 单文件原生 HTML | 维持至批次 5，迁移选型见 §2.2 |
| 队列降级 | SQLite / JSON 任务表 | 未实现（内存队列兜底） | 接受现状，生产演进补 PEL |
| PostgreSQL / SQLite | 预留 / 降级 | 未接入 | 按 §5 触发条件 |
| 向量化 / 向量库 | 轻量 embedding + 向量库 | 未实现 | 按 §5 触发条件 |
| 多 Worker 分布式锁 | Redis 锁体系 | 内存模式未启用 | 按 §5 触发条件 |
| SSE / ECharts / Leaflet / html2canvas | 使用 | ✅ 已落地 | 无需动作 |
| 旅行服务窗口（车票/机票/商家/景点/娱乐） | 竞品形态吸收（飞猪票务/携程品类/原型"看看附近+文博场馆"） | ✅ 演示数据落地 | 无真实交易，诚实标注 |
| 前端信息架构 | 单页堆积 | ✅ 已改弹窗式（五弹窗），toB 内容不进 toC | 无需动作 |
| 测试/质量工具链 | 文档未规定 | ✅ 本阶段引入 | 见 §4 |

---

## 9. 全量技术对照清单（v2.1 新增：代码 ↔ 文档双向核对基准）

> 审计口径：代码里用到的每一项技术都必须在本清单出现；本清单每一项都必须能在代码里找到。v2.1 审计通过。

### 9.1 后端模块（backend/app）

| 模块 | 技术/职责 |
|---|---|
| `main.py` | FastAPI 应用、四业务路由器 + 页面路由（`/` toC、`/b` toB）+ `/vendor` 静态挂载 + 启动恢复 |
| `api/plans.py` | 任务 CRUD、审批（乐观锁 409）、增量重规划（节点复用）、列表、审计、反馈、白标导出 |
| `api/experience.py` | C2 双栏/C3 swarm/C4 后悔药/C5 名导团+对话/C6 直播（SSE）+投票 |
| `api/admin.py` | B2 待审队列、B4 审计 CSV、B6 经营统计 |
| `api/safety.py` | B3 看板聚合 |
| `api/services.py` | 旅行服务大厅五窗口（演示口径） |
| `agents/` | Researcher/Planner/Itinerary/Validator/Debate/Mood 六 Agent + AgentBase 抽象 |
| `services/` | llm_client（real/mock 双模）、queue_client（内存+Redis Streams）、checkpoint_store、atomic_writer、safety_service、scenic_spot_service、travel_context_service（时空/出行要素/服务大厅）、experience_service、recovery、markdown_reporter、paths |
| `workers/plan_worker.py` | 后台线程消费循环 |
| `models/` | Pydantic schemas（含 origin/customer/tenant/mood）+ JobStatus 状态机枚举 |
| `data/scenic_spots_beijing.json` | 北京 12 景点静态库（含经纬度/票价） |

### 9.2 前端（frontend/）

| 文件 | 技术/职责 |
|---|---|
| `index.html` | toC 单文件：文旅主题 CSS（宣纸/黛青/朱砂/金）、弹窗式五弹窗、进度条、ECharts 无、Leaflet 地图、html2canvas 长图、EventSource 直播、localStorage 历史/收藏 |
| `admin.html` | toB 单文件：六页签工作台、ECharts 四图、KPI 卡、中文状态/行程标识、租户过滤 |
| `vendor/` | echarts.min.js、leaflet.js/css + images/（标记图标）、html2canvas.min.js —— 全部本地化，断网可跑（OSM 瓦片除外，自动降级） |

### 9.3 脚本（backend/scripts）

| 脚本 | 用途 |
|---|---|
| `smoke_test.py` | 七链路验收（内存/Redis 双模式） |
| `load_test.py` | 提交接口压测（QPS 口径） |
| `bench_replan.py` | S4 增量 vs 全量耗时对比 |
| `llm_offline_test.py` | LLM real 模式解析与降级离线验证 |

### 9.4 部署与文档

| 项 | 内容 |
|---|---|
| `Dockerfile` | python:3.12-slim，复制 backend/app + frontend，uvicorn 启动 |
| `docker-compose.yml` | Redis 7-alpine（AOF + healthcheck）+ backend；REDIS_HOST_PORT/BACKEND_HOST_PORT 可覆盖 |
| 文档体系 | MEMORY.md（索引）、README.md（运行/验收/演示）、任务一/任务二/需求WBS/实现顺序/架构手册/三方互评（任务一体系）、融合版需求+落地方案、落地实施方案_真实呈现版、技术栈清单（本文件） |
