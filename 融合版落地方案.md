# 融合版落地方案（S/C/B 现状映射 + 批次计划）

> 版本：v1.1 ｜ 日期：2026-08-29
> 输入依据：《融合版需求文档_竞品优点+创新点+可实现.md》+ AxureShop《地区文旅小程序原型》参考（46 页原型导航树）
> 结论先行：**S 系列 8 项全部落地（含 S4 节点级增量实测）；C2/C8 已实现；出行要素已由本地静态估算引擎承接；toB 工作台（B1-B5）完成并拆分 `/`（toC）与 `/b`（toB）双路由。S/C/B 三系列全部落地：融合版需求除"多语言占位/内容位"两个小尾巴外全部完成，C 系列内容在 LLM key 配置后自动从模板切真。**

---

## 1. S 系列 · 共享底座（现状盘点）

| ID | 需求 | 现状 | 落点 | 剩余动作 |
|---|---|---|---|---|
| S1 | 八 Agent 协同编排 | ✅ 5 Agent（Researcher/Planner/Itinerary/Validator/Debate）流水线，输出可观测 | `app/agents/`、`plan_processor.py` | 玩法按同一模式扩 |
| S2 | 结构化 travel_plan.md | ✅ 8 章节：需求摘要/每日行程/交通住宿/校验分项/决策辩论/翻车预演/工程说明/结构化索引 | `markdown_reporter.py` | B5 白标时补企业头部 |
| S3 | 时空可行性校验 | ✅ **已由硬编码升级为经纬度计算**：haversine+路网系数估算通勤，同日最长段超 90 分钟判 spatial_conflict（⚠️ 静态估算口径） | `travel_context_service.py`、`validator.py` | 无 |
| S4 | 增量重规划 | ✅ 节点级增量：replan 复用 Researcher/Planner/Itinerary（不读 constraints），mock 实测耗时降约 23%，LLM real 下放大 | `api/plans.py replan`、`scripts/bench_replan.py` | 无 |
| S5 | 断点续传 | ✅ 启动恢复 + resume_from + RECOVERY_REQUIRED，smoke 覆盖 | `recovery.py` | 无 |
| S6 | 红蓝对抗安全闭环 | ✅ 拦截 + 审计 + 计数 + 拦截率/攻击分布/按天曲线 | `safety_service.py`、`api/safety.py` | 无 |
| S7 | 私有化·数据不出域 | ✅ 本地文件 + docker compose，LLM real 失败降级 mock | 全局 | 无 |
| S8 | QPS≥200 | ✅ 实测 245.8 RPS（0 错误） | `scripts/load_test.py` | 无 |

## 2. C 系列 · toC 游客端

统一模式：**Agent 输出扩展 + Reporter 章节渲染**；`LLMClient.try_generate_json`（real）失败落确定性 mock 模板。

| ID | 需求 | 现状 | 实现方式 | 批次 |
|---|---|---|---|---|
| C1 | 行程生成与追问 | ✅ 基础（表单 + replan 追问） | 心情词随 C7；追问增强随 B1 | 3 |
| **C2** | **辩论式可解释** | ✅ **已实现**：三议题规划方 vs 游客方论点+结论 | `agents/debate.py` | 1 |
| C3 | 虚拟游客 swarm | ✅ `GET /{id}/swarm`：3 persona 引用真实通勤/强度数据反馈 | 4（完成） |
| C4 | 反事实后悔药 | ✅ `GET /{id}/counterfactual`：引用分项预算对照卡 | 4（完成） |
| C5 | 角色名导团 | ✅ `GET /{id}/guides` + `POST /{id}/dialogue`：杜甫/吃货/亲子妈妈点评+多轮对话 | 4（完成） |
| C6 | 直播辩论·当裁判 | ✅ `GET /{id}/debate/live` SSE 流式 + 投票写回 travel_plan.md | 4（完成） |
| C7 | 心情→人生剧本 | ✅ `PlanRequest.mood` → MoodAgent 条件节点 → 情绪弧线章节 | 4（完成） |
| **C8** | **诚实 AI 翻车预演** | ✅ **已实现**：≥3 条数据驱动翻车预警 + B 计划（天气/通勤超时引用真实估算数字/闭馆替补/超预算走 HITL） | `validator.py what_if` | 2 |
| C9 | 攻略长图 | ✅ html2canvas 一键导出 PNG（含标题区） | 5（完成） |
| C10 | 以图规划 | ✅（⚠️）上传图确定性附加偏好标签，标注"演示 mock" | 5（完成） |
| C11 | 演示彩头 | ✅（⚠️ 空壳）旅行盲盒 + 足迹统计，标注"行业标配" | 5（完成） |

## 3. B 系列 · toB 企业端

| ID | 需求 | 现状 | 实现方式 | 批次 |
|---|---|---|---|---|
| B1 | 顾问工作台 | ✅ `GET /api/plans` 列表+筛选 + `frontend/admin.html` 方案列表页签（客户归属 `customer`） | 3（完成） |
| B2 | HITL 审核台 | ✅ `GET /api/approvals/pending` + admin 独立审核台页签（confirm/reject/操作人） | 3（完成） |
| B3 | 安全治理看板 | ✅ 扫描计数/拦截率/攻击模式分布/按天曲线（ECharts 本地 vendor） | 3（完成） |
| B4 | 合规审计留痕 | ✅ 单任务审计 API + 全量 CSV 导出 + 好评/投诉反馈通道 | 3（完成） |
| B5 | 白标交付 | ✅ `POST /api/plans/{id}/export`：品牌/署名注入 + 交付留痕 | 3（完成） |
| B6 | 经营看板 | ✅ `GET /api/stats/overview` + admin 三图表（真实任务聚合：趋势/状态/客户） | 5（完成） |
| B7 | 多租户 | ✅（⚠️）`tenant` 字段 + 列表/待审/统计过滤（演示级隔离） | 5（完成） |

## 4. 出行要素（用户补充 · 原文档 ❌ 档的形态吸收）

由本地静态估算引擎 `travel_context_service.py` 统一承接，**只算不接**（不碰实时路网/真实票价/真天气）：

| 元素 | 实现 | 口径 |
|---|---|---|
| 地图/通勤 | 景点经纬度 + haversine + 路网系数 1.4 + 地铁/打车/步行速度模型 → 每段行程"地铁约 X 分钟 / 打车约 Y 分钟 Z 元" | 本地估算，非实时 |
| 打车 | 起步价 13 + 2.3 元/公里估算，用于交通对比与市内交通分项 | 估算 |
| 天气 | 四季风险提示静态样例，进行程"交通与住宿参考"章节 | 演示样例 |
| 大交通（机票/火车） | 10 城演示表：高铁/飞行时长 + 往返费用估算 + 高铁/飞机决策建议；`PlanRequest.origin` 可选输入 | 演示决策建议，非报价 |
| 住宿 | 预算分档（经济 300/舒适 600/品质 1000）+ 位置策略（靠近次日核心景点） | 演示分档，非预订 |
| 门票/分项预算 | 景点静态票价 + 分项预算（大交通/住宿/市内交通/门票/餐饮），Validator 按合计判超预算 | 演示口径 |

## 5. LLM 接入（批次 1 完成）

- `LLMClient`：`LLM_MODE=real|mock`（默认 mock）；real 走 OpenAI 兼容 `/chat/completions`，env：`LLM_BASE_URL/LLM_API_KEY/LLM_MODEL/LLM_TIMEOUT_SECONDS/LLM_MAX_RETRIES/LLM_TEMPERATURE`。
- 失败纪律：real 调用任何失败返回 None → 调用方落确定性 mock 模板；断网/无 key 主链路照常（S7）。
- 离线验证：`scripts/llm_offline_test.py`；**真实调通需用户配置 API key**。

## 6. 批次计划（v1.1，出行要素已融入）

| 批次 | 内容 | 状态 |
|---|---|---|
| 1 | LLM real provider + C2 辩论式可解释 | ✅ 完成 |
| 2 | 出行要素引擎（S3 升级/通勤/打车/门票/分项预算/大交通/天气/住宿）+ C8 翻车预演 | ✅ 完成 |
| 3 | toB：B1 顾问台 + B2 独立审核台 + B4 审计导出（含好评/投诉反馈通道）+ B3 看板增强 + B5 白标 + S4 节点级增量；路由拆分 `/`（toC）与 `/b`（toB） | ✅ 完成 |
| 4 | toC 玩法：C3 swarm + C4 后悔药 + C5 名导团（名人对话）+ C6 直播辩论（SSE+投票）+ C7 心情剧本 | ✅ 完成 |
| 5 | P2：C9 长图、C10 以图（mock）、C11 彩头、B6 经营看板、B7 多租户（演示级）+ 周边服务（找美食/车位/厕所/住宿，静态库）+ 景区舒适度（演示）+ 我的行程/收藏 + 多语言占位 | 待做 |
| 持续 | 录屏分镜随批次更新 | 每批同步 |

## 7. 原型参考吸收矩阵（AxureShop《地区文旅小程序原型》46 页）

| 原型页面/能力 | 借鉴方式 | 融合落点 |
|---|---|---|
| 行程定制 → 计算行程中 → 结果 → 选择景点 | 异步生成 + 结果确认交互 | 已有（异步 job + 结果页）；"选择景点"人工筛选进批次 3 B1 |
| 行程定制结果-**地图模式** | 地图化展示 | 批次 2 已有通勤数据底座；地图渲染（SVG 静态示意，断网可跑）批次 5 |
| 文博场馆/博物馆详情/**参观须知** | 景点详情扩展（预约要求/须知） | 批次 2 翻车预演已引用"故宫需提前预约"；字段化批次 5 |
| 互动 → **名人选择/名人对话** | 文化 IP 数字人对话 | 批次 4 C5 名导团（persona 点评+对话） |
| 服务 → 游玩必备/**地图导览/景区舒适度** | 拥挤度/导览 | 批次 5（演示数据） |
| **看看附近**：找景点/美食/车位/厕所/住宿 | 周边静态库 | 批次 5（住宿已在批次 2 承接分档建议） |
| 公共服务：文创/文化活动/智慧书房/公告 | 内容运营位 | 批次 3 B1 内容管理位（演示数据） |
| 我的：收藏/我的行程/**好评/投诉**/切换语言 | 用户中心 + 反馈通道 | 批次 5 前端；投诉/好评并入批次 3 B4 反馈通道 |
| 后台可视化编辑 | 内容管理 | 批次 3 B1（演示级） |
| 投诉与咨询通道 | 服务闭环 | 批次 3 B4 |

## 8. 与需求文档的偏差诚实声明

1. **前端栈**：文档写 React+TS+Vite；当前用最小单文件 HTML（toC）+ 计划中 admin.html（toB），功能闭环后再评估迁移。
2. **C2 双栏**：md 为结构化条目；真双栏渲染随批次 3/4 前端改造。
3. **S4 耗时≥70%**：批次 3 节点级复用后实测；当前不谎称达标。
4. **LLM 真实调通**：代码路径已离线验证，供应商实测需用户配置 key。
5. **出行要素**：全部为本地静态估算/演示样例，不写"已对接高德/真实数据"。
6. **⚠️/❌ 档**：按原文档第 7 节口径标注。
